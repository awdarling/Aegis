# QURIA — Development Roadmap & Progress Tracker

**Living document. Last updated: June 19, 2026.**

This is the operational source of truth for active development. It is meant to be read and updated by Claude (Claude Code / Cowork) every session.

> ## ▶ COMMITTED ORDER (reset 2026-06-23, Alexander) — THE THROUGH-LINE IS CONFIGURABILITY: nothing hardcoded, nothing that silently fails
> ### 🎯 HARD DEADLINE: JULY 1, 2026 — INVESTOR DEMO. The bar: the EMAIL system demos flawlessly end-to-end, with SMS research done in parallel so it's a ready-to-pull bonus for the first clients the investor introduces. "Bulletproof email so we can sell" is the win condition; SMS is the follow-on, not a blocker.
> *These 3 are locked, in this order. The full ranked backlog is below. After the deadline plan is set, update ALL dev docs for operational consistency.* 
>
> **1. ENGINE-CONFIG-1 — the scheduling engine must be fully configurable; no shift/role is special-cased. ADDRESS + TEST FIRST.** (Supersedes FLEX-COVERAGE-1 — Flex was only where it surfaced.) The engine treats EVERY shift identically from configuration, so a brand-new shift any future client invents can never silently slip past the safety checks. Concretely: (a) coverage/gender checks must count every qualifying guard regardless of which shift they're on — the man on Flex must count toward the gender rule; (b) a UNIVERSAL "no one works two overlapping shifts in a day" rule that holds for any shift, any client, automatically (Michael on AM Headguard + Flex must be impossible); (c) remove the hard-coded "Flex is excluded" assumption in `sex-coverage.ts` and any other name/role special-casing. **Diagnosis already done — see the FLEX-COVERAGE-1 lead-issue block below for the exact code paths.** Reproduction test first, then fix, then live-verify.
>
> **2. SOTERIA-CHECK-1 — Soteria validates EVERY schedule edit (and build) against the full, live client ruleset.** Her #1 job is making Homebase idiot-proof and giving managers maximum confidence. Every action she validates against ALL live rules — availability, **custom availability**, coverage, gender, veteran, banned-pairs, max hours, doubles/overlap — with nothing hardcoded and nothing skipped. She catches what the manager misses and guides them, so a wrong move can't slip through. **Trigger case:** a manager swap was cleared that should have been BLOCKED because an employee's custom availability made that shift impossible — her checks weren't consulting the full ruleset. Shares the same root disease as #1 (stale/hardcoded checks); fixing the engine's universal rule layer should feed Soteria's checks from one source of truth.
>
> **3. TEMPLATE-EDIT-2 — schedule template editor, fully configurable for all clients, with live preview.** Managers can edit structure, colors, size, and per-column rendering/layout (what content sits where, in which direction); edits persist reliably and drive their real schedules; the preview window renders their changes LIVE as they edit. AND the engine must build correctly against ANY template a manager creates — changing the template never breaks the engine (ties back to #1's configurability). Banked diagnosis in old item #3 / TEMPLATE-EDIT-1 below (swallowed save-error in `useScheduleTemplate.ts:67` + `TemplateEditorPanel`; verify the `schedule_templates.company_id` upsert constraint).
>
> **FULL RANKED ORDER (set 2026-06-23, Alexander) — work top to bottom:**
> 1. **ENGINE-CONFIG-1 ✅ DONE + merged** (Aegis PR #51, double-booking backstop) — engine fully configurable, universal no-double-shift rule (detailed above). Test first.
> 2. **SOTERIA-CHECK-1 ✅ DONE this session (branch `fix/soteria-validate-full-ruleset`; Alexander to push)** — Soteria validates every edit against the full live ruleset. **Root cause:** `/api/soteria-validate-schedule` never loaded `custom_availability` and let an LLM judge rule compliance (so it could silently miss rules). **Fix:** new pure module `Homebase/src/lib/soteria/validateScheduleEdit.ts` runs DETERMINISTIC hard-rule checks — qualification, regular + **custom availability** (date-limited & rotating, mirroring Aegis `resolveAvailabilityForWeek`), approved time off, max weekly hours, overlapping double-booking, never-together pairs. The route now loads custom_availability, runs the validator (errors are authoritative for `approved`), and uses the LLM ONLY for soft warnings (and degrades gracefully if the model is down). Test harness `validateScheduleEdit.test.ts` (ts-node, 9 checks incl. the exact custom-availability swap-block bug) all pass; Homebase tsc clean. **Builds** are covered by the engine's own checks (item 1 backstop). Follow-up: stand up a real Homebase test runner so this test runs in CI.
> 2. **SOTERIA — ✅ ALL 4 PILLARS DONE + merged (2026-06-23 sprint).** SOTERIA-CHECK-1 (validate, pillar 1), persistence guard (pillar 3), read-context for availability/custom-availability/veteran-rules (pillar 4), and document ingestion (pillar 2): handbook→setup plan, richer roster import, and existing-schedule→shift-structure import. See the SOTERIA PURPOSE block + the 2026-06-23 session log. Soteria is "good to go."
> 2.5. **TO-REC-STALE long-term fix** — deploy the already-built re-run tool, then move toward auto-recompute (on view of a pending request and/or re-check at approval). Sits between 2 and 3. *(~8h)*
> 3. **TEMPLATE-EDIT-2 — ✅ DONE across all four surfaces (Homebase merged; Aegis email piece pending Alexander's deploy).** The look configured in the template editor (layout: shift/employee/role rows; colors; sizes; column order; shown fields) now appears identically in: (a) the Homebase preview, (b) the drag-and-drop content editor managers work in, (c) the Excel/PDF download, and (d) the email sent to employees. Saving reconfigures the live schedule everywhere. **Slices:** 1 — RLS policy on `schedule_templates` + surfaced the swallowed save (root cause: RLS-enabled-no-policy made every save silently fail); 2 — layout-support gate; 3 — employee/role view layouts; 4 — downloads honor layout (`buildScheduleGrid`); 5 — editable content editor in any layout (`applyAltMove`); 6 — emailed schedule honors the template (Aegis `templated-grid.ts`, branch `feat/email-schedule-template`, **deploy = Alexander, Railway/live**). Tests added per slice; both repos tsc clean, Aegis 124/124. **Follow-on:** exact px row-height/col-width parity in email (email tables approximate) — minor.
> 4. **Directed two-way shift swap ✅ DONE + merged** (Aegis PR #50 + #46 unit tests). Was "untested"; now merged with the both-mode core tested.
> 5. **Event-shift rendering on download + in-app grid ✅ DONE + merged** (Homebase PR #23 display + Aegis PR #49 engine).
> 6. **Emergency-coverage-by-email — ✅ BUILT (branch `feat/coverage-email-buttons`; deploy = Alexander, Railway/live).** The fan-out engine was already merged + working via "reply YES/NO" on both email & SMS. This adds branded **Accept/Decline buttons** to the coverage email (matching time-off & availability), routed through the shared `/webhooks/decision` handler as `decision_type:'coverage'` (Accept→approve, Decline→deny). Isolated new core `processCoverageButtonDecision` in `emergency-coverage.ts` reuses the in-module helpers (schedule swap, shift-filled fan-out, manager notify, batch-exhaust) but does NOT touch the working inbound-reply handler or the time-off/swap decision branches. Pure `classifyCoverageButton` + 4 tests; Aegis tsc clean + 128/128. SMS still uses reply YES/NO; no-template/no-email falls back cleanly. **Round-trip (real email click) untested — verify in sandbox before relying on it.**
> 7. **Quria Gmail fix — ✅ DONE (the loose fix, now verified + ready to commit).** Root cause: the time-off **resolution-notice** fan-out (`sendManagerResolutionReplies` in `time-off.ts`) selected ALL `users` for the company, including the `role='quria'` platform-admin row, so the personal/Quria Gmail got the approve/deny notices. Fix adds `.in('role', ['manager','owner'])` — matching every OTHER manager lookup. **Verified complete:** audited all 7 `from('users')` manager-email queries across workflows (both other time-off queries, payroll, payroll-scheduler, onboarding, shift-swap) — every one already excludes `quria`; this was the lone leak. tsc clean + 128/128. Change is the uncommitted edit in `time-off.ts` — commit with the coverage branch or on its own. *(Future hardening: a shared `getCompanyManagers()` helper so a new query can't forget the filter.)*
> 8. **Veteran guardrail (+test)** — land the loose employee-facing veteran lockout.
> 9. **Schedule ordering + download/preview parity** — order by ROLE (not alphabetically) on the download + emailed schedule, AND verify EVERY aspect of the downloaded schedule matches the Homebase preview (carries into the new template editor's rendering from item 3).
> 9.5. **COVERAGE-FLAG-DISMISS (new, Alexander 2026-06-24)** — let a manager dismiss/ignore a coverage-review notice (the flagged-issue / gap / coverage warnings on the schedule view) via a small, subtle (semi-transparent) trash-can button *inside each notice*, with an "Are you sure you want to delete this coverage flag?" confirmation before it's removed. Homebase-only, contained UI. Dismissal should persist (so it stays hidden on reload) and not affect the underlying schedule data — it only hides the advisory notice for managers who choose to ignore it. *(~3–4h)*
> 10. 🟢 **Job-posting (undirected) shift swap — "anyone want my Saturday?" broadcast.** **ALL STAGES BUILT 2026-06-28 (Stages 1–4b); code-complete. Stages 1–4a are MERGED but INERT (no live behavior change). ⚠️ **CORRECTION (2026-06-30): Stage 4b — the cut-over that activates the broadcast — is ALREADY ON `main` and LIVE.** It rode in with PR #65 (`feat/swap-agreement-stage4a`); the 4b commit was stacked on 4a. The `feat/swap-broadcast-stage4b-cutover` branch is fully merged (redundant), NOT held. So the undirected broadcast is live in production but still UNTESTED — the 10.5 / 11.7(b) sandbox smoke is the only thing left before it's truly DONE. See Session Log 2026-06-30.** See Session Log (cont. 2–7). **REDESIGNED + EXPANDED 2026-06-28 (Alexander's full spec — confirmed via 3 design Qs). This is the real target; the email-first fix below was the foundation.**
>
>    **CONFIRMED DESIGN (build to this):**
>    - **Intake.** Requester tells Aegis the shift they can't work AND the days they CAN work in return (e.g. "I can't work my Sat AM, but I can work Mon/Tue/Wed"). If the willing-days are missing, Aegis asks before broadcasting.
>    - **Candidates = simultaneous broadcast (first-to-commit wins; the shift LOCKS on first commit).** Email goes at once to everyone qualified for the requester's shift who isn't already on that same shift. **Buttons are tailored per recipient** (Alexander confirmed 2026-06-28): **(A) "I'll pick up the shift"** (one-way — keep mine, add theirs) appears for EVERY recipient; **(B) "I'd like to swap shifts"** (two-way trade) appears **only** for recipients who have at least one of their OWN shifts on a day the requester is willing to work and aren't off then (a tradeable shift). If you have nothing valid to trade, you don't get the swap button — only pickup. This is exactly the `partitionSwapCandidates` split (pickup = all; swap = the subset with tradeable shifts).
>    - **Web landing pages via the email buttons (Homebase).** Pickup → a confirm pop-up. Swap → an **action-card shift-picker** showing only the candidate's shifts that fall on a requester-willing day; they pick the one to give up, then confirm.
>    - **Pickup path:** confirm → **lock the broadcast** → create one-way `swap_request` (pending_manager) → **notify requester** ("someone's picking up your shift, pending manager approval") → **manager approve/deny email** → approve → `executeScheduleSwap` (one-way) + notify both.
>    - **Swap path:** candidate picks their shift + confirms → **lock the broadcast** → Aegis writes to the **REQUESTER**: "{name} will give up their {shift on day} for your {shift} — do you agree?" → requester agrees → **manager approve/deny email** (two-way trade) → approve → `executeScheduleTrade` + notify both. (Asymmetry, intentional: pickup only NOTIFIES the requester; swap requires the requester to AGREE because they receive a different shift.)
>    - **Manager approval = ALWAYS, both paths** (pickup and swap), every time.
>    - **Locking / race:** first candidate to commit (a pickup confirm, or a swap proposal the requester then sees) locks the broadcast; later clickers get "this shift is already being handled." **Open design default (Alexander to confirm if he disagrees): if the requester DECLINES a proposed swap, the broadcast REOPENS to the remaining candidates rather than dying.**
>
>    **STAGED BUILD PLAN (each stage: feature branch → tsc + tests → push → PR; Homebase before Aegis on the landing-page pieces):**
>    1. **Aegis foundation (pure + tested):** willing-days intake/parse; candidate eligibility split into *pickup-eligible* (qualified for the requester's shift) and *swap-eligible* (also has a tradeable shift on a willing day) + a pure `tradeableShiftsForCandidate(...)`; new broadcast state record (`open|locked`, `locked_by`) with atomic first-commit lock.
>    2. **Two-button broadcast email + token types:** mint `swap_pickup` + `swap_trade_select` magic-link tokens per candidate; branded two-button outreach email; simultaneous fan-out to all eligible.
>    3. **Homebase landing pages:** pickup-confirm page; swap shift-picker (action cards) page; dispatcher cases + the Aegis internal endpoints they call (lock + create request + notify).
>    4. **Requester-agree + manager-approve + execute + notify:** requester agree/deny step (swap path); reuse `webhooks/decision.ts` swap approve/deny (already email-capable) for the manager gate; one-way vs two-way execution; reopen-on-requester-decline.
>
>    **FOUNDATION ALREADY LANDED (2026-06-28, branch `feat/undirected-swap-email-first`):** the legacy facilitated path was **SMS-only** in 3 spots (first-candidate contact, decline→next queue advance, auto-execute receiver notify) — reworked to **email-first** via `sendOutreachMessage`, gated by the new pure `isReachableForOutreach(emp, hasSmsChannel)`. 4 new tests (`shift-swap.test.ts`, 22 total); 147/147 green, tsc clean. This makes the swap machinery work over email at all; the staged plan above builds the two-button broadcast + landing pages on top of it. *(The old one-at-a-time queue is superseded by the simultaneous broadcast in Stage 2.)*
> 10.5. **SWAP-ACTIVATION — sandbox smoke-test the undirected swap, then merge Stage 4b to go live (deferred 2026-06-28 at Alexander's request; he's away from the sandbox).** All code is built (#10). What's left is the live verification + the switch-on. **Sandbox is ready** (TEST_IDENTITIES, refreshed 2026-06-30 — requester **Sam Rivera** `aegisscheduler@gmail.com`, candidate **Riley Brooks** `lightningmakigga@gmail.com`, manager notifications → **the sandbox manager inbox — see TEST_IDENTITIES; ⚠️ live `public.users.email` is still `sandbox-manager@quriasolutions.com`, which has NO mailbox, so approval emails go into a void. Repoint to a FREE M365 shared mailbox (e.g. `sandbox-mgr@quriasolutions.com`) before this smoke**; all sandbox Lifeguards have full availability + ample max-hours). **Schedule state (live DB):** active published week is Sun 6/28–Sat 7/4 (`669dc428`); Sam = Tue 6/30 AM, Thu 7/2 AM, Sat 7/4 AM; Riley = Mon 6/29 PM, Fri 7/3 PM. **Adjusted smoke (the old "Sat PM / work Monday" script doesn't fit this week):** (1) pickup — Sam emails "anyone want my Saturday shift?" (= Sat 7/4 AM) → Riley's inbox gets the two-button email → click Pick it up → manager (sandbox manager inbox — see note above) approves → verify the shift reassigns; (2) swap — Sam adds "...I can work Friday" → Riley (who works Fri 7/3 PM) clicks Swap → picks their Friday shift → Sam's inbox gets Agree/Decline → Agree → manager approves → verify the trade; (3) decline → verify the broadcast reopens. **⚠️ UPDATE 2026-06-30: Stage 4b is already merged/live (rode in with PR #65) — no merge step needed; it's already active in production.** Only the sandbox smoke remains before undirected `swap` is DONE end-to-end.
> 10.6. **SWAP-SAFETY — pickups & trades must honor banned pairs + manager-denial notifications (Alexander, 2026-07-01).** Two gaps found while testing #10:
>    - **Banned pairs on swap/pickup — FLAG BUILT 2026-07-01 (branch `feat/swap-banned-pair-flag`).** Flag-don't-force (Alexander, explicit): Aegis does NOT block a swap/pickup that would create a hard ('never') banned pair — it **warns the manager in their approval email** and the manager decides. New pure helpers `loadHardConflicts` + `bannedCohabPartnerName` in `src/workflows/shift-swap.ts` (5 unit tests) predict whether the joiner would sit beside a hard-banned coworker on the shift (pickup: receiver → requester's shift; trade: also requester → receiver's shift). If so, the manager approval email gets a **⚠️ heads-up warning block** (text + branded HTML, warn colors) above the Approve/Deny buttons; approving still works normally. No blocking in `decision.ts`. 187+5 tests green, tsc clean. **Enhancement (not blocking):** could also surface it to the requester/candidate earlier; deferred.
>    - **Manager-denial notifications — verify.** On a swap/pickup DENY the deny branch in `decision.ts` notifies both requester + receiver; confirm the CORRECT employees are notified for both the trade and the one-way pickup case (test in the sandbox). Fix if it names the wrong party.
> 11. ✅ **Request-an-additional-batch** workflow. — **BUILT + verified-in-code 2026-06-28 (Aegis-only; branch `feat/coverage-batch-button`).** The conversational version already worked (manager replies YES to "send another batch?"); this adds the **email-button** version consistent with the rest of coverage. When a coverage batch is exhausted and more candidates remain, the manager's prompt email now carries **"Send next batch" / "No, I've got it"** magic-link buttons. New `decision_type: 'coverage_batch'` routed through the existing `/webhooks/decision` (Aegis-only — no Homebase change): approve → `processCoverageBatchButton` reconstructs the manager context from the stored session and calls the same `blastNextBatch`; deny → clears the session. Pure `classifyCoverageBatchButton` (not_found / stopped / sent / exhausted) + 4 tests; the reply-YES/NO fallback still works for SMS or if they reply. 167/167 green, tsc clean. **Doesn't touch the swap files — safe to merge independently of the held Stage 4b cut-over.** Remaining for DONE: merge + a live eyeball.
> 11.7. 🧪 **VERIFICATION PASS — sandbox-test the batch of built-but-unverified workflows (standing item; added 2026-06-28 at Alexander's request).** We're intentionally in build-build-build mode; rather than gate every feature on its own live test, we batch the testing and run a sandbox sweep after a group of features lands. **As each new feature is built without an immediate test, add it to this checklist** (within reason — pure-helper-only or low-risk changes can be judged covered by their unit tests). **Currently awaiting a sandbox smoke:** (a) **#11 coverage "send another batch" buttons** — merged + deployed, never clicked live; smoke: trigger a coverage call-out in the sandbox, let a batch exhaust, click **Send next batch** (verify the next group is emailed) and on a fresh run click **No, I've got it** (verify it closes out). (b) **#10 undirected swap** — see item **10.5**. (Stage 4b is already live as of 2026-06-30 — no merge needed; just the end-to-end smoke.) (c) **#12 employee "what are my shifts?"** — merged + deployed, never exercised live; smoke: from a sandbox employee email "what are my shifts this week?" and "am I working <day>?" → verify the warm reply lists their real published shifts (and the empty/day-off cases read right). (d) **#14.5 availability-vs-time-off fix** — from a sandbox employee, email a positive availability statement with a week bound (e.g. Quin's "For the week of … I can work Monday 11–3:30, Wednesday …, and Thursday") → verify Aegis treats it as an AVAILABILITY change (not a time-off confirmation), and that a real "X off" message still reads as time-off. (e) **#14.5b open-ended partial time-off** — email "time off June 29 after 4pm" → verify Aegis confirms a PARTIAL off-window (≈4:00 PM–close), not a full day; and "off until 2pm" → open–2:00 PM. (f) **#14.5c combined time-off + availability** — email "Time off: June 18-21. Availability: 6/22 morning, 6/23 morning, …" → verify Aegis confirms ONLY the time off (June 18–21, full days — NOT a merged 18–26 range) and adds a P.S. asking for the availability separately. Future built-but-untested items get appended here. Run the sweep when Alexander is back at the sandbox; mark each DONE only after it passes end-to-end.
> 12. ✅ **Employee "what are my shifts?" by email** + employee-experience/tone. — **BUILT + verified-in-code 2026-06-28 (Aegis-only; branch `feat/employee-shift-query`).** New employee intent `query_my_shifts` (classifier: "what are my shifts?", "when do I work?", "am I working Saturday?" → optional `date`) → router → `handleMyShiftsQuery` (operational-query.ts): pulls the employee's own upcoming shifts from published schedules (deduped across republishes), warm plain-text reply via `reply()`, **no Homebase CTA**. Pure `formatMyShiftsReply` (upcoming vs specific-date, singular/plural, empty-state) + 5 tests. Disambiguated from `operational_query` (manager workforce Qs) and `query_my_time_off` (their time off). 172/172 green, tsc clean. **Doesn't touch swap files — independently mergeable.** *Broader tone audit of ALL employee emails is a larger optional follow-up; this feature itself ships in the warm voice.* Remaining for DONE: merge + the 11.7 sandbox eyeball.
> 13. ✅ **Accept custom availability by email** (date-limited + rotating). *(Alexander's 2nd "12" — renumbered to 13.)* — **BUILT + verified-in-code 2026-06-28.** Diagnosis found the full chain already shipped (in the earlier date-limited build + voice pass): classifier emits `extracted.end_date` for an "until/through" boundary → `intent-router` passes it → `handleUpdateAvailability` branches **rotating** (`isRotatingAvailabilityRequest`) / **date-limited** (`custom_end_date`) / permanent → employee YES → manager magic-link email mints `approve_/deny_custom_availability` (full snapshot payload) → Homebase `dispatcher` handles both action types → `POST /internal/apply-custom-availability-decision` → `applyCustomAvailabilityDecision` writes the `custom_availability` override (date_limited or rotating) + notifies the employee with **no Homebase link**. Insert columns verified against live `information_schema` (all match, all NOT-NULLs satisfied). **New this session:** added the missing **intake** test coverage (`custom-availability-magic.test.ts`, now 10 tests) proving an "until \<date\>" email becomes a date-limited pending (`custom_end_date` set), a plain change stays permanent (null), and a non-date value is ignored. Full Aegis suite 143/143 green, tsc clean. **Remaining for DONE:** push the test-coverage branch + one live sandbox smoke (employee→manager→approve) — the feature code itself is already deployed.
> 14. **Reference-doc rewrite** (docs 01–06). *(Alexander's 13.)*
> 14.5. ✅ **PARSE-INTENT-1 — Quin M's bug: positive availability misclassified as time-off. — SCOPED + BUILT 2026-06-28 (Aegis-only; branch `feat/availability-vs-timeoff-fix`).** **Root cause (from Quin's screenshots):** Quin (Watermark employee, qmead0630@gmail.com) emailed *"For the week of June 29 to July 5 I can work Monday 11am to 3:30pm, Wednesday 11am to 3:30pm, and Thursday."* — a pure **availability** statement — and Aegis replied with a **time-off** confirmation. The classifier rule *"if a message has BOTH a specific date AND availability-sounding language, the specific date wins → submit_time_off"* (plus an "indirect partials" rule that read "what they CAN do" as an off-window) forced positive "I can work …" into time-off. **Fix (`src/ai/claude.ts`):** (1) new prompt rule — a PURELY positive availability statement ("I can work / I'm available / put me down for …") with NO off/can't language is ALWAYS `update_availability`, even with dates; a week/range bound → date-limited (`end_date` = last date of range). (2) clarified the indirect-partials rule to require an off/busy context. (3) added examples incl. Quin's exact message. (4) **deterministic backstop** — pure `looksLikePositiveAvailability` + `applyAvailabilityBackstop` reclassify a mis-fired `submit_time_off`→`update_availability` (carrying the range end as `end_date`), abstaining on any off/can't/mixed message. 8 new unit tests reproduce Quin's case; 180/180 green, tsc clean. **Harder follow-up (logged, not fixed):** the *combined* message (screenshot, June 15: "Time off: June 18-21. Availability: 6/22…") where one email carries BOTH a time-off request AND availability — Aegis merged them into one wrong range. That's a multi-intent-in-one-message problem; the backstop deliberately abstains on it (it contains "off"). **Verify in the 11.7 sandbox pass** (LLM classification isn't unit-testable offline; the backstop logic is).
> 14.7. **PRIVACY-TELNYX-1 — update landing-page privacy / SMS legal copy for the new provider + number (before the Homebase overhaul & SMS migration).** The SMS provider is moving to **Telnyx** with a **new sending number**; the public landing-page privacy policy / SMS terms must name the new provider and number. **Make it client-specific:** every future client gets their OWN Aegis number, so the privacy/terms copy must render the number per-client (config/data-driven), NOT hardcode Watermark's. Lives in the marketing/landing-page site. *(Surfaced 2026-06-28 by Alexander. Small copy/config task — do before the UI overhaul and the SMS provider migration.)* **EXPANDED 2026-06-30 (Alexander):** the **Telnyx 10DLC-registration landing page** currently displays a single **dedicated/hardcoded number**, which is not viable — every new client gets their OWN Aegis number once live, so the page can't name one specific number and still be fully 10DLC-compliant. Rework the landing page so the 10DLC/compliance copy is **number-agnostic** (or renders the correct per-client number from config/data), naming no single hardcoded number. Do this as part of the SMS-migration work.
> 15. **Full Homebase UI overhaul** *(use Cowork design mode)*. *(Alexander's 14.)*
> 16. **CLIENT MONITORING / OBSERVER INBOXES.** ⚠️ **STATUS CORRECTION (2026-06-30): the FIRST SLICE IS BUILT + LIVE — this item is NOT dormant.** The `company_monitoring_inboxes` table exists (`id, company_id, email, active, created_at`; all NOT NULL) and `messaging/email.ts` BCCs every outbound email for a company to its active rows (`resolveMonitoringEmails` → `buildBccList`, fail-safe to no-BCC). **Watermark is already opted in** (one row). **Account convention — CORRECTED (Alexander, 2026-06-30): one FREE Microsoft 365 *shared mailbox* per client on `@quriasolutions.com`** (e.g. `monitor1@`). The domain runs on **M365 via GoDaddy** (NOT Google Workspace), so we mint free shared mailboxes on our own domain — no per-seat cost, accessed from `awdarling@`'s Outlook. The earlier "one free `@gmail.com` per client" plan was **abandoned** (those Gmail accounts were never created — hit Google's phone-verification cap). Watermark's monitor target is **`monitor1@quriasolutions.com`** (created 2026-06-30). ⚠️ The live DB row read `monitorone@quriasolutions.com` (non-existent mailbox → BCCs bouncing) until repointed to `monitor1@quriasolutions.com`. **Still TODO under this item:** an admin UI to manage the flag (currently DB-only), and threading the BCC through any operational sends that bypass `sendEmail`. *(Original concept set 2026-06-24; first slice shipped ~2026-06-28.)* Original notes follow. — Let Alexander attach a dedicated per-client Gmail account — one he creates himself — to each client as a Quria-admin **observer inbox** that passively **receives a copy of every operational communication for that client**: time-off requests + approve/deny decisions, availability changes, shift swaps, emergency-coverage outreach + results, schedule distributions/republishes, and any action-item emails. Purpose: a clean troubleshooting/audit trail per organization, so when something fails Alexander can see exactly what went out. **Design constraints (important):** (a) this is NOT "let all `role='quria'` accounts receive again" — the Quria Gmail fix (item 7) deliberately removed quria from manager emails, and Alexander's PERSONAL email must STAY OFF all of it. Receiving must be **explicit opt-in** — a per-account/per-client "monitoring inbox" flag (e.g., a column on `users` or a `company_monitoring_inboxes` table), not the `quria` role itself. (b) Aegis just CC's/copies these inboxes on the existing sends; it does NOT generate new notification traffic — Alexander mutes phone/desktop notifications on the Gmail side himself. (c) Dormant until Alexander provisions the Google accounts; no immediate work. **Scope when built:** add the monitoring-inbox flag + admin UI to manage it, then thread the copy-recipient list into every operational `sendEmail` fan-out. *(~6–8h when picked up.)*
>
> 17. **AEGIS USER HANDOUTS — manager guide + employee guide (Alexander, 2026-06-30). Build AFTER everything is tested (11.7), so every example reflects real verified behavior.** Two send-to-client documents, one per role, that teach users exactly how to use Aegis. **Client-AGNOSTIC** (NO Watermark specifics — reusable for every future client) and **foolproof**: after reading, a user should be able to self-answer "is this working?", "did I do this right?", and "how do I phrase this?". For EACH function available to that role, walk through (a) what it does, (b) exactly how to phrase the message, (c) example messages, and (d) a **preview of the full exchange** — what the user sends and what Aegis replies, step by step (screenshot-style). Warm, plain-English tone. **Manager guide:** build/distribute schedule, approve/deny time-off + availability (email buttons), emergency coverage + "send next batch", workforce questions ("who's working Saturday?"), shift-swap approvals. **Employee guide:** time-off (full day, partial incl. "after 4pm", combined-message note), availability changes ("I can work…", custom date-limited + rotating), "what are my shifts?", shift swaps (directed + "anyone want my shift?" once #10 is live). NOTE: existing Desktop drafts `Watermark_Manager_Guide` / `Watermark_Employee_Guide` are a starting point but must be **generalized + completed** to match tested behavior. *(Sequenced right after today's test+ship.)*
>
> 18. **SANDBOX RESET FOR LIVE DEMO (Alexander, 2026-07-01). Sequenced AFTER #17 (handouts).** Once every email workflow is confirmed working AND the handouts (#17) are built, re-set up the sandbox tenant into a clean, realistic state suitable for a **live demo to a prospect/client**. Requirements: a fresh published week with a sensible roster + shifts; the real test inboxes wired (Sam/Riley + the M365 shared mailboxes for manager/monitor); **no leftover test artifacts** (stuck/duplicate `swap_requests`, half-finished trades, stale time-off, orphaned pending rows from the 2026-06-30/07-01 sweep); and a rehearsed happy-path that shows the full loop end-to-end (build/distribute schedule → availability → time-off approve → shift swap → emergency coverage) with clean, human-feeling replies. Goal: a demo Alexander can run start-to-finish with zero surprises. *(Added after the 2026-06-30/07-01 email-workflow test sweep.)*
>
> 19. **★ TEST & BUILD ONBOARDING — THE NEXT ITEM TO DO (Alexander, 2026-07-01).** The employee-onboarding flow (`initiate_onboarding` → `handleInitiateOnboarding` → the welcome + availability-collection conversation in `src/workflows/employee-onboarding.ts`) exists in code but has **not been exercised end-to-end** the way the other workflows were in the 2026-06-30/07-01 sweep. Do a full sandbox test of the manager-initiated onboarding of a new hire: manager says "onboard <name>" → new-hire inbox gets the welcome → they reply with availability → Aegis parses + confirms + writes it → they're schedulable. Fix whatever the test surfaces, then round out any gaps (multi-employee onboarding fan-out, availability parse edge-cases, re-onboarding, the "reply YES to confirm" chain, SMS-vs-email). Treat this as its own build-and-verify item, not just a smoke. **Sequenced as the immediate next priority — ahead of #18 (sandbox reset), since a clean onboarding is part of a credible demo.**
>
> 20. **PAYROLL SYSTEM BUILD (Alexander, 2026-07-01). Sequenced AFTER the Full Homebase UI overhaul (#15).** Build out a real payroll/hours system (the earlier lightweight "payroll check" is being removed from the user handouts as not-yet-real). Scope TBD when picked up, but the intent is a proper payroll capability — accurate scheduled-vs-worked hours, overtime handling, wage math tied to `wage_rate`/roles, exportable payroll output, and manager review — rather than an ad-hoc hours summary. **Do NOT surface payroll in client-facing materials until this is genuinely built.** *(Added 2026-07-01; payroll removed from the manager handout the same day.)*
>
> **ROUGH EFFORT ESTIMATE (updated 2026-06-24, ±30%) — remaining ≈ 97h:** auto-recompute (2.5) 8h · veteran guardrail+test (8) 3h · schedule ordering/parity (9) 6h · coverage-flag-dismiss (9.5) 4h · job-post swap (10) 10h · request-additional-batch (11) 6h · employee shift queries + tone (12) 8h · custom-availability-by-email (13) 8h · reference-doc rewrite (14) 12h · Homebase UI overhaul (15) 40h · SMS provider migration (parallel) 16h. **Done: items 1, 2 (all Soteria), 3 (TEMPLATE-EDIT-2, all 4 surfaces), 4, 5, 6 (emergency-coverage-email buttons — built, pending deploy), 7 (Quria Gmail fix).**
>
> **PARALLEL TRACK — SMS provider migration (moving OFF Twilio; new provider under investigation, TBD).** **Twilio decommission DONE this session:** (a) code made Twilio fully OPTIONAL — branch `chore/twilio-optional` (`config/env.ts` + guarded client construction in `messaging/sms.ts`, `webhooks/sms.ts`, `middleware/verify-signature.ts`) so Aegis boots + runs email-first with Twilio fully removed (`sendSms` is a safe no-op, inbound SMS route inert); tsc clean + 116/116. (b) **Both Twilio numbers RELEASED in the console** — the blocker was an E911 emergency address on each (had to be removed first, ~10–20s async, then release; the release control lives on the number's Properties tab, not Configure). **Remaining cutover order (no breakage): (1) deploy `chore/twilio-optional`, (2) THEN remove TWILIO_* env vars from Railway — NOT before, or the live server crashes on restart.** Releasing the numbers already stopped the spend. The new-provider integration is the (later) SMS-migration item; A2P HELP/STOP responder folds into that.
>
> ---
>
> ## ◆ SOTERIA — PURPOSE & DEFINITION OF DONE (set 2026-06-23, Alexander — SUPERSEDES the "Aegis parity" framing in item 16)
>
> **Soteria is NOT meant to do everything Aegis can.** Her sole purpose: make Homebase effortless — a conversational layer over Aegis's brain + data so non-technical clients never have to learn new software (making them learn a new app would defeat the product's value). She manages and safely EDITS the brain/data; she does NOT need to run live operations. **Explicit non-goals:** publish/republish, shift swaps, emergency coverage, operational Q&A — those are Aegis/UI, NOT Soteria.
>
> **Definition of done — 4 pillars:**
> 1. **Foolproof.** Every change she makes is validated against the full live ruleset; she cannot break Homebase. *(SOTERIA-CHECK-1 — deterministic full-ruleset edit validation incl. custom availability — ✅ DONE + merged 2026-06-23.)*
> 2. **Document ingestion.** She can absorb company documents (handbooks, policies, rosters, existing schedules) and configure Homebase from them. *(✅ FIRST SLICE DONE 2026-06-23 — handbook/policy → reviewed setup plan (`apply_setup_plan`); built on branch `feat/soteria-document-setup`, pending merge. Pure tested `ingestionPlanner` orders+validates roles → wages → shifts(+role reqs) → policies → veteran rules, de-dupes vs existing, flags unresolved refs. Out of this slice (follow-ons): existing-schedule ingestion, richer roster import [in progress], conflicts (need employee IDs first).)*
> 3. **Persistence.** Every change/edit she makes actually saves and sticks in Homebase — no silent failures; each write must round-trip (same bug class as the template-editor swallowed-save). *(✅ DONE + merged 2026-06-23 — `persistGuard.throwOnWriteError` applied to the 5 executor actions that were swallowing DB errors (delete_employee, update_profile, delete_policy, update_availability wipe, set_custom_availability deactivate); client already surfaces failures. 17-check ts-node test.)*
> 4. **Explains everything.** She can clearly explain any aspect of Homebase to a user on request. *(✅ DONE + merged 2026-06-23 — Soteria now loads recurring availability, active custom availability, and active veteran rules into her read context via pure `contextFormatters` (wording mirrors the schedule page); 16-check test.)*
>
> When all four hold, Soteria is "good to go." **STATUS 2026-06-23: all four pillars substantially addressed** — pillars 1, 3, 4 merged; pillar 2's first slice built (pending merge). Remaining Soteria depth = the document-ingestion follow-ons above; the next non-Soteria roadmap item is TEMPLATE-EDIT-2 (committed order #3).
>
> ---
>
> ## ⚠️ LEAD ISSUE — FLEX-COVERAGE-1: the Flex shift is invisible to the engine's safety checks (set 2026-06-19) — **#1 PRIORITY**
>
> **Reported by Watermark managers (week of Jun 22, 2026).** Three symptoms, one theme: the **Flex** shift (13:00–21:00, multi-role: accepts Lifeguard OR Headguard) is treated inconsistently by the scheduling engine — counted as a real guard slot in some places, ignored as "not a guard" in others.
> 1. **A guard got double-booked.** Michael McCorkle appeared on AM Headguard (11:00–15:30) AND Flex (13:00–21:00) the same Wednesday — overlapping hours, physically impossible.
> 2. **The gender/coverage warning miscounted.** A day flagged "all-male on duty" while the Flex guard that day was female — the Flex person wasn't counted toward coverage.
> 3. **Flex is missing from warnings entirely** — the manager's instinct that "it's not even being seen."
>
> **Diagnosis (read-only, 2026-06-19 — evidence gathered, NOT yet fixed):**
> - **Confirmed — sex/gender coverage mishandles Flex.** Watermark's gender rule is a `concurrent_coverage` policy (`policies` table, key `gender_requirement`): `population_roles = ["Headguard","Lifeguard","AManager"]`, `minimums {male:1, female:1}`. The evaluator `src/lib/engine/sex-coverage.ts` counts only assignments whose role is in `population_roles`. **Its own comment (lines 11–13) says the design assumed "Flex is excluded by population_roles" — but Flex fills as Lifeguard/Headguard, which ARE in that list.** Stated design intent and live config contradict each other. Compounding it, the `onDuty.length < 2` skip (line 84) silently drops any single-guard time window — so a window covered only by the Flex guard is never evaluated. This is the structural "deeper issue."
> - **Confirmed engine bug — the double-booking (Alexander, 2026-06-19).** Michael was on BOTH AM Headguard and Flex in the **downloaded schedule AND the Homebase next-week preview, before any manager edit** — so the engine itself produced it; it is NOT a manual hand-assign. Puzzle: all three single-step placement paths DO guard time-overlap — main fill (`schedule-build.ts:553`), the banned-pair cascade candidate filter (`cascade.ts:84`), and the (empty-for-Watermark) attribute-mix swap (`attribute-mix.ts:286`) all call `sameDayDoubleReason`, and the Flex slot carries correct times (13:00–21:00). So a single placement can't create the overlap. **Leading hypothesis:** the **multi-step banned-pair cascade** (`cascade.ts` builds a tree/chain of `moves`, lines ~258/268) validates each move against the pre-cascade state but applies the whole chain together — so a later move can land someone onto a shift that overlaps an earlier move's result (the same "validated independently, applied together" class as the TO-REC-STALE bug). Flex's multi-role pool (Lifeguard OR Headguard) widens cascade options and makes it the shift most likely pulled into such a chain. **First task next session: a reproduction test that recreates Michael-on-both from a controlled scenario — that will reveal the exact path before any fix.**
> - **Multi-role fragility (the root pattern).** Flex is the only shift using `accepted_roles` (Lifeguard|Headguard) rather than a single role. The engine was largely written for single-role shifts; Flex's dual nature is what makes it fall through the cracks. **NOTE (2026-06-23): the Aegis engine NEVER reads `accepted_roles` — it only uses the single `role` column (Flex's is "Lifeguard"). So Flex's "multi-role" is not actually expanded in the engine. Watermark also has ZERO banned-pairs, so the cascade never runs → the cascade-chain hypothesis is RULED OUT.**
> - **REPRODUCTION RESULTS (2026-06-23) — `src/lib/engine/__tests__/flex-configurability.test.ts`, all 4 pass.** Built Watermark-like fixtures (AM Weekday 11:00–15:30 overlapping Flex 13:00–21:00; concurrent_coverage gender rule pop_roles=[Headguard,Lifeguard]). Findings: (1) **the engine does NOT double-book** — when only overlapping guards remain, Flex correctly GAPS instead of double-assigning; (2) **the engine DOES count a Flex guard toward gender coverage** — with the only female available only 13:00–21:00, she's placed on Flex and no false "no female" flag fires during her hours (only the genuine 11:00–13:00 pre-Flex window flags). **CONCLUSION: both reported symptoms are NOT reproducible from the engine's clean build path with this config.** ⇒ The **double-booking is almost certainly a RENDERING duplication** in the download + Homebase preview (an assignment shown in two rows), NOT an engine assignment — likely tied to the shift_name↔shift_type mapping (shift_type "Afternoon"/ee96852c carries requirements named BOTH "PM" and "Afternoon"; "AM" requirements map to shift_type "AM Weekday"). **NEXT: investigate the renderers** — Homebase `buildScheduleGrid` + `ScheduleRenderer`, and Aegis `buildFullScheduleGridHtml` — for how assignments are matched to template rows (a fuzzy/duplicate match would put one person in two rows). The gender symptom the manager saw may have been the correct pre-Flex-hours flag misread, OR a fuller-roster edge; the stale "Flex excluded" comment + the `onDuty.length<2` skip in `sex-coverage.ts` remain real configurability tech-debt to harden regardless.
>
> **✅ RESOLUTION (2026-06-23) — branch `fix/engine-double-book-backstop` (sandbox; Alexander to push).** Decisive scan of ALL Watermark saved schedules found real double-bookings — but the pattern is scarce role-holders (mostly the only **Manager**: Jack McCorkle, Carolyn Ringler) on AM + Afternoon (overlap 15:00–15:30), plus a few Flex cases (Karsten, Katie), spanning weeks back to mid-May. The reported "Michael" was Jack (both McCorkles). **Reproduced every shape against the CURRENT engine — it does NOT double-book** (`flex-configurability.test.ts`, 5 tests). ⇒ The double-bookings in live data are **stale artifacts of an OLDER engine**; current code is correct. **Done this session:** (1) 5 reproduction/guard tests; (2) a **universal build-level backstop** in `runScheduleBuild` that flags ANY employee with two overlapping same-day assignments — any shift, any client (new `double_booking` FlaggedIssue, rendered in the build email); (3) corrected the misleading "Flex excluded" comment in `sex-coverage.ts` (coverage counting is driven solely by configurable `population_roles`, nothing hardcoded). tsc clean + 116/116 vitest. **REMAINING (Alexander): rebuild the affected published weeks (6/15, 6/22, and going forward) so the live schedules are clean — a fresh build will not reproduce the double-booking.** Deferred configurability follow-ups: engine doesn't read `accepted_roles` (multi-role shifts only accept their single `role`); revisit the `onDuty<2` skip.
>
> **Fix plan (original — superseded by the resolution above):**
> 1. Write a failing **reproduction test** in the engine harness: one person eligible for both AM and Flex, plus a day whose only female is on Flex — assert (a) no overlapping double-booking is ever produced, and (b) the Flex female counts toward gender coverage. Pin the bug before any change.
> 2. Give the engine a **single coherent notion of "does this shift/role count as on-deck guard coverage."** Reconcile `sex-coverage.ts` (drop the false "Flex excluded" assumption; reconsider the `<2` single-guard skip) so Flex guards are counted exactly once, correctly.
> 3. Close the double-booking at its source (confirmed engine, leading hypothesis = the multi-step cascade chain): make the cascade re-validate overlap against the **post-move** state for every move in a chain, not just the pre-cascade snapshot. As a belt-and-suspenders, add a final "no employee on two overlapping shifts" assertion at the end of every build.
> 4. Keep every other workflow byte-for-byte unchanged; `tsc` + full `vitest` green; hand Alexander the branch + git steps.
>
> ---
>
> ## ⚠️ LEAD ISSUE — TO-REC-STALE: stale time-off approval recommendations under a queue (set 2026-06-16)
>
> **The problem (correctness gap in the recommendation engine).** Aegis computes each time-off request's approval recommendation (`aegis_recommendation` / `aegis_reasoning`) **at submission time**, by running the coverage simulation (`runSimulation` → `generateRecommendation` in `time-off.ts`) against the set of TOs **approved at that moment**. Pending requests are blind to each other. So if several requests pile up while a manager is away and are then **batch-approved**, each one's "Approve — no coverage impact" was computed without seeing the others, and approving them together can collectively breach staffing minimums on a shared day. The recommendation a manager sees days later may simply be wrong.
>
> **Short-term mitigation (NOW, no code):** managers should **approve/deny requests as they come in**, not let them queue. Surface this as client guidance.
>
> **Increment — `TO-RERUN-1` BUILT 2026-06-16 (IN REVIEW, not pushed).** A re-check capability surfaced THREE ways so a manager can refresh a pending request's recommendation against **current** approvals: (1) **Homebase "Re-run check" button** on each pending request in the Time Off tab; (2) **email-card "Re-run check"** magic-link that recomputes + shows the result on the branded landing page; (3) **conversational "re-run" command** (`recheck_time_off` intent) so a manager can email/text "re-run the check on Shmubba's time off." Engine: Aegis `recomputeTimeOffRecommendation(requestId)` (reuses `runSimulation` + `generateRecommendation`; `runSimulation` already re-reads the live approved-TO baseline, so it correctly accounts for everything approved since) → `POST /internal/recompute-to-recommendation`, called via the existing `postToAegisInternal` bridge / aegis-action dispatcher. Read-only w.r.t. the decision — only rewrites `aegis_recommendation`/`aegis_reasoning`. Test scenario seeded in `SANDBOX_RERUN_SEED.sql`. tsc clean + 52/52 vitest (both repos). Pending: Alexander deploy + the live flip-test.
>
> **Long-term robust fix (roadmap):** make recommendations never-stale automatically — recompute on **view** of a pending request and/or **re-check at approval time** (warn/block if approving now would breach coverage given everything currently approved). The rerun button is the manual first step toward this. Decide button-only vs auto-recompute when picked up.

> ## ▶ STATE & NEXT UP (set 2026-06-18, late — read this first)
>
> **The full 2026-06-18 queue (items 3-email, 13, 4) plus two Alexander-added refinements are merged to `main` and live in both repos.** Shipped this session:
> - ✅ **Veteran tag in the emailed schedule (item 3, email half) — day-accurate.** The build/publish report email now tags constrained shift rows with the same wording as the web grid ("Veterans only" / "≥N veterans"), but **only on the days a rule actually applies** (a Sat/Sun-only rule tags only Sat/Sun rows, never the whole shift). Engine helper `veteranLabelForShiftDate` + `resolveShiftRuleLabel` resolver. Merged AG PR #41 (initial) → **PR #42 (day-accurate)**. LIVE. Tests: `schedule-build-email.test.ts`.
> - ✅ **Plain-English Rules-page explainers (item 13).** Every Rules-page label/tooltip/explainer rewritten jargon-free + example-led ("Attribute Mix" → "Required Staff Mix", "Banned Pair Conflict Resolution" → "When Two People Shouldn't Work Together", etc.). Copy-only, no logic change. Merged HB PR #18. LIVE.
> - ✅ **Capabilities / help + role-aware scope guard (item 4).** "What can you do for me?" → role-aware list (employee vs manager vs owner); out-of-scope asks get a kind redirect naming what they CAN do, never a dead-end. ONE source-of-truth list per repo (`Aegis/src/router/capabilities.ts`, `Homebase/src/lib/soteria/capabilities.ts`) drives the help reply + the redirect in both Aegis and Soteria. Merged AG PR #43, HB PR #19. LIVE. Tests: `capabilities.test.ts`.
> - ✅ **VET badge unified to brand orange everywhere (item 15 piece).** One shared `VetBadge` component (`Homebase/src/components/common/VetBadge.tsx`) used by the schedule grid AND the employees/data tab (data tab + add/edit toggle were red — now orange). Merged HB PR #17. LIVE.
> - ✅ **Scope-aware veteran-rule display on the schedule.** Whole-week rules still show the always-on orange row badge; **day-scoped rules now show an expandable "Rules ▾" marker** with a plain-English note ("Veterans only on Saturdays & Sundays") instead of a misleading whole-row badge. Merged HB PR #17. LIVE.
> - ✅ **A2P DECISION (Alexander, 2026-06-18) — HELP/STOP reserved.** The capabilities answer is triggered by **natural language only** ("what can you do", "what can I ask for"). A bare **"help"** / **"stop"** is deliberately NOT routed to capabilities — those are reserved SMS-compliance keywords. The actual A2P HELP/STOP compliance responder is **deferred to the SMS phase** (standard approach: handle via Twilio Messaging Service Advanced Opt-Out at the carrier level so the keywords never reach Aegis). Merged AG PR #44, HB PR #20. LIVE.
>
> **NEXT UP — the post-queue backlog (no committed order yet; item 18 is the cleanest next pickup).**
> 1. **Item 18 — Shift-swap testing + investigation (Aegis).** `shift-swap.ts` (directed + facilitated modes) is fully BUILT but has ZERO automated tests and is not live-verified. Investigate, run a sandbox end-to-end round-trip, add tests for both modes, fix what's broken. Self-contained, green-able — best next pickup.
> 2. **Item 19 — Dynamic business partner.** Build on item 4: proactively explain capabilities + guide the user at conversation start, role-aware. The item-4 capability lists were structured for exactly this.
> 3. **Item 8 — Soteria schedule-build trigger.** Let Soteria kick off a schedule build conversationally (wire to the existing Aegis build endpoint), not only via email. First concrete piece of item 16 (Aegis/Soteria parity).
> 4. **Item 6 — Veteran rules Phase 2** (custom-time event shift that replaces the normal shift). **Item 17 — Access-management page** correctness pass (do live with Alexander). **Item 11 — Homebase UI overhaul** (parked last). **SMS migration** (needs Alexander's provider research; folds in the A2P HELP/STOP responder above).
>
> Build each on its own branch off `main`; keep `tsc` + `vitest` green; hand Alexander the git steps (sandbox can't push). The comprehensive reference-doc rewrite (`docs/01–06`) remains the deferred big job — living docs (this file + the trackers) are kept current in the meantime.
>
> ---
>
> **PUSH STATE (top-of-file banner — read every session):** Aegis is pushed and live (`46eaa70`). **Homebase is pushed and live (`29ed00e`).** **48-hour sprint COMPLETE (2026-06-09):** ENGINE-2/gender rule, S2/SCHED-EDIT-1, S3/in-tab TO notify all DONE and live-verified; S1/ENGINE-1 closed-as-diagnosed (no engine bug; JL residual routed to Role Groups; two product decisions pending — Afternoon end-time, JL scheduling). **Schedule download is DONE on Watermark (2026-06-12):** Piece 1 colors (merged) + download full-name/role content fix (merged 2026-06-12) — live-verified working on real Watermark data; **`DOWNLOAD-500` RESOLVED** (symptom gone in prod 2026-06-12; cross-reference the name/role fix, which reworked the suspected `employee_name` path in `buildScheduleGrid` — exact root-cause line not independently re-confirmed). **`distribute_schedule` fan-out executed successfully in production 2026-06-12** to the full Watermark roster (warm copy + inline full-week schedule + per-employee shift-assignment message); only remaining piece on distribute is a visual-consistency fix so the emailed schedule matches Homebase exactly (= template-unification Piece 3). **DELIV-1 downgraded BLOCKER → MONITOR / client-education** (the 30-person fan-out ran 2026-06-12 with no spam problem — kept as a watch item, not a gate). **Active priority: build + test the remaining Aegis email workflows at Watermark** — see the new direction note in NOW / NEXT below. Next structural plan: the **Forward Build Sequence (Phases 1–4)** — see the section below (it supersedes the `PRIORITY2_ANALYSIS.md` A/B/C option framing). Reference docs + trackers aligned to this direction 2026-06-09.

---

## How to use this document (instructions for the agent)

1. **Read this whole file first** at the start of every session. The Current Sprint is the priority; the backlog is everything after.
2. **Diagnose before you fix.** Every bug here is diagnose-first. Do not write a fix until the diagnosis section for that item is filled in with real evidence (logs, dispositions, the actual code path).
3. **Update statuses as you go.** Change the status tag on an item the moment its state changes. Tick checklist boxes when each sub-step is verified (not when attempted).
4. **Append a Session Log entry at the end of every working session** — date, what was done, what's next, any new bugs/decisions. Append only; never rewrite past entries.
5. **Keep in sync with the repo trackers.** Bug status changes here must also land in `EMAIL_WORKFLOWS_TRACKER.md` (and `SCHEMA_DRIFT_LOG.md` for any new schema finding). The six `.docx` reference docs are the stable reference; this file is the live state.
6. **Hold the standing principles** (bottom of file). The big one: **no orphan outputs** — every change an AI makes must land as valid, visible Homebase state within the constraints. And: read the actual diff before any push.

**Status legend:** `NOT STARTED` · `DIAGNOSING` · `DIAGNOSED` · `FIXING` · `IN REVIEW` (pushed, awaiting verification) · `DONE` · `BLOCKED`

---

## LOGGING PROTOCOL — run this on every approval (mandatory for every agent)

This is the single most important habit. The moment a piece of work is **approved by Alexander**, and before you consider the task done, update the project's memory so the *next* agent — and Alexander — never face a context gap. Do all that apply:

1. **DEV_ROADMAP.md** — flip the item's status, tick only the boxes you actually verified, and append a dated entry to the Session Log (what changed, what's next, any new decision or bug).
2. **EMAIL_WORKFLOWS_TRACKER.md** — if a bug or workflow's state changed, mirror it here.
3. **SCHEMA_DRIFT_LOG.md** — if you found anything in the live database that differs from the docs (a column, constraint, type, enum), append it (append-only; never delete).
4. **TEST_IDENTITIES.md** — if tenants/identities changed (new test user, removed Bubba, etc.), record it.
5. **Reference docs (`docs/01–06`)** — if the change alters how the system actually works (new feature, changed workflow, schema change), update the relevant reference doc so the stable reference stays true. Significant changes also get reflected in the matching `.docx` in the Claude.ai project knowledge.

**Rule:** never end a session without doing the above. The next agent self-briefs from these files. Skipping this re-creates the exact context gap this whole system exists to prevent. If you changed something and didn't log it, the work isn't finished.

---

## NOW / NEXT — current priority plan (set 2026-06-10)

This is the current Now/Next ordering for active work. It sits **above** the (now-closed) 48-hour sprint and the Forward Build Sequence — those remain the structural plan; this is what's being worked next. The three items are ordered **#1 → #2 → #3**; do not start #3 until #1 and #2 are at least in fix-shape. All three are diagnose-first (no blind fixes).

> **Direction note (set 2026-06-12).** With the schedule download + distribute fan-out now live-working on Watermark, the active priority is **building + testing the remaining Aegis email workflows at Watermark** under the AEGIS-EMAIL-1 umbrella below: verify `deny_to` via the email-button path; build the `approve_availability` / `deny_availability` magic-link buttons + the shared employee-notify-back; emergency coverage accept/decline; `request_additional_batch`; manager labor-force queries; employee shift queries; employee shift-swap. **The comprehensive system-document rewrite is deferred** until those workflows are built + verified live — only targeted doc reconciliation in the meantime so the cowork agents aren't working from a wrong map.

### NEXT BATCH — post-workflows polish + experience-rules follow-ups (set 2026-06-13)

The Aegis email + Soteria workflows are built and largely verified live, including the **veteran / experience shift rules** (Phase 1 `DONE 2026-06-13`: `shift_experience_rules` table + migration 015; engine enforcement for all-veterans + min-N, day-of-week + season scoped, swap-then-flag; Soteria create/edit/delete; verified live — Soteria created a rule and the build staffed the shift all-veteran). The next batch, in roughly this order:

> **2026-06-16 directive (Alexander) — build order + scope additions for this session.** Picking up the voice + branding pass first, then the Homebase rules UI + veteran visual indicator, plus several new current-week-operations items. Decisions captured this session:
> - **Voice + branding rollout = sample-first.** Rebrand + rewrite ONE workflow email end-to-end, get Alexander's sign-off on look + tone, THEN roll the approved style across all remaining emails + the magic-link pages. (Items 1 + 2 below run together, file-by-file.)
> - **Veteran indicator = small brand-orange "VET" badge** beside the employee name on the Homebase schedule grid (and in the build/distribute report). Source field: `employees.is_veteran`. (Item 3 below.)
> - **Mid-week change notifications = affected-people-only.** When a published current-week schedule changes (swap, adjusted shift, closed day), notify ONLY the employees involved; the published schedule updates silently for everyone else. (New items 9–10 below.)
>
> **New scope added this session (items 7–11):**
> 7. ✅ **DONE & LIVE (PR #15).** `ShiftVeteranRulesSection.tsx` on the Rules page. **Homebase rules-management UI.** A page where a manager can view / create / edit / delete the veteran-experience rules per shift (reads + writes `shift_experience_rules`, mirroring the engine contract + the existing Soteria/Aegis write path). Pairs with item 3.
> 8. **Soteria schedule-build trigger.** Soteria (the in-app NL agent) should be able to kick off a schedule build conversationally, wired to the existing Aegis build endpoint — not only by emailing Aegis.
> 9. ✅ **DONE & LIVE (AG PR #38, HB PR #16).** Source-of-truth resolved to `published_at` timestamp (not the status enum); status-clobber bug closed. **Publish button (= distribute + published status).** A Homebase "Publish" button that distributes the schedule to staff AND flips the schedule to a **`published`** state. This supersedes/absorbs old item 5 ("Homebase Build + Distribute buttons") — Build stays a button too; Distribute is renamed **Publish** and now also sets the published status. Reconcile with the known `distributed`/`published` status-clobber bug (HB writes `status='distributed'`, Aegis overwrites to `'published'`) — pick ONE source-of-truth field (lean `published_at`/`distributed_at` timestamp over the ambiguous enum) so the re-distribution guard stops breaking.
> 10. **Published schedule = live-operations source of truth.** Once the current week is published, it becomes the authoritative schedule that current-week workflows read + write: texting people on closed days, adjusting a shift for a swap, emergency-coverage edits. Mid-week edits notify affected people only (per decision above) and update the published schedule in place.
> 11. **General Homebase UI overhaul.** Broad visual + UX modernization of the whole Homebase app to the Quria look. Parked at the END of this roadmap (see the "HOMEBASE UI OVERHAUL" section at the bottom) — do the targeted pieces (rules UI, veteran badge, publish button) first; the full overhaul is the larger follow-on.

1. **Branding sweep (Quria Solutions).** Handout PDFs `DONE` (branded employee + manager guides). Remaining: rebrand every **Aegis HTML email** (dark header + orchid logo + orange `#f97316` glow + orange action buttons, brushed-silver/charcoal type), and rebuild the **magic-link landing pages** (Homebase Approve/Deny pages) to match the dark app — orange glows, professional polish, correct casing. Brand source: `homebase/public/QuriaSolutionsBlack.jpg`; palette = black / brushed silver / orange `#f97316` (see `homebase/src/app/globals.css`).
2. **Voice / tone sweep.** A pass over every line Aegis says (email + future SMS) so it reads like a smart, warm, understanding assistant-manager — fewer robotic confirmations, more natural acknowledgement, consistent tone.
3. **Visual indicators for experience (veteran) rules.** ✅ Homebase grid badge DONE & LIVE (PR #15). ⏭ **REMAINING (QUEUED NEXT, quick win #1):** the **build/distribute email report** tag is not done. Surface a badge/indicator (e.g., "Veterans only" / "≥2 veterans" tag on the affected shift) in the emailed schedule so managers see which shifts are constrained. Reads `shift_experience_rules`.
4. **Capabilities / help + role-aware scope guard (Aegis & Soteria).** ⏭ **QUEUED NEXT (build #3 of the next chat).** (Revised 2026-06-17 per Alexander.) Two halves: **(a)** a user can ASK "what can I have Aegis do for me?" / "what can you do?" and get a clear, **role-aware** list of exactly what they're allowed to ask for (employee vs manager vs owner). **(b)** When they request something OUTSIDE their access, it does NOT dead-end with "I didn't understand" — it kindly explains it can't do that *for them* and then tells them what they CAN ask for instead. Mirror the same behavior in Soteria. (Router: a new `capabilities`/`help` intent + the `MANAGER_ONLY_INTENTS` privilege path + the `default` fallback in `intent-router.ts`.)
5. **Homebase Build + Distribute buttons (with activity logs).** A logged-in manager should be able to **build a schedule** and **distribute it** from the Homebase site directly (not only by emailing Aegis), each writing a proper `activity_log` entry. The build/distribute report email already carries a Distribute button — bring the same to the web app, plus Build. Reuse the Aegis build + distribute internal endpoints.
6. **Veteran rules — Phase 2: custom-time event shifts.** Phase 1 covers all/min-N veteran rules on recurring shifts (seasonal) and on a specific date at normal hours. Phase 2: the one-off **event shift with CUSTOM times** that replaces the normal shift (e.g., swim meet 7am–2pm, veteran-only) — rides the `events` table / `shift_overrides` + a veteran field; engine reads and staffs it. (Surfaced 2026-06-13.)

> **ADDED 2026-06-17 (Alexander) — three new items (12–14):**
> 12. ✅ **DONE & LIVE (AG PR #38, HB PR #16; migration 016 applied).** Decisions resolved: notify **changed-only** employees; **archive** old schedule (superseded, not deleted). **Republish / swap the current week's schedule** (extends items 9 + 10, the Publish button + published-as-source-of-truth). A manager can build a SECOND, alternate schedule for the SAME week that's already published — for a mid-week overhaul, or to test a different week before committing — review/edit it (rebuild or hand-assign), then hit **Publish** on the new one, which **atomically unpublishes the old schedule and publishes the new one**. On republish: (a) **notify the affected employees** about the changes to their work week, and (b) **supersede the old schedule's saved wage/hours estimates** so reports + payroll reflect the new published schedule. Keys on the same `published_at` source-of-truth field as item 9. **TWO DECISIONS PENDING (Alexander):** (i) notify EVERYONE on the new schedule, or only those whose shifts actually CHANGED vs the old one (lean: changed-only with their new shifts, matching the "affected-people-only" rule) — Alexander leaning toward notifying all who are affected; (ii) **archive** the old schedule (keep the row, marked superseded, for history/undo) vs **hard-delete** it — recommend archive + clear only its derived wage/hours so nothing is lost.
> 13. ⏭ **QUEUED NEXT (quick win #2 of the next chat).** **Plain-English everywhere — start with the Rules-page explainer buttons.** Our clients are the **least technical group possible**; the product must be simple, intuitive, and free of jargon. Immediate: the Homebase Rules-page info/explainer buttons must explain each rule in **super plain English** — no terms like "attribute mix," "min-N," "scope," "concurrent coverage," "policy_value_json," etc. Standing rule going forward: every user-facing label, tooltip, button, and message (Homebase + Aegis) avoids technical jargon.
> 16. **★ STRATEGIC — Aegis + Soteria can do (almost) EVERYTHING across Homebase, conversationally.** Both the email/text brain (Aegis) and the in-app NL agent (Soteria) should be able to perform essentially **every manager action** by natural language — build / publish / republish schedules, edit shifts, manage time-off / availability / swaps / emergency coverage, set veteran + scheduling rules, manage wages / roles / employees, and answer any operational question — the full surface of Homebase, with **parity between Aegis and Soteria**. The ONE exception is the access-management page (UI-only — see item 17). **Why this matters (Alexander):** the product must NOT feel like "just another scheduling app." Conversational control of everything is the core differentiator. Umbrella goal — audit each manager capability and ensure both Aegis and Soteria can drive it; fill the gaps (item 3 "Soteria can build the schedule" is the first concrete piece).
> 17. **Access-management page — refresh + correctness pass (Homebase, UI-only).** The access page (add / remove users + assign roles) is deliberately kept OUT of conversational control — it gates who can do what, so it must be a deliberate, in-app action. It needs a pass-over to make sure it actually WORKS: people get **added** and **removed correctly** (deletes fully revoke access with no orphaned rows, role changes apply, adds create the right records). Audit the add/delete/role flows + the underlying DB writes + RLS.
> 15. **Consistent assets/styling across Homebase.** The same UI element must look identical everywhere — e.g., the **VET badge looks different in the schedule tab vs the data/employees tab**. Audit every shared element (badges, buttons, status pills, tags, icons, colors, spacing) and unify them into shared components/design tokens so nothing drifts tab-to-tab. Folds into the end-of-roadmap Homebase UI overhaul, but is a concrete do-anytime consistency pass.
> 14. ✅ **DONE & LIVE (PR #39).** **MANAGER-COMM-1 — manager inquiry answers are inaccurate and leak internals (Aegis).** Diagnosed 2026-06-17 from a live test ("How many employees did I have on staff today?" → a rambling non-answer that exposed internals and guessed numbers). **ROOT CAUSE:** `handleOperationalQuery` in `operational-query.ts` builds the answer's data context as `JSON.stringify(rows, null, 0).slice(0, 4000)` (line ~303) — it **truncates the schedules JSON mid-record**, so the model can't see today's assignments, hedges, and exposes the mechanics ("the schedule data provided is truncated," "the complete schedule payload," "pull the full June 17 slice from Homebase"). **FIX:** (a) for headcount/coverage questions, **compute the answer deterministically** from the schedule `data.assignments` (count distinct employees scheduled on the date, by role) and hand the model clean facts — don't dump truncated raw JSON; (b) remove / rework the 4000-char truncation for these tables (or pre-summarize per table); (c) **harden the answer prompt** so Aegis NEVER mentions internal mechanics (data / payload / records / truncated / "Homebase slice"), answers plainly + confidently in the warm brand voice, and if it genuinely lacks the data says so simply ("I don't have today's schedule loaded — want me to pull it up?") without explaining why. Ties into item 13 (plain English) + the voice target.
> 18. **Shift-swap testing + investigation (Aegis).** The shift-swap workflow is fully BUILT (`shift-swap.ts`: **directed** mode — "swap my Saturday with Jordan" — and **facilitated** mode — "can anyone take my Saturday?"; wage recalculation; manager approval; writes the swap onto the schedule via `executeScheduleSwap`) but has **ZERO automated tests** and has not been verified live. Investigate the full flow, run a sandbox end-to-end round-trip (employee initiates → target/candidate accepts → manager approves → schedule + wages update), add automated tests for both modes, and fix whatever's broken. (Added 2026-06-17.)
> 19. **★ Dynamic business partner — Aegis & Soteria explain themselves + guide users (extends item 4).** Beyond the reactive capabilities/help of item 4: when a user opens a conversation — or seems unsure what to ask — Aegis/Soteria proactively explain what they can do FOR THAT USER and walk them step-by-step toward what they're trying to accomplish, like a sharp business partner rather than a command parser. Role-aware, conversational, hand-holding — the "doesn't feel like another scheduling app" feel applied to the very first message. Builds on item 4 + the item-16 parity goal. (Added 2026-06-17.)

---

### #1 · SCHED-DELETE-1 — Delete-schedule button for managers + owners only (Homebase) — **NEW**
**Repo:** Homebase · **Status:** `DONE` (live-verified 2026-06-11) · **Phase tag:** `[P1]` (live-product hardening + UX gap)

**Resolution (2026-06-11):** Migration 014 applied — `schedules.deleted_at` column added + per-command RLS (incl. `FOR DELETE USING (false)` so the client can no longer hard-delete). Both repos merged + deployed to prod (Homebase server route `/api/schedule/delete` + Aegis read-site sweep filtering `deleted_at IS NULL`). Verified live: soft-delete sets `deleted_at`; schedule disappears from the schedule page, dashboard, and downloads; row preserved and reversible via `deleted_at = NULL`. Actor binary (manager vs owner) per convention; owner audit-granularity backlogged.

**Findings (read-only diagnosis 2026-06-11):** delete already live but UI-only (`page.tsx:969` + `confirmDeleteSchedule` client-side delete, no route); `schedules` RLS permits DELETE for any same-company user (security gap); zero FKs ref `schedules.id` (hard delete FK-safe); no soft-delete column. `03 §4.3` is correct; "no delete control today" and the FK-hazard note are stale/wrong; Tier 3 understates scope.

**Reframed scope:** SECURITY FIX + feature — server route (mirror SEC-1) + RLS DELETE lockdown (load-bearing) + temporal/role gate + soft delete (gated DDL) + read-site sweep (Homebase AND Aegis).

**Product decisions (confirmed 2026-06-11):**
- **(a) Temporal gate, computed server-side in company tz:**
  - current+upcoming (`week_end >= today`): manager, owner, quria
  - past (`week_end < today`): owner, quria (managers excluded)
  - tenant-bound for manager/owner; quria may cross-company. Distributed-schedule warning (type-to-confirm + "emails are not recalled") preserved regardless of role.
- **(b) Soft delete (`deleted_at`)** — required by the cross-Homebase Undo (Tier-1) goal.
- Note: RLS lockdown (client `FOR DELETE USING false`) closes the irreversible hard-delete hole; soft delete runs as a service-role UPDATE via the route. Residual: client could set `deleted_at` via direct UPDATE (low severity, reversible) — backlogged.


There is no delete-schedule control today; managers / owners need one. Build it role-gated end-to-end, not just hidden in the UI. Diagnose-first scope to capture:
- **Route-level authz.** A delete must be enforced server-side, not by hiding the button. **Mirror the SEC-1 `create-user` pattern** (Homebase `security/create-user-authz`): sign-in gate → role gate (`owner` / `manager` / `quria` only; the `'quria'` literal — `'quria_admin'` is an activity_log/ContactRole label only) → company-binding (owner is forced to own `company_id`; the body cannot override it; `quria` may target any company). Add a route-level test alongside the existing 22-case `security-authz.test.ts` pattern.
- **Hard vs soft delete given FK references off the `schedules` row.** Same FK hazard as `DELETE-USER` (Phase 1 above): `schedules.id` is referenced by at least `activity_log` rows and likely other tables; the default `ON DELETE NO ACTION/RESTRICT` will block a hard delete and a swallowed error will silently no-op. Before building: query `information_schema` / `pg_constraint` for every FK targeting `schedules.id`, and read the actual `ON DELETE` clause on each. Then pick the model — soft-delete (`deleted_at` timestamp + filter on the list view) is the lower-risk default; hard delete requires explicit `ON DELETE CASCADE` (or `SET NULL` on the FKs) plus a service-role route. **Do NOT touch prod DDL from an agent — gated.**
- **Confirmation UX.** Hard-to-reverse destructive action → require an explicit confirm step (type the week_start, or a typed phrase) before the route fires; surface the server error in the UI (don't swallow it — same trap as `DELETE-USER`'s `handleRevoke`).
- **Already-distributed schedules.** If `status='distributed'` or the schedule has already been emailed to employees, what happens on delete? Two acceptable shapes: (a) refuse and surface "this schedule was already distributed — soft-archive instead?"; or (b) allow but log/notify and clearly mark in `activity_log`. Decision pending; do not silently delete distributed schedules.
- **Data shape reminder.** `schedules.data` is JSONB keyed by `id` (Aegis reads it verbatim in `src/workflows/schedule-build.ts:1259-1291`). Delete must clean up `schedules.data` along with the row, and any soft-delete must hide the row from BOTH the Homebase list view AND any Aegis lookup that resolves a schedule by id.

**Done when:** anon/manager-cross-company/owner-cross-company calls → 403; same-company owner/manager → success (or refused-with-message on distributed); FK constraints handled deliberately (soft-delete or explicit cascade); confirmation step in UI; route-level test added; visible in `activity_log`.

### #2 · AEGIS-EMAIL-1 — Every Aegis email-action workflow works end-to-end + carries a test — **NEW**
**Repo:** Aegis + Homebase · **Status:** `DIAGNOSED` (2026-06-11) · **Phase tag:** `[P2]` (Forward Build Sequence Phase 2 — Complete the comms loop)

**AEGIS-EMAIL-1 — Status: DIAGNOSED (2026-06-11).** 8 action types defined; only 3 wired end-to-end (`approve_to`, `deny_to`, `confirm_distribution`); 5 are dead stubs that return a fake success page and are never minted by any email (`approve_availability`, `deny_availability`, `accept_emergency_coverage`, `decline_emergency_coverage`, `request_additional_batch`). `approve_to` is prod-verified (21 Watermark consumptions); `deny_to` is built but the email-button path was never consumed (in-app Time Off tab deny works; email button unverified — Alexander testing 2026-06-11); `confirm_distribution` is sandbox-only and had a cross-repo status-clobber bug (HB writes `status='distributed'`, AG overwrites to `'published'`) that defeated the re-distribution guard.

**TARGET STATE (assistant experience; launch is email-first — bring all flows to email parity):**
- **Manager** — distribute schedule (conversational command + Homebase button, NOT a magic-link email button); approve/deny time off (magic-link buttons in the TO email); approve/deny availability (magic-link buttons — to build, mirror TO); request a build (command); request emergency coverage (command); operational inquiries (conversational Q&A).
- **Employee** — ask about upcoming shifts; receive shifts (redesigned distribution email); submit time off; update availability; initiate a swap with a target.
- **Design rule:** notification-RESPONSE actions (approve/deny TO + availability) = buttons in the email; manager-INITIATED actions (distribute, build, coverage, queries) = conversational commands (+ Homebase button for distribute). Employee emails never carry Homebase CTAs; attachments fine.

**WORK LIST (priority order):**
1. **Distribute email redesign** — per-employee shifts/positions + attach the full week's all-staff schedule (reuse the Homebase schedule-grid renderer). DONE (merged PR #5/#6, deployed Railway 7c7f158e, live-verified preview desktop+phone)
2. **Remove the magic-link `confirm_distribution` path**; distribute via Homebase button + command; fix the re-distribution guard to key on `distributed_at`, not `status`.
3. **Build availability approve/deny magic-link buttons** (mirror TO) — real handlers + mint tokens in the availability notification email.
4. **Verify `deny_to` email-button path** (in progress).
5. **Verify emergency coverage (email) + operational inquiries (email).**
6. **Verify/build employee "ask about my shifts" + swap-over-email.**
7. **Remove or properly handle the remaining dead stubs** (currently return fake success).

Verify + fix + **test** every email-action workflow end-to-end: inbound email → magic-link issued → manager clicks link → action consumed via Homebase `/api/aegis-action` → correct DB effect (and, where applicable, employee notified). The token layer itself is already audited sound (SEC-4: 256-bit CSPRNG, SHA-256 hash-stored, atomic TTL + single-use enforcement). **This item is the workflows themselves**, not the crypto.

The set to cover (the `ActionType` union in `src/lib/aegis-actions/types.ts`):

| Action | Symptom if broken | Test shape |
|---|---|---|
| `approve_to` | manager click → TO row not set to `approved` OR `decided_by` empty OR employee not notified | sandbox TO request → approve via magic link → assert row + activity log + notify-call |
| `deny_to` | as above for denial | same shape with `deny_to` |
| `approve_availability` | manager approves availability update → `availability` row not written OR window not applied | sandbox availability request → approve → assert `availability` rows match |
| `deny_availability` | as above for denial | same |
| `accept_emergency_coverage` | employee clicks accept → shift not reassigned OR no flag cleared | sandbox EC request → accept → assert `schedules.data.assignments` update + notify |
| `decline_emergency_coverage` | as above for decline | same |
| `confirm_distribution` | manager click → distribution doesn't fire / fires partially / not idempotent on re-click | sandbox build → click confirm → assert distribution log + idempotent on second click |
| `request_additional_batch` | manager click → no follow-up batch issued / batch issued but wrong context | sandbox build → click → assert next batch issued with correct payload |

Plus any sibling/follow-up workflows surfaced during this pass (e.g. swap-accept/decline, onboarding fan-out steps) — log them in `EMAIL_WORKFLOWS_TRACKER.md` as they come up rather than expanding this list speculatively.

**Test runner constraint.** Homebase has NO test runner yet (tracked Tier-3 — `vitest` exists only on the throwaway `test/security-verify` branch). "Tested" here may mean standing up the runner first (vitest config + `test` script + first commit on Homebase) AND porting the existing 22-case `security-authz.test.ts` over before the workflow tests land. Aegis has a working harness in `src/lib/engine/__tests__/` (vitest) — workflow tests on the Aegis side can reuse it.

**Track per-workflow status in `EMAIL_WORKFLOWS_TRACKER.md`.** Each row above gets a tracker entry that flips through `OPEN → DIAGNOSED → FIXING → IN REVIEW → DONE (test green + sandbox round-trip green)`. The tracker already has Phase 7 ("risky fan-outs") and the TODO/UNTESTED intents — fold AEGIS-EMAIL-1 above those as the umbrella for the per-workflow verification + test pass.

**Done when:** all 8 `ActionType`s have (a) a sandbox end-to-end round-trip that produces the expected DB effect and notification, AND (b) a committed automated test that exercises the route + asserts the effect. `EMAIL_WORKFLOWS_TRACKER.md` per-workflow rows all show `DONE`.

### #3 · TEMPLATE-EDIT-1 — Schedule template edits persist AND re-render the current schedule — **RE-PRIORITIZED behind #1 + #2**
**Repo:** Homebase · **Status:** `OPEN, queued behind #1 + #2` · **Phase tag:** `[P1]` (live-product gap)

Previously a Tier-3 polish item; promoted here because on-site managers can't actually save template edits. **Expanded scope (2026-06-10):** the fix must (a) make template edits actually persist to `schedule_templates`, AND (b) re-render the currently-viewed schedule against the edited template so the manager sees the effect of their edit without manually rebuilding. Today neither happens.

**Banked leads (do not redo the diagnosis; carry forward):**
- **Distinct path from SCHED-EDIT-1.** Schedule edits flow through `ScheduleReviewPanel.save()` → `schedules.data` (JSONB keyed by id). Template edits flow through `useScheduleTemplate()` (`src/lib/hooks/useScheduleTemplate.ts`) → `schedule_templates` table. **No shared code; fixing one does not fix the other.**
- **Strongest behavioral lead:** `useScheduleTemplate.ts:67` — `if (!error && data) { setTemplate(data) }`. The error branch does NOTHING. `TemplateEditorPanel.handleSave` (`TemplateEditorPanel.tsx:186-191`) `await`s `saveTemplate(local)` and calls `onClose()` regardless. **On-site symptom ("edits don't take effect / won't save, no error shown") matches this exactly** — fix is to surface the error to the user AND not close the panel on failure.
- **Verify before the fix lands:** `.upsert(payload, { onConflict: 'company_id' })` requires a UNIQUE (or PK-component) constraint on `schedule_templates.company_id`. Query `information_schema.table_constraints` + `key_column_usage` for `schedule_templates` on prod (or Supabase Dashboard) — if the constraint is missing, the upsert silently inserts a duplicate row and no edit ever appears to take. See `SCHEMA_DRIFT_LOG.md` 2026-06-10 entry.
- **Sibling Tier-3 entry:** `saveTemplate id:''` bug — the id-strip is OK (`id ? next : rest` correctly handles `id === ''` → uses `rest`); the failure is downstream of that. Worth a single pass alongside this fix.
- **(b) Re-render the current schedule against the edited template.** Currently editing the template doesn't reflect on the actively-viewed schedule's structure (shifts/rows). Decide the contract: on template-save success, either trigger a re-render of the current week's view against the new template (cheaper UX, no DB write), or offer a "rebuild this week against the updated template" action (clearer, but heavier and rewrites the schedule). Lean toward the lighter re-render-only-the-view option unless rule-driven slot changes require an actual rebuild — needs the diagnosis pass.

**Done when:** a template edit (a) round-trips to `schedule_templates` with the change persisted and reloadable, (b) surfaces any save error to the user (panel does NOT close on failure), (c) the actively-viewed schedule re-renders to reflect the new template, and (d) carries a test on the route + the hook.

### Ordering
**Strict order: #1 → #2 → #3.** SCHED-DELETE-1 first (small, well-scoped, clears a manager UX gap and pattern-tests the SEC-1 authz approach on a second route). AEGIS-EMAIL-1 second (the comms-loop verification + tests are the highest-leverage thing for Phase-2 confidence). TEMPLATE-EDIT-1 third (queued behind because it's larger, on-site-only verifiable, and the banked lead is strong enough that picking it up after the test runner is stood up — which AEGIS-EMAIL-1 may do — makes the fix + test cheaper).

**IDs note.** `SCHED-DELETE-1` and `AEGIS-EMAIL-1` are new in the project; no collision with existing IDs (`ENGINE-1/2`, `SCHED-EDIT-1`, `TEMPLATE-EDIT-1`, `DOWNLOAD-500`, `DELETE-USER`, `SEC-1..4`, `DELIV-1`, `BUG-5`, `CASCADE-1/2`, `SANDBOX-SEED-1`, `POLICY-JSON-SHAPE-1`).

---

## CURRENT SPRINT — 48-hour priority (started June 8) — **COMPLETE 2026-06-09**

All four sprint items closed: ENGINE-2/gender rule, S2/SCHED-EDIT-1, S3/in-tab TO notify shipped and live-verified; S1/ENGINE-1 closed-as-diagnosed (no engine bug — JL residual is structural, routed to Role Groups; two product decisions pending — see S1 entry). Next focus: Cowork operating model, then the forward plan (`PRIORITY2_ANALYSIS.md` A/B/C).

### S1 · ENGINE-1 — Builder skips eligible employees
**Repo:** Aegis (`src/lib/engine/`) · **Status:** `CLOSED-AS-DIAGNOSED` (no engine bug; JL residual routed to Role Groups; two product decisions pending)
Aaron Barrigan (Headguard, fully available) is never placed. Erin Berigan reported as "can't work" with no custom availability. Suspected systemic, not one-off.

**Post-sprint determination (2026-06-09):** ENGINE-1 is **not** an engine code bug. The two named cases dissolved under diagnosis: "Aaron Barrigan" = Erin Berigan (one employee, not two), and Erin's exclusion was a 15-min availability-precision issue (data fix, applied + verified). The remaining systemic miss — **4 Junior Lifeguards (Jenna Stibitz, Cameron Osterhaven, Colin Marvin, Quin Mead) get 0h because no `Junior Lifeguard` shift_requirements / canvas slots exist this week** — is **structural** and is **routed to Role Groups** (Tier 2). It is NOT an ENGINE-1 code fix. ENGINE-1 stays `DIAGNOSED` (not `DONE`) and is **blocked on two Alexander/manager decisions**: (a) true Afternoon shift end — 21:00 or 21:15? (b) does Watermark schedule Junior Lifeguards at all (off-roster vs Role Groups / fold into Lifeguard)?

- Diagnosis (fill in before fixing):
  - [x] Dry-run built for the affected week; `per_employee_dispositions` dumped for Aaron, Erin, **and full roster**
  - [x] Aaron's & Erin's `employees` rows pulled (`primary_role`, `qualified_roles`, `max_weekly_hours`, `active`) + their `availability` rows
  - [x] Disposition reason identified (`not_qualified` / `availability_mismatch` / `max_hours_reached` / `eligible_but_unchosen` / …)
  - [x] Determined: roster-wide pattern or isolated? (count how many employees show the same reason)

**Findings (2026-06-08 diagnostic pass — dry-run for build week 2026-06-15..21):**

Method: `scripts/dry-run-schedule.ts` (next week) + `scripts/diagnose-s1.ts` (one-off, traces date-level eligibility for every employee × every canvas slot and pulls Aaron/Erin from the live DB). Both ran against live Watermark data with no writes.

1. "Aaron Barrigan" does not exist in employees and is CONFIRMED (Alexander, 2026-06-08) to be a misremembering of Erin Berigan — there is no separate employee. The two named ENGINE-1 cases are therefore ONE case: Erin. Her availability was entered ending 21:00 while the Afternoon shift ends 21:15, locking her out of evenings. Availability extended (data fix) and verified 2026-06-08.

2. **Erin Berigan — exists, active, and IS being placed (4.5h), but is structurally locked out of the dominant evening shift.**
   - Row: `id=1b868570-1ade-443d-9781-4cbe16a5cf06`, `primary_role=Headguard`, `qualified_roles=["AManager","Headguard","Lifeguard"]`, `max_weekly_hours=40`, `active=true`, `is_veteran=true`.
   - Availability: 7 rows, every day, **09:00:00–21:00:00**. No `custom_availability`. No approved TO in the build week.
   - Dry-run placement: **1 shift, Friday AM Weekday Headguard 11:00–15:30 (4.5h)**.
   - **Why she's shut out of the largest Headguard pool (Afternoon, 7×wk):** the "Afternoon" shift runs **15:00:00–21:15:00**. `isAvailableForShift` (`src/lib/engine/eligibility.ts:17–29`) requires the availability window to **fully contain** the slot: `availStart ≤ slotStart && availEnd ≥ slotEnd`. `21:00:00 < 21:15:00` → mismatch on every Afternoon slot, every day. Disposition code = `availability_mismatch`. This is the literal cause of the manager's "she's marked as can't work" perception — the engine truthfully says she can't cover *any* Afternoon. **Off by 15 minutes is the binding constraint, not a code defect.** A fix is a product decision (15-min grace window? Shorten Afternoon end? Edit Erin's availability to 21:15?), not a code bug per se.

3. **Roster-wide tally (distinct employees ever excluded by a date-level reason, summed across all 70 canvas slots):**
   - `not_qualified`: **29** (every active employee gets `not_qualified` against at least one slot whose role they don't hold — this counter, on its own, is uninformative. The interesting subset is below.)
   - `availability_mismatch`: **5** (Kori Baumann, Ally Becker, Erin Berigan, Michael McCorkle, Letizia Cumbo-Nacheli) — same root pattern as Erin worth checking per employee.
   - `on_time_off`: **3** (Will Roelofs, Miles Holter, Rosa Thornburg).
   - `inactive`: **0**.
   - **`eligible_but_unchosen` / `max_hours_reached` / `doubles_blocked` / `in_conflict` — not measured this pass:** the dry-run came in at 70/70, 0 gaps, so the engine never wrote a single `per_employee_dispositions` block (those only generate for unfilled slots). To get slot-level reasons, we need a week with real gaps, or a synthetic stress run.

4. **The real systemic pattern: 4 active employees were NEVER date-level-eligible for ANY slot this week, and the engine never surfaces them.** All four are Junior Lifeguards:
   - Jenna Stibitz, Cameron Osterhaven, Colin Marvin, Quin Mead — `qualified_roles=["Junior Lifeguard"]`, all available 7d × 00:01–23:59.
   - Canvas this week has **zero `Junior Lifeguard` slots**. Distinct slot.role values: `Headguard` (14), `Lifeguard` (35), `Manager` (14), `Greeter` (7).
   - A 5th, Nick Jovanovic (`qualified_roles=["Junior Lifeguard","Greeter"]`), was eligible for the 7 Greeter slots but ranker chose Bennet/Kason instead → 0h.
   - **These five do not appear in any `per_employee_dispositions` list because no gap exists for the slots they're qualified for.** A manager looking at the schedule sees them silently dropped. This is the "suspected systemic" pattern in the bug report.
   - Fix is structural (Role Groups — already in Tier 2 backlog as `shift_requirements.accepted_roles`), not a one-line eligibility patch. As a short-term diagnostic, we could surface an "active employees with zero eligible slots this week" flag on every build.

5. **String-mismatch / casing check (this was the original suspicion in the bug report):**
   - Distinct `slot.role` values in canvas: `"Headguard"`, `"Lifeguard"`, `"Manager"`, `"Greeter"`.
   - Distinct `qualified_roles` values across active employees: `"AManager"`, `"Greeter"`, `"Headguard"`, `"Junior Lifeguard"`, `"Lifeguard"`, `"Manager"`.
   - **No casing or whitespace mismatch for any role currently in the canvas.** `isQualifiedForRole` uses exact-string `.includes(role)` (`eligibility.ts:11–13`); it would silently fail on `" Headguard"` or `"headguard"`, but none of those exist in this data. So the bug is **not** roster-wide string normalization.
   - CORRECTION (2026-06-08): "AManager" in Erin Berigan's and Michael McCorkle's qualified_roles is NOT a typo. Per reference doc 01, AManager = Assistant Manager, a legitimate Watermark role (Erin is the named AManager, "maps to Headguard/Lifeguard in practice"). It is inert only because no shift_requirements carry role='AManager'. DO NOT rewrite it to "Manager" and DO NOT log it as schema drift — it is expected data. Open (low priority): should AManager be its own schedulable slot, or always resolve to Headguard/Lifeguard? Verify Michael McCorkle is in fact an Assistant Manager.

**Plain-English summary:** The bug report's two named cases dissolve under inspection. Aaron isn't in the database — confirm with Alexander whether the name is wrong or the employee just needs adding. Erin is being placed, but is locked out of every Afternoon shift because her availability ends at 21:00 and Afternoon ends at 21:15 — an off-by-15-min data precision issue, not an engine defect. The real systemic miss is structural: four Junior Lifeguards with no matching shift_requirements get silently skipped, and the engine never produces a disposition row for them because their slots don't exist to gap on. The casing/normalization hypothesis was checked and ruled out for this week's roster, but `"AManager"` on two employees confirms free-text `qualified_roles` is a real future risk.

- Fix (only after diagnosis):
  - [ ] **Decision needed from Alexander before coding:** (a) Is "Aaron Barrigan" a real employee that needs to be added, or a misremembering of Erin Berigan? (b) For Erin's 21:00 vs 21:15 mismatch — grace window in the engine, shorten Afternoon to 21:00, or edit Erin's availability to 21:15? (c) For the four Junior Lifeguards — is this expected (off-roster this week) or should we accelerate Role Groups / fold Junior Lifeguard into Lifeguard `qualified_roles`?
  - [ ] Add a build-time diagnostic: warn when active employees have zero eligible slots in the week (so the Junior Lifeguard pattern surfaces without needing a manual trace next time).
  - [ ] Clean up `"AManager"` in Erin's and Michael's `qualified_roles` (data fix, not code).
  - [ ] Dry-run re-run confirms the missing people are now placed (or are knowingly off-roster).
  - [ ] DECISION (Alexander, operational): true Afternoon shift end — 21:00 or 21:15? Fix whichever data point is wrong. NOT an engine grace window (rejected — global containment change to mask one data point).
  - [ ] DECISION (Alexander, domain): are the 4 Junior Lifeguards off-roster this week (expected) or should JL be schedulable (Role Groups / fold into Lifeguard)?
  - [ ] CODE (greenlit pending): build-time diagnostic warning on active employees with zero eligible slots in the built week.
- **Done when:** the affected employees are scheduled in a dry-run, and no other employee is being silently dropped for the same reason.

### S1b · ENGINE-2 — Hours not distributing across the roster (suspected fairness)
**Repo:** Aegis (`src/lib/engine/ranker.ts`, `schedule-build.ts` fill loop, `src/lib/engine/sex-coverage.ts`) · **Status:** `DONE`

**Resolution (2026-06-09, fully live-verified):** Per-shift `attribute_mix` sex swap — the real cause of the bimodal Headguard hours — has been **replaced** with `sex_coverage` (scope=`concurrent_coverage`, validate-and-flag, no swap). The policy `policy_value_json` has been **flipped** to the new model. **Live-verified by the 6/15 Watermark build:** Lucas 26.3h → 15.3h, Erin 6.3h → 10.8h; the coalesced `unsatisfied_sex_coverage` flag renders in BOTH the manager schedule-preview email AND the Homebase Preview & Edit view (`CoverageFlags` mounted in the UpcomingCard preview, Homebase pushed at `29ed00e`). The post-fill swap pass is dormant for Watermark (the parser yields no sex `attribute_mix` under the new json). The swap code itself still exists generically and would fire for any tenant with an `attribute_mix`-shape policy — captured as a separate Tier 2 decision (see backlog).
After Erin's evening availability fix, a fresh next-week sample still places her once while a few names repeat (e.g. Audrey Rook, Headguard, 3 afternoons). Expectation: accumulated hours should lower an employee's rank for later slots so hours spread. Symptom suggests hours-fairness may not be accumulating during the fill — NOT yet confirmed. Diagnose-first; do not touch the ranker until a trace names the deciding sort key.

**Findings (2026-06-08 instrumented trace):** NOT a fairness bug. The hours-fairness pipeline is verified intact: rankCandidates is called fresh per slot (schedule-build.ts:500/521), each placement updates weekState.weeklyHoursMap immediately (:596), and the sort key reads it (ranker.ts:48/61/76). Erin's availability fully contains the Afternoon Headguard slot on all 7 days — availability ruled out.
ROOT CAUSE: enforceAttributeMixForShift runs as a post-fill pass to satisfy an ACTIVE gender_requirement (attribute_mix sex: >=1 male + >=1 female per shift, scope=all_shifts). ~3 swaps/week. When it swaps a ranker-awarded employee OUT, the displaced person is not re-placed, and same-day-doubles that fired during the main fill loop are not re-evaluated — so they lose slots they were blocked from. The hours ledger IS refunded (attribute-mix.ts:284-285); re-placement is what's missing. Net: Erin, Kori, Michael each end at 6.3h (one shift) while Lucas (repeatedly inserted male) hits 26.3h. Headguard distribution bimodal across 6 HGs / 14 slots split 3M/3F.
TWO PROBLEMS:
- (A) Engine correctness: post-fill swaps don't reconsider/backfill the displaced employee.
- (B) Policy decision (Alexander): is '>=1 of each sex on EVERY shift' intended? Options: keep broad / narrow scope / remove. DECIDE FIRST — it sets how much of (A) is needed.
DOC DISCREPANCY: gender_requirement is documented as DORMANT (doc 04 §2.4, doc 06 §9) but is configured and active. Flag for reference-doc correction and verify in the Homebase Rules tab.
DECISIONS NEEDED:
- [ ] (B) Confirm/narrow/remove the gender attribute_mix policy.
- [ ] (A) After (B): surgical backfill vs structural fold-of-attribute-mix-into-fill-loop. Lean structural if the rule stays broad.
- **Done when:** displaced employees are reconsidered so Headguard hours flatten, without violating the confirmed gender policy. No fix until (B) is decided.

**Agreed fix direction (2026-06-08):** ENGINE-2 is resolved by REPLACING the gender rule's model, not by patching the swap. The real requirement (Alexander) is facility-wide temporal coverage: ≥1 male + ≥1 female on duty among counted roles at every moment the pool is staffed — NOT per-shift composition. New constraint sex_coverage: attribute=sex, minimums {male:1,female:1}, scope=concurrent_coverage (evaluate over the day's timeline segmented at shift boundaries), population_roles=[Headguard, Lifeguard, AManager] (Greeter + Junior Lifeguard NOT counted; pure Manager pending confirm), on_infeasible=flag. Evaluation = validate-and-FLAG (FlaggedIssue 'unsatisfied_sex_coverage'); NO swap pass. Retiring the per-shift enforceAttributeMixForShift swap eliminates the ENGINE-2 churn and makes Problem A (no backfill) moot. Ship as pure validate-and-flag first; add a forward-looking soft fill-loop coverage bias only if flags prove common. Contract-first (engine before Rules-tab UI), like Role Groups. MIGRATION: remove the current attribute_mix sex scope=all_shifts policy + its swap pass in the same change to avoid double-application.
Decisions confirmed: JL not counted; presence = guard staff on floor; flag-don't-force; no single-staff open windows (only Greeter/Flex are 1-person shifts). Managers NOT counted (confirmed 2026-06-08). Population FINAL: Headguard, Lifeguard, AManager. sex_coverage implementation spec drafted 2026-06-08; build may run ahead of policy flip, but the policy_value_json migration is held pending Watermark management sign-off on the rule.
DOC FIX still needed: gender_requirement documented dormant (doc 04 §2.4, doc 06 §9) but is live — correct on next doc pass, and replace the per-shift framing with the concurrent_coverage model.

DECISION 2026-06-08 (Alexander): NO Phase 2 soft coverage bias — rejected to keep the engine generic/config-driven. sex_coverage stays validate-and-flag, toggleable per client. Because flag-only is the chosen model, the coverage flag is now the safety mechanism: it MUST surface as a visible manager review/action item in the Homebase schedule view (not only the Aegis email). Remaining sex_coverage work: (a) Homebase renders + surfaces the no-shift_name coverage variant as an action item; (b) coalesce contiguous same-missing-sex flags into one; (c) management sign-off + policy_value_json flip to scope=concurrent_coverage.
**UPDATE 2026-06-09:** (a) DONE — Homebase `FlaggedIssue` reconciled to the Aegis union, and new `CoverageFlags` component renders `unsatisfied_sex_coverage` as a "Coverage to review" manager action item (date, time window, missing sex, on-duty) in the schedule view + history report detail. (b) DONE — `sex-coverage.ts` now coalesces time-contiguous same-missing-sex segments into one flag (verified by a synthetic check: 3 contiguous segments → 1 flag 11:00–18:00; a satisfied middle window correctly stays 2 flags). (c) STILL PENDING — needs management sign-off + the policy_value_json flip (exact SQL prepared this session, presented to Alexander to run in Supabase; NOT executed by the agent). Live-roster verify harness (hours-flatten + real flags) could NOT run from the agent sandbox (no network egress to Supabase) — run `scripts/verify-sex-coverage.ts` where the DB is reachable. The flip retires the per-shift attribute_mix swap automatically: once the sex policy's json is scope=concurrent_coverage, the parser yields a concurrentCoverage constraint and no sex attribute_mix, so the swap pass has nothing to act on.

### S2 · SCHED-EDIT-1 — Manual schedule edits don't persist
**Repo:** Homebase (`src/app/(app)/schedule/page.tsx`) · **Status:** `DONE` — fix committed (`f28cb30`), Homebase pushed (`29ed00e`), live-verified 2026-06-09: a manual assignment move persists the corrected hours through to `schedules.data.assignments`.
Moving an employee between shifts updates the displayed card but not `schedules.data.assignments`; distribute then sends the new shift name with stale hours. **This gates safe distribution — no manual-edited schedule may be distributed until this is green.**

- Diagnosis:
  - [x] Manual-edit save handler read; determined whether it persists to Supabase at all
  - [x] Identified which fields it carries vs drops on a move (`shift_name` vs `start_time`/`end_time`/`role`/`hours`)

**Findings (2026-06-08 diagnostic pass):**

1. **Move handler (the part that runs when a card is dragged between cells).**
   File: `src/components/schedule/ScheduleRenderer.tsx`, lines 512–530. Triggered from `handleDragEnd` (lines 555–561), which reads the drop target out of `DroppableCell` props (`shiftName={row.id}`, `date={date}` — lines 641–642).

   ```ts
   function moveAssignment(source: ScheduleAssignment, targetShift: string, targetDate: string) {
     if (!onAssignmentChange) return
     if (source.shift_name === targetShift && source.date === targetDate) return

     let moved = false
     const next = assignments.map(a => {
       if (
         !moved &&
         a.employee_id === source.employee_id &&
         a.shift_name === source.shift_name &&
         a.date === source.date
       ) {
         moved = true
         return { ...a, shift_name: targetShift, date: targetDate }
       }
       return a
     })
     if (moved) onAssignmentChange(next)
   }
   ```

   `onAssignmentChange` is wired to `setPendingAssignments` in `src/app/(app)/schedule/page.tsx` (lines 1394, 1467). So a move only mutates *local React state* (`pendingAssignments`); the renderer itself never touches Supabase.

2. **Persistence to Supabase — does happen, but only on explicit Save in the review panel.**
   The schedule page does not auto-persist on each move. Persistence runs from `ScheduleReviewPanel.save()` (`src/components/schedule/ScheduleReviewPanel.tsx` lines 188–216), invoked after the user opens the Soteria review drawer and clicks Save Changes:

   ```ts
   const newData = {
     ...(schedule.data ?? { assignments: [], gaps: [], summary: '' }),
     assignments: pendingAssignments,
   }
   const { data: saved, error: updateErr } = await supabase
     .from('schedules')
     .update({ data: newData, staffing_report: newReport })
     .eq('id', schedule.id)
     .select()
     .single()
   ```

   So *when* a save happens, the **entire `pendingAssignments` array is written back to `schedules.data.assignments`**, replacing it. Other top-level columns of `schedules` are untouched; inside `data`, only `assignments` is overwritten (other keys — `gaps`, `summary`, `closed_dates`, etc. — are preserved via spread). The `staffing_report` column is recomputed alongside.

   **This means the persistence mechanism itself is fine. The bug is upstream in `moveAssignment`'s reshape of the row, which then gets dutifully persisted.**

3. **Fields on a move — what updates vs goes stale.**
   `ScheduleAssignment` (`src/lib/types.ts` lines 186–196) has nine fields. After a move, in the pending row that gets persisted:

   | Field            | Updated? | Source on move | Notes |
   |------------------|----------|----------------|-------|
   | `date`           | ✅ updated | `over.date` from drop target | correct |
   | `shift_name`     | ✅ updated | `over.shift_name` = `row.id` of the target `DroppableCell` | correct (RowConfig.id IS the shift_name per `src/lib/types.ts:155–161`) |
   | `employee_id`    | ✅ preserved | (carried) | correct |
   | `employee_name`  | ✅ preserved | (carried) | correct |
   | `employee_photo` | ✅ preserved | (carried) | correct |
   | **`role`**       | ❌ **stale** | carried from source assignment | wrong when shifts have different roles |
   | **`start_time`** | ❌ **stale** | carried from source assignment | wrong whenever target shift has different hours |
   | **`end_time`**   | ❌ **stale** | carried from source assignment | same |
   | **`hours`**      | ❌ **stale** | carried from source assignment | same — this is the field that drives the email's hour total |

   **Where the target's correct `start_time` / `end_time` / `role` would come from:** `shift_types` (the canonical source per the deprecation notice on `shift_templates` in `src/lib/types.ts:122–140`). The renderer does *not* have `shift_types` data in scope — only `RowConfig` (`id`, `label`, `height`, `visible`, `order`), which carries no time/role information. So `moveAssignment` cannot recompute these locally without a new data dependency. This is the structural reason it doesn't.

4. **Distribute path — confirms exactly what gets sent.**
   Homebase only sends `{ schedule_id }` to Aegis (`src/lib/aegis-actions/dispatcher.ts:286–289`). Aegis then re-reads the schedules row and reads **`schedules.data.assignments`** — confirmed in `~/Desktop/Aegis/src/workflows/schedule-build.ts:1259–1291`:

   ```ts
   .from('schedules')
     .select('id, week_start, week_end, data, status')
     .eq('id', scheduleId)
   ...
   const schedData = scheduleRow.data as unknown as ScheduleData;
   ...
   const myShifts = schedData.assignments.filter(a => a.employee_id === emp.id)...
   ```

   The email/SMS templates render `s.shift_name`, `s.role`, `s.start_time`, `s.end_time`, `s.hours` straight off each assignment (`schedule-build.ts:1303–1331`). No second lookup against `shift_types` to correct the times — whatever sits in `schedules.data.assignments` is what employees receive.

**Net diagnosis:** the manual edit *does* persist to `schedules.data.assignments` (the Save path is whole-array overwrite, not partial). The bug is that `moveAssignment` only patches `shift_name` + `date` and leaves `start_time`/`end_time`/`role`/`hours` set to the *source* shift's values, so the persisted row is internally inconsistent ("new shift name, old hours"). Aegis then distributes that inconsistent row verbatim, producing the reported symptom.

**Fix shape (not implemented yet):** the move handler needs the target shift's authoritative `start_time` / `end_time` / role(s) at the moment of drop. Two viable spots — recompute inside `moveAssignment` (requires passing `shift_types` into `ScheduleRenderer`), or normalize inside `ScheduleReviewPanel.save()` before writing (single chokepoint, also catches `add_shift` paths). Decide on direction before writing the fix.

**Agreed fix approach (2026-06-08):** Shared pure resolver resolveAssignmentForSlot, called at BOTH moveAssignment (live UI correctness) and ScheduleReviewPanel.save() (persist chokepoint). Preserve role (it is NOT stale on a move — corrects finding-3). Resolve start/end by copying from a sibling assignment in the same shift_name+date (engine-authoritative this week); fall back to shift_types only for empty targets. Recompute hours from the resolved window.

- Fix:
  - [x] A move recomputes the **full** assignment object from the target slot and persists the complete `ScheduleData`
  - [x] Verified: edit → reload → DB shows corrected hours *(live round-trip confirmed 2026-06-09; a test distribute against real data still gated by distribution rules + DELIV-1, but the data axis — what distribute would read — is now correct)*
- **Done when:** a manual move round-trips to the DB with all fields correct and distribute reflects it.

Session recap (2026-06-08) reports S2 implemented (4 files + 2 helpers, claimed tsc clean) but NOT yet diff-reviewed by Alexander. PENDING: read full diff; run npx tsc --noEmit independently; verify the empty-target fallback time source (shift_types vs shift_requirements — original S1 PART 3, still unconfirmed). Common-case sibling-copy path is fine; the fallback is the risk.

Implemented (uncommitted, 2026-06-08): new helpers src/lib/schedule/resolveAssignment.ts + hours.ts; edits to ScheduleRenderer, ScheduleReviewPanel, plus UNREQUESTED edits to GapResolverPanel + ManualScheduleBuilder (likely shared-hours dedup — verify behavior unchanged). PENDING Alexander: read all diffs incl. the two unscoped files; independent npx tsc --noEmit; verify resolveAssignment empty-target fallback time source (shift_types vs shift_requirements, still unconfirmed). tsconfig.tsbuildinfo should not be tracked.

**RESIDUAL RESOLVED (2026-06-09):** All three PENDING items above are now cleared. (1) The fix is committed in Homebase as `f28cb30` (not uncommitted) — `resolveAssignment.ts` + `hours.ts` + edits to ScheduleRenderer/ScheduleReviewPanel/GapResolverPanel/ManualScheduleBuilder; `tsconfig.tsbuildinfo` is now gitignored. (2) **Empty-target fallback time source = shift_types — CONFIRMED CORRECT, matches the engine.** `buildCanvas` (Aegis `src/lib/engine/canvas.ts:89,100-105`) sources a slot's `start_time`/`end_time`/`hours` from the **shift_type** (`st.start_time`/`st.end_time`); only `role` comes from the shift_requirement (`req.role`). The Homebase fallback (`resolveAssignment.ts:24-27`) looks up `shiftTypes` by name and copies `st.start_time`/`st.end_time` — same source. Note: `shift_requirements` *has* its own `start_time`/`end_time` columns, but `buildCanvas` ignores them — shift_types is authoritative. The save-time backstop (`ScheduleReviewPanel.save()` lines 193-201) fetches real `shift_types` from Supabase and re-resolves every pending row, so even an empty-target move normalizes against shift_types before persisting. (3) Independent `npx tsc --noEmit` on Homebase = **0 errors**. (4) The two unscoped files (GapResolverPanel, ManualScheduleBuilder) are pure dedup — they delete byte-identical local `computeHours` definitions and import the shared `@/lib/schedule/hours`; behavior unchanged. (5) **CLOSED 2026-06-09:** Homebase pushed (`29ed00e`) and the live edit→reload round-trip verified — a manual move persists the corrected hours to `schedules.data.assignments`. Status flipped to DONE.

### S3 · Manual TO approval in Homebase doesn't notify the employee
**Repo:** Homebase Time Off tab → Aegis notify bridge · **Status:** `DONE` — committed (`f8e2505`), Homebase pushed (`29ed00e`), Vercel env confirmed, **sandbox approve-TO round-trip verified 2026-06-09**: in-tab Approve fired the employee notification, `decided_by` was written, and the manager-facing toast surfaced the "got it — change made and employee notified" acknowledgment. Magic-link path delegates to the same shared helper.
The email magic-link approval notifies the employee; the in-tab Homebase approval does not. Also set `decided_by`, and have Aegis acknowledge the acting manager.

- Diagnosis:
  - [x] In-tab approve/deny path read; Aegis employee-notification function (used by the magic-link path) located
- Fix:
  - [x] In-tab approval fires the same employee notification (via the shared helper → `/internal/notify-to-decision`)
  - [x] `decided_by` set on the in-tab path (from the server auth cookie)
  - [x] Manager gets a "got it — change made and employee notified" acknowledgment (toast surfaces the helper's message, incl. notify channel)
- **Done when:** approving a TO in Homebase notifies the employee and the manager, and `decided_by` is populated.

**Findings (2026-06-08):** In-tab TimeOffTab.handleDecision (TimeOffTab.tsx:230-241) updates status + decided_at, logs activity, refetches — and stops. Missing vs magic-link path: employee notification, decided_by, race guard (.eq('status','pending')), manager acknowledgment. Notification primitive postToAegisInternal('/internal/notify-to-decision') exists in Homebase (aegis-internal.ts) but needs server env vars; TimeOffTab is a client component so it can't call it directly.
Agreed fix approach: extract shared decideTimeOffRequest helper (guarded update w/ decided_by from server auth cookie, activity log, fire-and-tolerate notify, return manager-facing message); add server route POST /api/time-off-decision; refactor magic-link dispatcher to call the same helper; TimeOffTab calls the route and surfaces the message (toast). Pre-fix checks: grep src/ for other client-side time_off_requests.update({status}) callers; confirm AEGIS_URL + AEGIS_INTERNAL_SECRET set on Vercel. Out of scope: cross-notify other managers (fast-follow).

**BUILT (2026-06-09, uncommitted — Homebase):** exactly the agreed shape, 4 files.
- NEW `src/lib/time-off/decide.ts` — `decideTimeOffRequest(input)`: guarded pending-only update setting `decided_by`, activity-log entry (`source: 'magic_link' | 'in_tab'`), fire-and-tolerate `postToAegisInternal('/internal/notify-to-decision')`, returns a "feels-like-a-person" manager message (incl. notify channel / config-missing fallbacks). Dates parsed local-time per Doc 5 §6.1.
- NEW `src/app/api/time-off-decision/route.ts` — POST; authenticates the manager via the cookie session (`createServerSupabase().auth.getUser()` → `users.company_id`), loads the request with the service-role client, enforces same-company ownership, calls the helper with `source: 'in_tab'` + the manager's identity, returns the result (200 ok / 409 already-decided / 4xx).
- `src/lib/aegis-actions/dispatcher.ts` — `handleTimeOffDecision` refactored to delegate to the shared helper (−96 lines net). Removed now-dead `NotifyToDecisionResponse` type + unused `AegisInternalError` import + unused `dateRange` local. `logDecision`/`logAegisDeliveryFailure` retained (still used by the distribute path).
- `src/app/(app)/data/tabs/TimeOffTab.tsx` — `handleDecision` now POSTs to the route and surfaces the returned message as a dismissible toast; Approve/Deny disabled + "Saving…" while in flight. The old client-side `time_off_requests.update` + `logActivity` removed from this path.

Pre-checks done: (1) Only client-side `time_off_requests.update({status})` caller was `TimeOffTab` itself — `page.tsx:389` is a read-only SELECT (out-this-week), not a decision writer. No other competing path. (2) `AEGIS_URL` + `AEGIS_INTERNAL_SECRET` are referenced in code + declared in `.env.example`, but **absent from `.env.local`** and **could not be verified on Vercel from this session** — Alexander must confirm both are set on Vercel prod and match the Aegis side. If unset, the helper degrades gracefully (decision still persists; toast says notifications aren't configured).
Verified: independent `npx tsc --noEmit` = **0 errors**; full diffs reviewed. NOT done: live round-trip (approving a real TO fires a real employee notification → would fan out to a real employee; hold per the standing rules — test with the sandbox identity / a Watermark test TO, not a live employee). NOT pushed.

> **Out of this sprint (deliberately):** cross-notify the *other* managers when one acts ("no action needed") — moderate build, near-term fast-follow.

---

## Forward Build Sequence (Phases 1–4)

The post-sprint direction (set 2026-06-09). This is the **north-star sequencing** for everything after the 48-hour sprint; it supersedes the A/B/C option framing in `PRIORITY2_ANALYSIS.md` §5 (those options are now folded into this phased plan). Effort tags: `[S]` small, `[M]` medium, `[L]` large. Lane tags: ⚙️ = fully Cowork-safe-lane (reads / sandbox / branch work); 🔒 = has human-gated steps (main push, prod write, prod env/policy, or messaging real employees). Most items below already exist in the Active backlog or the bug board — the **Note-map** column ties each to its source note and existing item so nothing is duplicated; only genuinely new items are marked **NEW**.

**End-state vision (product north-star — also in doc 01):** Once Phases 1–4 land, Aegis is a genuinely conversational AI assistant manager running the entire employee side of workforce ops over email (SMS once A2P clears) — compliant onboarding, availability, time-off, swaps, emergency coverage, weekly distribution (each employee gets their own shifts plus the full schedule) — in a human-feeling voice, backed by a deterministic engine that builds fair, rule-driven schedules and surfaces real coverage gaps with suggested fixes instead of silently overworking staff. Homebase is the manager command center: data + rules that actually drive the engine (fairness/conflicts/coverage/doubles wired), schedules that persist and download cleanly, one-click TO/availability approval, coverage flags with suggested swaps, and natural-language admin via Soteria. Thesis: config-over-code multi-tenancy (a new client is a data operation, not an engineering project), a deterministic auditable engine, and a flag-don't-force model that keeps humans in final authority while the AI does the legwork — with security solid enough to sell.

### Phase 1 — Harden & fix the live product
| Item | Effort | Lane | Note-map / existing item |
|---|---|---|---|
| **Security audit + hardening** — audit all Homebase `/api/*` for missing auth; wax-seal replay/timestamp window; remove dead IP-allowlist fallback; RLS + secrets review. **`/api/*` auth audit: IN REVIEW** (branch `security/api-auth-audit`, 2026-06-09 — 4 routes guarded; wax-seal/IP-allowlist/secrets still open). **SEC-1 `create-user`: IN REVIEW** (branch `security/create-user-authz`, 2026-06-09 — sign-in + owner/quria-only + role cap + owner company-binding; sole caller is the signed-in Access UI). **Fix reviewed 2026-06-10 — logic sound, uses the correct `'quria'` string, gates before creation; remains IN REVIEW pending live verification (anon=401, manager=403, owner cross-company=403, owner same-company=200 with role cap).** **LOGIC VERIFIED 2026-06-10 via automated test — 22/22 cases PASS** (throwaway branch `test/security-verify`, vitest + mocked Supabase auth/users + captured `users.insert`, no DB/network): the 4 guarded routes (anon=401, own-company=allowed, cross-company=403) and `create-user` (anon=401, no-users-row=403, manager=403, role-cap-above-own=403, owner forced to own company / foreign id ignored, quria may target any company, garbage role=400). The two security branches proven **clean-merge via `git merge-tree`**. **✅ DONE — DEPLOYED TO WATERMARK PRODUCTION 2026-06-10** (both `security/api-auth-audit` (4 route guards) and SEC-1 `create-user` authz merged via PR into Homebase `main` → Vercel; app verified working post-deploy). Still flagged for Alexander (NOT in this deploy): `stripe` billing route, `stripe/webhook` middleware allowlist, RLS defense-in-depth, wax-seal/IP-allowlist/secrets. | [M] | ⚙️/🔒 | Notes 0 + 4. Existing: Tier-2 "Dedicated security track" + Tier-2 "audit Homebase `/api/*`" + the wax-seal/IP-allowlist fast-follows (§6.3 / EMAIL tracker). |
| **Schedule download working** — `xlsx → exceljs` + a PDF that matches the built schedule. **DONE 2026-06-12** — live-verified on real Watermark data. Path: exceljs renderer (`schedule-download-exceljs`) deployed 2026-06-10 → `DOWNLOAD-500` independently fixed at `a3464bc` → **Piece 1 (download template colors)** merged 2026-06-12 (per-day colors match the screen) → **download full-name/role content fix** merged 2026-06-12. Real Watermark download (Excel + PDF) now opens with correct colors AND full employee names + per-assignment role text. Note: the suspected name/role gap is **PRE-EXISTING — NOT caused by Piece 1**; evidence on record is that `git diff 4a813e5^ 4a813e5` touches zero text-path lines, and the first-name-only display + missing per-assignment role predate Piece 1. | [S–M] | ⚙️ | Note 7. Existing: Tier-3 "Schedule download format should match the builder" + Tier-2 "`xlsx → exceljs`". |
| **Email deliverability / DELIV-1** — SPF/DKIM/DMARC + sender warm-up. **Downgraded BLOCKER → MONITOR / client-education (2026-06-12).** The full ~30-person `distribute_schedule` fan-out ran in production 2026-06-12 with no spam problem observed. Kept as a watch item, not a gate. | [M] | 🔒 | (security/deliverability). Existing: Tier-2 DELIV-1 + EMAIL tracker Phase 6.5. |

**Phase 1 — PRODUCTION DEPLOY (2026-06-10).** The first Phase-1 batch was merged into Homebase `main` via **PR** and deployed to **Watermark production** (Vercel). Shipped + verified working: `security/api-auth-audit` (4 route guards) and **SEC-1** `create-user` authz. Also shipped: `schedule-download-exceljs` (exceljs renderer) — **but its styling is not yet observable in production because the schedule download 500s on real data (see `DOWNLOAD-500`)**. Two pre-existing bugs were surfaced post-deploy (both DIAGNOSED, neither caused by the deploy): `DOWNLOAD-500` and `DELETE-USER`.

**OPERATING-MODEL CHANGE (2026-06-10): Homebase `main` is now PROTECTED — changes require a PR, not a direct push.** Future deploy flow = push feature branch → open PR → merge → Vercel auto-deploys. ⚠️ **Doc correction needed:** `05_Development_Guide` §4 (Deployment) still says "push to `main`" — that is now WRONG. Flag for the next reference-doc refresh (NOT edited here — docs 01–06 out of scope this pass).

**`DOWNLOAD-500` — schedule download (Excel AND PDF) 500s on real Watermark data (PRE-EXISTING, NOT the deploy).** Status: **RESOLVED — symptom verified gone in production on real Watermark data 2026-06-12 (download returns 200, opens correctly).** Initial fix landed independently on `origin/main` at `a3464bc fix(download): null-guard buildScheduleGrid + try/catch routes` (the throw was in **shared `buildScheduleGrid`** — the **PDF path uses zero exceljs** yet failed the same way, which proved it was not the `xlsx → exceljs` change; that fix hardened `buildScheduleGrid` with a null-guard on `employee_name` and wrapped the route renders in `try/catch`). The **2026-06-12 download full-name/role content fix** (merged same day) reworked the same suspected `employee_name` path in `buildScheduleGrid` while restoring the missing last name + role text — the two together correlate to the symptom being gone in prod. **Mechanism caveat:** the exact root-cause line for the original 500 was not independently re-confirmed against the post-fix path; recording only that the symptom is gone in prod, not over-claiming which line cured it. **SCHED-EDIT-1 residue hypothesis: RETIRED.** Static end-to-end trace of the SCHED-EDIT-1 persistence path on `origin/main` (2026-06-10) confirms no manual-edit code path produces a null `employee_name` — the move handler preserves `employee_name` from the source assignment; the add-shift handler validates an employee is selected; the gap-fill handler passes the candidate name. SCHED-EDIT-1 itself is DONE on prod (commit `f28cb30 fix(SCHED-EDIT-1): recompute shift times/hours on manual move`, live, re-verified 2026-06-10). (Separately, the exceljs renderer itself was verified robust to every grid shape/content tried — empty, 0-rows/cols, multi/all-closed, control chars/emoji/surrogates — so it was not the culprit.)

**`DELETE-USER` — old users can't be deleted from the Access page (PRE-EXISTING, NOT the deploy).** Status: **DIAGNOSED, lower priority.** `handleRevoke` (Access page, untouched by any Phase-1 branch) uses the **anon/browser client**, **ignores the returned error**, and deletes only `public.users`. New users delete fine (no linked rows); **old users are blocked by FK constraints** referencing `users.id` — notably `schedules.generated_by` (NOT NULL), `time_off_requests.decided_by`, and `activity_log` — so with the default `ON DELETE NO ACTION/RESTRICT` the delete fails and, because the error is swallowed, the UI silently appears to do nothing. (Secondary: even a successful delete leaves the `auth.users` row — no `auth.admin.deleteUser` — so the email can't be re-added.) Proposed fix: a **server route (service-role)** that surfaces the error and handles linked records deliberately — **product decision needed: soft-delete (deactivate/revoke) vs. reassign** the NOT-NULL `schedules.generated_by` (and `SET NULL` the nullable refs) — plus `auth.admin.deleteUser` to clear the auth row. At minimum, surface the delete error in the UI.

**Phase 1 — Security findings (SEC-1..SEC-4, 2026-06-09).** Surfaced by the `/api/*` auth audit (branch `security/api-auth-audit`, Homebase; full per-endpoint table in `SECURITY_AUDIT_API.md` on that branch). Status meanings on this list: `IN REVIEW` = branch committed + compiles, NOT live-verified; `FIXING` = branch in flight; `DIAGNOSED` = root cause confirmed, fix not yet built; `DECIDED` = product call captured, code may follow. **UPDATE 2026-06-10: SEC-1 is now DONE (deployed to Watermark prod via PR). SEC-3 fix merged into Homebase `main` and deploying — LIVE-VERIFY PENDING (Stripe webhook 200s + `STRIPE_WEBHOOK_SECRET` on Vercel prod); NOT DONE until both clear. SEC-4 audited — VERIFIED no security defect (256-bit CSPRNG + atomic TTL/single-use enforcement); the 72h default TTL is an OPEN product decision, not a defect. SEC-2 unchanged.**

- **SEC-1 — `create-user` route authz (HIGH).** Status: **IN REVIEW** (branch `security/create-user-authz`, Homebase, `tsc` clean, NOT live-verified). Access model **DECIDED 2026-06-09**: `owner` may create users **only within own company**; `quria_admin` may create cross-company; `manager` may not create users; the new user's role is **capped at the creator's privilege** (`quria_admin` > `owner` > `manager`). Implementation on branch: require sign-in; role gate (`owner`/`quria_admin` only); role cap; `owner` forced to own `company_id` (body `company_id` ignored), `quria_admin` may target any. (Spec's `quria_admin` = live role `quria` in `users.role`.) Sole caller is the signed-in Access UI — sign-in gate breaks no automated path. Verify live: anon → 401; manager → 403; owner cross-company create attempt → 403; owner same-company → 200 with role cap applied.
- **SEC-2 — Stripe billing route authz (MED).** Status: **NOT STARTED** (decision-gated). The `/api/stripe` billing route (checkout / portal / customer creation) is currently unscoped; access model has not been decided. Likely shape: **owner-only + company-bound** (parallel to SEC-1's owner-binding), but needs Alexander's call before code lands. No branch yet.
- **SEC-3 — Stripe webhook not in middleware `isPublic` allowlist (FUNCTIONAL, not auth).** Status: **fix merged (Homebase) + deploying — LIVE-VERIFY PENDING. NOT DONE until live-verified.** The `stripe/webhook` route uses correct Stripe-signature authentication, but the Homebase middleware did NOT include `/api/stripe/webhook` in the `isPublic` allowlist, so the unauthenticated POST from Stripe was 307-redirected to `/login` and the billing event silently never processed. **Fix landed 2026-06-10** (Homebase branch `security/webhook-ispublic-authz`, commit `9bb092d`): one-line `pathname === '/api/stripe/webhook'` EXACT match added to `isPublic` (NOT a prefix, so the SEC-2 billing route `/api/stripe` stays middleware-gated). Verified off-prod via 5-row curl matrix — no-signature POST → 400 "Missing stripe-signature header" (was 307 pre-fix); invalid-signature POST → 400 (constructEvent rejects); GET `/api/stripe` → 307 (SEC-2 still gated); GET `/api/stripe/anything-else` → 307 (no prefix spill); GET `/` → 307 (no over-broadening). PR merged into Homebase `main` and deploying to Vercel. **Remaining gates to DONE:** (a) confirm `STRIPE_WEBHOOK_SECRET` is set on Vercel prod; (b) live-verify on the Stripe dashboard that recent events 200 instead of 307. Until both clear, this is IN REVIEW / live-verify pending — **not** DONE.
- **SEC-4 — `aegis_action_tokens` TTL / entropy (LOW).** Status: **VERIFIED — no security defect; TTL value is an OPEN product decision.** Read-only audit completed 2026-06-10 across both repos (Aegis `src/lib/aegis-actions/tokens.ts` for generation; Homebase `src/lib/aegis-actions/tokens.ts` + `src/app/api/aegis-action/route.ts` for verify+consume). **Entropy:** `crypto.randomBytes(32)` → 256 bits CSPRNG, base64url-encoded, SHA-256 hash-stored (plaintext NEVER persisted). ✓ **TTL:** explicit `expires_at` on every row; default `DEFAULT_TTL_MINUTES = 72*60 = 4320` (72h); every production caller (Aegis `schedule-build-email.ts:484`, `time-off-manager-email.ts:411,419`) explicitly passes 4320. ✓ **Enforcement:** TTL checked at read-time (`verifyToken` Homebase tokens.ts:67-69 → `'expired'`) AND atomically at consume-time (`consumeToken` tokens.ts:87 — `.gt('expires_at', nowIso)` is a WHERE-clause predicate of the same UPDATE that flips `consumed_at`). Single-use enforced in the same atomic UPDATE (`.is('consumed_at', null)`). On-success `activity_log` row written. ✓ **OPEN — product decision only (NOT a security defect):** is 72h the right TTL value? Informal recommendation: tighten to **24h for TO/availability magic-links** (1440 min) for tighter blast radius; keep **72h** (or bump to 120h) for `confirm_distribution` because managers may queue Thursday-for-next-week. Implementation when decided: one-line change in Aegis `src/lib/aegis-actions/tokens.ts:9` to `DEFAULT_TTL_MINUTES`, plus optional per-call overrides at the 3 call sites. Cross-repo coordination NOT needed — Homebase verification already enforces whatever `expires_at` sits on the row.

### Phase 2 — Complete the comms loop
| Item | Effort | Lane | Note-map / existing item |
|---|---|---|---|
| **All email workflows up & verified** — finish + test employee (swap, emergency coverage, query), manager (beyond `build_schedule`), and onboarding. | [L] | 🔒 | Note 9a. Existing: EMAIL tracker Phase 7 (risky fan-outs) + all TODO/UNTESTED intents (now tagged Phase 2 in that tracker). |
| **Two deliverables on distribute** — per-employee shifts message + a full-schedule email. | [M] | 🔒 | Note 2. **NEW** (full-schedule email is new; per-employee shifts already exist in `distribute_schedule`). |
| **Availability approval in Homebase** — magic-link buttons + Homebase backstop, mirroring TO. | [M] | ⚙️ | Note 10. Existing: Tier-1 "Availability approval buttons (mirror TO magic-link) + Homebase backstop". |
| **Route notify-assignment through Aegis** — kill the Homebase→Twilio direct path; all employee comms go through Aegis compliance/opt-in. | [M] | ⚙️ | Note 1. Existing: Tier-2 "Homebase `notify-assignment` should route through Aegis". |

### Phase 3 — Configurable, correct rules
| Item | Effort | Lane | Note-map / existing item |
|---|---|---|---|
| **Rules actually apply** — wire `conflict_resolution_preference`, fairness weight, doubles emergency handling. | [M] | ⚙️ | Note 6. Existing: §6.4 "`conflict_resolution_preference` wiring" + doc 06 §9 banked items (`doubles_policy='emergency_only'` behaves like `never`). |
| **Role Groups** — `accepted_roles` eligibility + role-preference rule; engine before UI. **Engine: DRAFT** (branch `role-groups-engine`, 2026-06-09 — eligibility + `resolveAssignedRole` + smoke tests green; `db/types.ts` field optional pending fixture migration; NO UI; live-verify + seeding pending). | [L] | ⚙️ | (existing high-pri). Existing: Tier-2 "Role Groups". Also the structural resolution path for ENGINE-1's Junior-Lifeguard 0h miss. |
| **Rules-tab UI + configurable rules** — TO-rules-as-policy, rule/attribute create-edit UI. | [L] | ⚙️ | Note 5. Existing: Tier-2 "TO-rules-as-policy program" (rule/attribute create+edit UI) + doc 03/06 "Rules-tab UI build-out" open item. |
| **Coverage-flag resolver** — manager-assisted swap suggestions. | [M] | ⚙️ | Note 8. Existing: Tier-2 "Coverage-flag resolver (engine helper + Homebase UI)". |
| **Decide the inert per-shift swap** — keep-as-capability-+-guardrail vs remove (`enforceAttributeMixForShift`). | [S] | ⚙️ | (this session's logged decision). Existing: Tier-2 "Decide the fate of the retired per-shift attribute-mix swap". |

### Phase 4 — Experience & leverage
| Item | Effort | Lane | Note-map / existing item |
|---|---|---|---|
| **Aegis personability pass** — voice across every Aegis string. | [M] | ⚙️ | Note 3. Existing: the "feels like a person" standing principle + Tier-1 tone-pass note. |
| **Soteria fully operational** — NL control of all Homebase + schedule editing. | [L] | ⚙️ | Note 11. Existing: Tier-2 "Soteria fully operational". |
| **User guides** — two deliverables per user type for Watermark to hand staff. | [S] | ⚙️ | Note 9b. **NEW** (user-guide deliverables not previously tracked). |

**Source-note ledger (the 11 notes → where they live):** 0/4 → Phase 1 security; 1 → Phase 2 notify-assignment-through-Aegis; 2 → Phase 2 two-deliverables (full-schedule email NEW); 3 → Phase 4 personability; 5 → Phase 3 Rules-tab/configurable rules; 6 → Phase 3 rules-actually-apply; 7 → Phase 1 schedule-download; 8 → Phase 3 coverage-flag resolver; 9a → Phase 2 all-workflows-via-email, 9b → Phase 4 user guides (NEW); 10 → Phase 2 availability approval in Homebase; 11 → Phase 4 Soteria. Genuinely new vs the pre-existing backlog: **note 2's full-schedule email** and **note 9b's user guides**; everything else dedupes onto an item already tracked above.

---

## Active backlog

> **Phase tags** below (`[P1]`–`[P4]`) map each backlog item onto the Forward Build Sequence above. Untagged items are launch-cleanup / polish that sit outside the four-phase arc.

### Tier 1 — near-term fast-follows
- `[P2]` Cross-notify managers on TO/availability action ("no action needed").
- **Access page: can't revoke Homebase access for Bubba** — fix; then execute launch cleanup (remove Bubba's manager row, `aegisscheduler` test employee, stray pending test TO, sandbox/test activity).
- `[P2]` Availability approval **buttons** (mirror TO magic-link) + Homebase backstop — also the fix for "communications feel robotic / yes-no reply bs" (do a tone pass in the same pass — `[P4]` personability).
- Undo action button.
- Expand doc 03's Access Management section (docs gap).
- **`getWeekBounds` is NOT company-timezone-aware** — uses server-local `new Date()` (UTC on Railway), so this/next-week resolution can be wrong near week boundaries for non-UTC tenants (Watermark = `America/Detroit`). Affects `build_schedule` AND `distribute_schedule`. `06_Supplemental` §8 claims week bounds are "computed in local tz" — code contradicts the doc. Fix: resolve "today" via `Intl.DateTimeFormat('en-CA', { timeZone: companyTimezone })`; flag 06 §8 for correction at next doc refresh.

### Tier 2 — significant builds (contract-first: engine/parser before UI)
- `[P3]` **TO-rules-as-policy program** (one program): move TO rules into the same `policy_value_json`/constraint-vocabulary system the schedule engine uses; attribute classifier so workflows know what to pull; Rules/Attribute creation+edit UI that updates everywhere; Soteria + Aegis can read/write. Includes the "new UI and engine for TO rule policies" and "attribute edit/creation page" notes. (Note 5.)
- `[P3]` **Role Groups** — `shift_requirements.accepted_roles` (exists, NOT read yet); structural fix for Headguard coverage gaps **and** the resolution path for ENGINE-1's Junior-Lifeguard structural miss (4 employees with 0h because no JL slots exist). Engine eligibility before UI. (Distinct from ENGINE-1 as a bug class: ENGINE-1 is `DIAGNOSED` and the JL portion is structurally owned here.)
- `[P3]` **Coverage-flag resolver (engine helper + Homebase UI)** — when a schedule carries an unsatisfied_sex_coverage flag (all slots filled, but the concurrent-coverage mix is unmet — e.g. no male guard 11:00-21:15), Homebase displays the flag but offers no way to act on it. Build a manager-assisted resolver, analogous to the gap resolver: an engine helper computes candidate swaps that would satisfy the flagged window without creating a new gap/conflict/coverage hole; Homebase surfaces them as suggestions; the manager applies one via the manual-edit path. Manager-driven, NOT an automatic swap — the assisted version of the per-shift swap retired in ENGINE-2 (preserves flag-don't-force + config-over-code). Two stages: (1) read-only — name the missing sex and list qualified, available employees who could cover; (2) one-click apply-a-swap. Dependencies: gate on SCHED-EDIT-1 verified; needs an engine swap-suggestion helper (shares eligibility/ranking infra with the gap resolver and the retired attribute-mix logic). Sequence after SCHED-EDIT-1; overlaps with gap-resolver / Role Groups work.
- `[P3]` **Decide the fate of the retired per-shift attribute-mix swap (`enforceAttributeMixForShift`, schedule-build.ts:707).** ENGINE-2 retired the per-shift gender swap by *configuration* — flipping Watermark's policy from attribute_mix → concurrent_coverage — not by removing code. The swap (and its `unsatisfied_attribute_mix` flag) still exists and still fires for any policy with an attribute_mix-shape policy_value_json. Inert for Watermark today (the flipped policy feeds the concurrent_coverage path, so hard.attributeMix is empty — confirmed by the 6/15 build: flat hours, only sex_coverage flags). Footgun: re-adding an attribute_mix-shape policy to Watermark would resurrect the displacing bimodal-hours behavior. Decide: (a) KEEP it as a generic multi-tenant capability + add a guardrail so Watermark can't accidentally acquire an attribute_mix policy; or (b) REMOVE the per-shift swap if committing to concurrent_coverage as the only sex/attribute model. Leaning (a) on config-over-code grounds; needs an explicit call. Low urgency (inert today); logged so it isn't forgotten.
- `[P3]` **Per-org shift jargon tags (natural-language shift aliases).** Today the availability parser maps "mornings/afternoons/evenings" to a GENERAL clock meaning (morning = open–noon, etc., clamped to company hours). That's a safe default but not what work jargon means per client — at Watermark "mornings" colloquially = the AM shift (to ~3:30), not literal noon. Build a per-organization tag system so each client defines its own jargon instead of hardcoding Watermark's into every future client. Three pieces: (1) **schema** — add a natural-language alias field to `shift_types` (e.g. `nl_aliases text[]`); this is a **gated migration** (Alexander applies to prod, per the safety rule). (2) **Homebase** — a toggleable editor (on the Shift Types screen or the Template editor — UNDECIDED) where managers add/edit aliases per shift, e.g. AM Weekday → `am, morning, mornings`. (3) **Aegis** — the availability parser checks the org's shift aliases FIRST and resolves the jargon to that shift's actual times, falling back to the general clock meaning only when no alias matches. **Deferred 2026-06-13 at Alexander's direction — it distracts from the email-workflow North Star; revisit after the email workflows are proven.** (Surfaced while building date-limited custom-availability-by-email.)
- `[P2 — after the email workflows are polished]` **Finish the Access Management page (Homebase).** Make it work properly and feel intuitive: as `quria`, delete/revoke ANY person's access regardless of role; managers can manage their own employees' access; and a general UX pass (the page needs to be clearer overall). Builds on / subsumes the **DELETE-USER** bug (Phase 1 — `handleRevoke` uses the anon client, swallows the error, and FK constraints block deleting old users; needs a service-role route + a soft-delete-vs-reassign decision + `auth.admin.deleteUser`). Deferred until the email workflows are proven, per Alexander (2026-06-13).
- `[P4]` **Soteria fully operational** — natural-language control of all of Homebase + can edit the schedule. (Note 11.)
- `[DONE 2026-06-13]` **Emergency coverage — write the accepted shift into the schedule.** Built: on accept, Aegis swaps the coverer in for the absent employee on the matching shift in `schedules.data` (best-effort `swapScheduleAssignment`/`applyCoverageToSchedule`; if it can't find the shift it tells the manager to adjust in Homebase). Verified in the sandbox DB (PM Lifeguard 6/13 → Shmubba). Accepted coverage now flows onto the schedule and planned hours follow. **Remaining payroll nuance (still `[P2]`):** the schedule holds PLANNED hours only; real worked/clocked hours for true payroll are future work, gated on the same payroll-data track as the pay-inquiry item below.
- `[DONE 2026-06-13]` **Emergency coverage fan-out — parallel blast + manager-gated batches.** Replaced sequential one-at-a-time outreach. Manager picks **all** (`isContactAll`) or specific names → Aegis blasts that whole group in parallel; first YES wins (existing race/lock logic). If the group lapses (`NEXT_BATCH_PROMPT_MINUTES`, currently 30) or all decline, Aegis asks the manager whether to send another batch — **never auto-sends** (`promptForNextBatchOrExhaust`); manager YES → `blastNextBatch`. Touches `emergency-coverage.ts`, `coverage-timeout.ts` (timer now nudges the manager instead of advancing per-person), `intent-router.ts` (routes the `awaiting_next_batch_decision` reply).
- `[P3 — config]` **Make the coverage escalation timer a per-company Homebase setting.** `NEXT_BATCH_PROMPT_MINUTES` (the "ask the manager about another batch after N minutes" window, default 30) is a hardcoded constant in `emergency-coverage.ts`. Long-term it should be a per-org configurable setting in Homebase (alongside other coverage rules) so each client can tune urgency. Captured 2026-06-13 at Alexander's direction while building the fan-out redesign.
- `[P2]` **Pay / labor-cost questions via the inquiry system (payroll-grade).** Employees asking "how much am I making this week (pretax)?" and managers asking about spending / labor cost. The inquiry pipeline is general enough to attempt these, but pay is sensitive and the schedule only holds PLANNED hours, so anything now is a rough "scheduled hours × wage" ESTIMATE, not real pay. Two tiers: (a) near-term — a clearly-labeled pretax ESTIMATE (employee: own scheduled hours × own wage; manager: planned labor cost), only ever using the asker's own wage for employees; (b) future — real pay stubs + real labor cost, which require actual worked/clocked hours (gated on the same payroll-data work as the coverage→payroll gap above). Deferred 2026-06-13 at Alexander's direction. Built the rough-estimate tier only when payroll becomes the focus.
- `[P3]` **Manual builder recommends employees with engine-level efficacy** — surface the engine ranking in the manual builder.
- `[P1]` **Dedicated security track** (for client acquisition) — `/api/*` auth audit, wax-seal replay/timestamp window, RLS review, secrets hygiene, remove dead IP-allowlist fallback. (Notes 0 + 4.)
- `[P1]` **DELIV-1** — SPF/DKIM/DMARC + sender warm-up. **Downgraded BLOCKER → MONITOR / client-education (2026-06-12)**: the 30-person `distribute_schedule` fan-out ran in production 2026-06-12 with no spam problem observed. Kept as a watch item, not a gate.
- `[P2]` **Two deliverables on distribute** — per-employee shifts message **plus a full-schedule email** (the full-schedule email was the new piece). **Largely DONE (2026-06-12):** `distribute_schedule` fan-out executed successfully in production 2026-06-12 to the full Watermark roster with the warm per-employee shift-assignment message AND the inline full-week schedule. Only remaining piece: a **visual-consistency fix** so the emailed schedule matches the Homebase render exactly — that work is captured under template-unification **Piece 3** in this section. (Note 2.)
- `[P2]` **Route Homebase `notify-assignment` through Aegis** — kill the direct Homebase→Twilio path so all employee comms pass Aegis compliance/opt-in. (Note 1. Also listed in the Tier-2 EMAIL-tracker carry-over below.)
- `[P4]` **User guides** — two deliverables per user type (manager + employee) for Watermark to hand staff. (Note 9b — new.)
- **Unified schedule rendering — template as single source of truth (contract-first, cross-repo).** The schedule TEMPLATE (`schedule_templates`, edited in `TemplateEditorPanel`) must be the single source of truth for every surface that renders a schedule: the on-screen Homebase render, the Excel/PDF download, and the Aegis distribute email. Today they diverge. Build contract-first; the "resolved cell appearance" (template-first → role-color fallback) is the contract.
  - **Piece 1 (Homebase) — `DONE` (merged 2026-06-12 to Homebase `main`, branch tip `4a813e5`; deployed to Watermark prod via Vercel; all-blue download fix confirmed live — downloads now render per-day colors matching the screen).** The separate **download full-name/role content fix** — restoring the missing last name + per-assignment role text — was a **pre-existing content gap, NOT caused by Piece 1** (evidence on record: `git diff 4a813e5^ 4a813e5` touches zero text-path lines; first-name-only display + no per-assignment role predate Piece 1). That fix was scoped and merged separately on 2026-06-12; the live download is now correct on color AND content. Extracted cell-appearance resolution into ONE shared **pure** resolver (`src/lib/schedule/resolveCellAppearance.ts`) used by the on-screen render (`ScheduleRenderer` — pure refactor, rendered output unchanged) AND both download renderers (`renderScheduleGridXlsx` + `renderScheduleGridHtml`); deleted the forked kind-only color palettes that produced the all-blue download. **Contract — Pieces 2/3 consume this, recorded verbatim from `resolveCellAppearance.ts`:**

    ```ts
    resolveCellAppearance(input: {
      colorConfig: ColorConfig    // template.color_config
      columnColor: string         // the day column's hex
      rowId: string               // shift row id (used only when by:'shift')
      kind: CellKind              // empty | filled | partial | gap | closed
    }): CellAppearance

    interface CellAppearance {
      color: string       // resolved template color '#RRGGBB'
      kind: CellKind
      background: string  // on-screen CSS (theme token / rgba / 8-digit hex) — exact UI string
      fill: string        // opaque '#RRGGBB' for Excel/PDF export
      accent: string      // role-label text color (= color)
    }
    ```

    **Semantics:** role-FREE, template/day-color driven — mirrors on-screen `getColor` exactly: `by:'day'|'role'|'none'` → column color; `by:'shift'` → `map[rowId] ?? columnColor`. **Contract correction:** the initiative line above frames the contract as "template-first → role-color fallback"; the Piece-1 diagnosis found there is NO role-color path on any surface — the on-screen grid already colors from the TEMPLATE (day/shift), live not dormant — so the real contract is template-driven with no role fallback. (Surfaced to Alexander before building; role-free contract chosen.) **Piece 2 coupling:** the resolver hard-reads `color_config` + `column_config[].color`, so when `saveTemplate` is fixed (Piece 2) it must persist exactly that shape; a `color_config.by:'shift'` map is what activates the currently-dormant per-shift override path (covered by a direct resolver test). Its output IS the contract.
  - **Piece 2 (Homebase):** fix the `saveTemplate` bug (`TemplateEditorPanel`) and wire the saved template into the shared resolver so editing the template visibly changes the on-screen schedule (keystone).
  - **Piece 3 (cross-repo):** snapshot the resolved appearance onto the schedule at build/save (`schedules.data` or a `template_snapshot` field); Aegis distribute renders its grid from that snapshot — no Homebase call at fan-out, no forked renderer. Same contract, persisted.
  Order 1→2→3. Subsumes the Tier-3 "`saveTemplate` bug" and "download should match the builder" items.

### Tier 3 — polish / smaller fixes
- **Download full-name + role content gap (was logged "Piece 1 follow-up" — reframed 2026-06-12).** Status: **DONE (live-verified on Watermark 2026-06-12).** **Reframing:** this was originally logged as a Piece 1 regression. On closer look it is a **PRE-EXISTING download content gap**, NOT caused by Piece 1 — evidence on record: `git diff 4a813e5^ 4a813e5` (the Piece 1 commit) touches **zero** text-path lines, and the first-name-only display + missing per-assignment role text **predate Piece 1**. **Original symptom:** the schedule download (Excel + PDF) was missing every employee's **last name** and **role** text. On-screen render was fully correct (first + last + role + colors all present); download **colors** were correct after Piece 1. **Resolution:** a content fix in `buildScheduleGrid` / the download renderers (`renderScheduleGridXlsx` + `renderScheduleGridHtml`) restored the missing last name + per-assignment role text; merged 2026-06-12 to Homebase `main` and deployed to Watermark prod. Real Watermark download verified live 2026-06-12: full names + roles render in both Excel and PDF. (Does NOT preempt Piece 2 — Piece 2 may resume.)
- `saveTemplate id:''` bug in `TemplateEditorPanel` (before any client edits a template).
- **TEMPLATE-EDIT-1 — Schedule template edits don't work / won't save; the template builder needs more custom build-out.** Symptom (on-site): editing a schedule template doesn't take effect. Possible shared root cause with SCHED-EDIT-1 (manual schedule edits don't persist) — diagnosis should check whether both hit the same edit-persistence path (UI state → API → DB write) or are distinct (e.g. template UI state mutation never sent to the API vs. API received but DB write dropped). Candidate approach to validate (NOT committed): scaffold the template editor from an existing/old template as a working sample to build from. Status: **OPEN, not started — address on-site. Diagnose-first** (no blind fixes). Likely related to the existing `saveTemplate id:''` Tier-3 entry above and worth checking together. (Logged 2026-06-10.) **2026-06-10 update:** overlap check with SCHED-EDIT-1 = DISTINCT path (schedule edits → `schedules.data` JSONB via `ScheduleReviewPanel`; template edits → `schedule_templates` table via `useScheduleTemplate()` hook). Strongest behavioral lead: silently-swallowed save error at `useScheduleTemplate.ts:67` (`if (!error && data) { setTemplate(data) }` — error branch does NOTHING; panel closes regardless). Secondary lead: `.upsert(..., { onConflict: 'company_id' })` requires a UNIQUE constraint on `schedule_templates.company_id` — verify in prod before any fix (see `SCHEMA_DRIFT_LOG.md` 2026-06-10).
- Hour rounding in the schedule tab for contributors.
- `[P1]` Schedule download format should match the schedule builder (ties to `xlsx → exceljs` + matching PDF — Note 7).
- Orange glow around each rule (Rules tab UI).
- `[P1]` **Homebase `src/lib/getCompanyServer.ts` appears to be a near-duplicate of `src/middleware.ts`** (security-adjacent, low priority). Next.js only loads middleware from `src/middleware.ts`, so this file's `middleware`/`config` export is likely dead code (or wired in via an untraced import). Trace its references and either remove it or wire it intentionally — a stray second copy of the auth gate is a drift/confusion risk. (Surfaced 2026-06-10 during the SEC-1 review.)
- `[P1 — do immediately after the email workflows are tested]` **Quria-only Activity Log delete button (Homebase).** Alexander wants to delete activity-log rows himself (as `quria`) to clean up launch/test clutter — the fake-employee + fake-manager test entries with the unprofessional email address. Small build: a `quria`-gated **server route** (service-role; mirror the SEC-1 / SCHED-DELETE-1 authz pattern — sign-in → role gate `quria` only) that deletes (or soft-hides) the selected `activity_log` row(s), plus a delete control + confirm step on the Activity page (destructive action). Build now only if genuinely quick; otherwise this is the first thing after the workflows. (Still also wanted: quria-only delete old schedules.)
- **Strip committed `node_modules` from Aegis history** (~142MB across 2 historical commits — surfaced during the 2026-06-09 secret-scrub audit). Confirm `node_modules` is in `.gitignore`. Safe to force-push the rewrite later (solo repo).
- ⚠️ **`test/security-verify` is a THROWAWAY verification branch — do NOT merge it.** It assembles the two security branches' route files + adds a temporary vitest harness purely to run the authz test (2026-06-10). Merge the two **original** branches instead — `security/api-auth-audit` and `security/create-user-authz`.
- `[P1]` **Homebase has NO test runner** (no vitest/jest, zero tests) — surfaced 2026-06-10 during the SEC-1 authz verification, which had to add vitest + the first auth test on the throwaway `test/security-verify` (22 cases, all green). **Adopt a real test setup in Homebase** (vitest config + `test` script as a committed dev dependency) and **port that auth test over** (`src/app/api/__tests__/security-authz.test.ts`) so the coverage isn't lost when the throwaway branch is discarded.
- **`ShiftMeta.days_active` type is `string[]` but the live DB column is `number[]`** (and the docs describe it as `number[]`). Surfaced 2026-06-10. This is a **code type bug, not DB drift** — the column is correct; the TS type lies. Fix: change the declared type to `number[]` so consumers stop coercing. (Not logged in `SCHEMA_DRIFT_LOG.md` per its own rule — append-only DB drift log, not for code type bugs.)
- **`scripts/test-cascade.ts` is broken on `main`** (Aegis) — references the removed field `WeekState.assignmentsByDate` (×9) and an old `ResolverDeps` shape (missing `settings`). Stale harness, surfaced during the 2026-06-09 Role-Groups draft work; unrelated to that work. The canonical engine test is `src/lib/engine/__tests__/smoke.ts` (passes). Either update the harness against the current engine surface or delete it.
- **CASCADE-1 (future improvement) — refine `cascade.ts:legalToPlace` to take an optional second-ignore target.** Today the function hides only one row in its `viewState` (the row at `ignoreAssignmentIndex`), so on the moverEmp leg of a banned-pair swap, the PARTNER row stays in the mover's worked set and the consecutive-day cap check over-predicts the run length. Safe direction (over-reject, never under-reject) but it can cost a fill when a tenant has BOTH a `max_consecutive_days_worked` cap AND hard banned-pairs. Deferred because it's a signature change that ripples through every caller of `legalToPlace`. The fixture-(c) cascade test in `src/lib/engine/__tests__/max-consecutive-days.ts` deliberately exploits the current conservatism to prove the cap-check fires; rationale + the verbatim trace are in `BUILD_NOTES.md` ("Enforcement-gap fix (2026-06-10)" → fixture (c)). (Surfaced 2026-06-10 during the consecutive-days enforcement-gap fix.)
- **CASCADE-2 (pre-existing bug, NOT introduced by `feature/max-consecutive-days`) — cascade resolver can accept moves that double-book at the apply step.** `cascade.ts:legalToPlace` accepts a swap as legal even when applying its `SwapOperation` would put the same employee on two slots of the same `(date, shift_name)` instance — because the legality checks evaluate each leg in isolation against a partial `viewState`, not against the post-swap state. Reproducible by hand with a small fixture; surfaced (but not pursued) during the consecutive-days assignment-path audit. Orthogonal to the consecutive-days cap fix; that code path was unchanged on this branch (cross-ref `BUILD_NOTES.md` "What I did not do"). Cascade fires only when `slotEligible` is empty AND a `blockedByConflictOnly` candidate exists AND a swap chain is found, which requires hard banned-pair conflicts configured on the tenant — Watermark may not have any, so verify scope before scheduling. (Surfaced 2026-06-10 during the consecutive-days enforcement-gap fix.)
- **SANDBOX-SEED-1 — sandbox needs at least one active `shift_types` row for real-tenant engine verification.** The sandbox has 1 `shift_requirements` orphan-without-parent + 3 employees, but **0 active `shift_types`**. With no shift_types, `buildCanvas` produces no slots → `runScheduleBuild` returns `totalRequired=0` → ANY engine-side constraint (cap, attribute-mix, concurrent-coverage, doubles policy, etc.) is unobservable through the production code path on real DB data. Surfaced 2026-06-10 during the `max_consecutive_days_worked` sandbox dry-run — the DB→parser chain was provable end-to-end, but enforcement on assignments could not be. **Action:** seed one minimal PM Lifeguard shift_type (active=true) on sandbox to back the existing PM Lifeguard `shift_requirements` row (15:00-21:00 all days, accepted_roles=ARRAY['Lifeguard'], per TEST_IDENTITIES seed list), then verify a build produces non-zero slots. Once seeded, this also unblocks live verification of any future engine constraint without re-seeding. (Logged 2026-06-10.)
- **POLICY-JSON-SHAPE-1 — pick + document a canonical `policy_value_json` shape for scheduling-engine policies before the Phase-3 Rules-tab UI writes any.** The engine parser (`src/lib/constraints/parser.ts`) reads ONLY `policy_value_json`; the text `policy_value` column is ignored (parser.ts:197 — see SCHEMA_DRIFT_LOG 2026-06-10 entry). The existing TO policy rows on sandbox (`max_consecutive_days_off`, `min_notice_period_days`) use text `policy_value` with `policy_value_json = null` because they're consumed by a SEPARATE loader (`src/lib/time-off-policies.ts`). `parseIntegerInRange` / `parseNumberInRange` accept either a bare number or `{ value: N }` — fine for the parser, but the Rules UI / Soteria should write one canonical shape per policy_key family so we don't accumulate two-of-everything. Recommendation (informal): bare value for scalar policies (number/boolean/string); object for structured policies (`attribute_mix`, `concurrent_coverage`). Sits with the [P3] **TO-rules-as-policy program** in Tier 2 — flag here so it isn't lost when that program kicks off. (Logged 2026-06-10.)
- **Reconcile local Homebase `main` to `origin/main`** (one-off, post-PR-merge housekeeping). After the 2026-06-10 Phase-1 PR merge, the laptop's local `main` lags `origin/main` until you `git checkout main && git pull` — normal for the new protected-branch flow, but logged so it isn't mistaken for in-flight work. Re-run after each merge so the next branch starts from the right base.
- **Homebase `CLAUDE.md` needs the same Deploy-flow + Remote Control note** added here (Aegis `CLAUDE.md` was updated 2026-06-10; Homebase is a separate repo, not edited from this one). One-liner to apply over there next time you're in `~/Desktop/homebase`.
- **Reference-doc refresh** — **DONE 2026-06-09** (this doc-alignment pass). Reconciled docs 01–06 + trackers to post-sprint reality and the Phase 1–4 direction: gender rule corrected from dormant per-shift `attribute_mix` to live `sex_coverage` (scope=`concurrent_coverage`); `FlaggedIssue` documented as the discriminated union (incl. `unsatisfied_sex_coverage`); the two coexisting `schedules.data` flag formats noted; the ruled-out ENGINE-1 hypothesis replaced with the real cause (availability precision + the JL structural miss); doc 03 §7 corrected (Homebase types live in `src/lib/types.ts`; `src/db/types.ts` is an Aegis path); SCHED-EDIT-1 + in-tab `decided_by` marked resolved; CoverageFlags + its three mount views documented; Phase 1–4 sequence added to docs 05/01. (Original items surfaced in `PRIORITY2_ANALYSIS.md` §3 / SCHEMA_DRIFT_LOG.)

### Business / ops (non-dev — tracked, not on the build timeline)
- Fix Quria landing page: add SME AI-integration consulting service line.
- Travis Stoliker follow-up.

---

## Standing principles (do not violate)
- **No orphan outputs:** every AI change (Aegis, Soteria, engine) lands as valid, visible Homebase state within the constraints.
- **Diagnose before fixing.** Read the actual diff before any push. Don't trust "tsc clean" without seeing it.
- Verify column names against `information_schema` before any write (`src/db/types.ts` is incomplete — see `SCHEMA_DRIFT_LOG.md`).
- Classifier prompts inject today's date (timezone-aware). Employee emails never link to Homebase. One Soteria action per response. The schedule build is LLM-free.
- "Feels like a person" tone on every Aegis string.
- Never distribute a real Watermark schedule without manager coordination (Carolyn, Jack) — and not while SCHED-EDIT-1 is open.
- **Configuration over code:** the engine/platform is generic and multi-tenant; client behavior is driven by their Supabase data + the constraint vocabulary, never by client-specific code. Accommodating a client is a data/config operation, not an engine change. Per-client rules are toggleable (e.g. sex_coverage on/off). If a client needs something the vocabulary can't express, that's a product conversation — never a quiet engine patch.

## Reference map
- Stable reference: the six `.docx` (01 Business, 02 Schema, 03 Homebase, 04 Aegis, 05 Dev Guide, 06 Supplemental).
- Live trackers: this file (sprint/progress), `EMAIL_WORKFLOWS_TRACKER.md` (email workflows), `SCHEMA_DRIFT_LOG.md` (append-only drift), `TEST_IDENTITIES.md` (tenants/identities).

---

## SESSION LOG (append-only)

### 2026-06-08 — Doc refresh + sprint definition
- Rewrote all six `.docx` reference docs to v3.0 (Engine V2, post-launch reality, resolved schema drift). Updated `SCHEMA_DRIFT_LOG.md`, `TEST_IDENTITIES.md`, `EMAIL_WORKFLOWS_TRACKER.md`.
- Logged two live bugs: ENGINE-1 (engine skips eligible employees) and SCHED-EDIT-1 (manual edits don't persist).
- Defined the 48-hour sprint: S1 ENGINE-1, S2 SCHED-EDIT-1, S3 manual-approval-notify. All diagnose-first.
- **Next session:** run the three diagnostic prompts (S1 dry-run + disposition dump in Aegis; S2 save-handler read in Homebase; S3 approve-path read). Fill in each item's Diagnosis section here before writing any fix.

### 2026-06-08 (cont.) — availability fix + fairness question
- Confirmed Erin's availability ended 21:00 vs Afternoon 21:15; extended availability (data fix), verified.
- Post-fix sample still schedules Erin once; repeat names observed. Opened ENGINE-2 (S1b) as DIAGNOSING — suspected hours-fairness not distributing load, four competing hypotheses, instrumented dry-run pending.
- Corrected agent error: AManager is a real role, not a typo (no cleanup, no drift entry).

### 2026-06-08 (cont. 2) — ENGINE-2 root cause + S3 diagnosed
- ENGINE-2 is NOT a fairness bug. Fairness pipeline verified intact. Root cause = enforceAttributeMixForShift post-fill swaps (active gender_requirement, 1M+1F/shift, all shifts) displacing ranker picks without backfill; same-day-doubles not re-evaluated. Erin/Kori/Michael starved to 6.3h, Lucas 26.3h. Two problems: (A) engine no-backfill, (B) policy decision. gender_requirement documented dormant but is LIVE — doc correction needed (04 §2.4, 06 §9).
- S3 diagnosed: in-tab TO approval does ~1/3 of magic-link path; shared-helper + server-route fix approach agreed; build held.
- S2 reported implemented by recap; IN REVIEW pending Alexander's diff read + tsc + fallback time-source check.
- No engine/Homebase fixes authorized this session — all hold on decisions above.

### 2026-06-08 (cont. 3) — gender rule reframed to temporal coverage
- Real requirement clarified: facility-wide 1M+1F at every staffed moment among counted guard roles (Headguard/Lifeguard/AManager; Greeter + Junior LG excluded; Manager TBD), guard-staff-on-floor, flag-don't-force, no single-staff windows. This becomes the ENGINE-2 fix: new sex_coverage constraint (scope=concurrent_coverage, validate-and-flag, no swap), retiring the per-shift attribute_mix swap that caused the inequity. Contract-first; spec pending Manager-counts confirm.
- S2 found implemented but uncommitted/unreviewed; expanded scope to GapResolverPanel + ManualScheduleBuilder. IN REVIEW pending Alexander's diff read + tsc + fallback check.

### 2026-06-09 — S2 residual closed + S3 built; cross-repo coverage-flag gap surfaced
- **Repo state at session start:** Aegis `main` ahead of origin by 3 (unpushed), with uncommitted edits to CLAUDE.md/DEV_ROADMAP.md + 2 untracked diagnostic scripts (diagnose-s1.ts, verify-sex-coverage.ts). Homebase `main` ahead by 2 (unpushed); the SCHED-EDIT-1 fix is committed (`f28cb30`), with an uncommitted CLAUDE.md edit + untracked `.claude/`. **Neither repo is fully committed/pushed** — flagged to Alexander (no push performed; his call). Roadmap is current. NOTE: orphan copies on Desktop (`CLAUDE_for_Aegis.md`, `CLAUDE_for_Homebase.md`, `DEV_ROADMAP.md`) exist outside both repos — the in-repo Aegis `DEV_ROADMAP.md` is canonical; recommend deleting/reconciling the Desktop copies to avoid drift.
- **S2 / SCHED-EDIT-1 residual CLOSED (code axis).** Answered the open question: `buildCanvas` sources slot `start_time`/`end_time`/`hours` from **shift_types** (`canvas.ts:89,100-105`), role from `shift_requirements`. The Homebase empty-target fallback (`resolveAssignment.ts:24-27`) uses shift_types — matches. `shift_requirements` carries its own time columns but the engine ignores them. Save-time backstop wires real shift_types in. Independent `tsc` = 0 errors. The two unscoped files = byte-identical `computeHours` dedup (safe). Only the live round-trip remains before DONE.
- **S3 BUILT (uncommitted, Homebase, 4 files):** shared `decideTimeOffRequest` helper + `POST /api/time-off-decision` route (cookie-auth, same-company guard) + magic-link dispatcher refactored onto the same helper + manager toast in TimeOffTab; `decided_by` now set on the in-tab path; guarded pending-only update on both paths. `tsc` = 0 errors; diffs reviewed; NOT pushed. **Open for Alexander:** confirm `AEGIS_URL` + `AEGIS_INTERNAL_SECRET` on Vercel (absent from `.env.local`, unverifiable from here); run the live round-trip with a sandbox/test identity (not a live employee).
- **Cross-repo coverage-flag check (priority 3) — RESULT: won't break, but the required surfacing is UNBUILT.** Homebase's schedule view reads `schedule.data.gaps` but **never reads `flagged_issues`** — no `.tsx` references it at all. So the new `unsatisfied_sex_coverage` variant cannot crash the view (nothing parses it). BUT roadmap line 111(a) requires the coverage flag to surface as a visible manager action item in the Homebase schedule view, and that renderer does not exist yet. Also a **type-contract mismatch**: Aegis emits a discriminated union (`{type,date,description,metadata}`, no `shift_name`/`severity`/`message` on the sex_coverage variant) while Homebase's `FlaggedIssue` interface is `{type,severity,message,metadata}`. A renderer built against the current Homebase type would read `.message`/`.severity` that Aegis never sends. **Precondition for the policy flip is therefore NOT met.** Proposed next step (NOT built — sits in the gated sex_coverage track): reconcile Homebase's `FlaggedIssue` to Aegis's union, then build a coverage action-item renderer (no-shift_name, time_window from metadata) + coalesce contiguous same-missing-sex flags.
- **Held for Alexander's decision (not touched):** the `sex_coverage` policy_value_json flip (scope=concurrent_coverage) — engine code may run ahead but the policy migration awaits Watermark management sign-off + your go. NOTE: "Phase 2 coverage-bias" appears **already decided as NO** on 2026-06-08 (line ~111) — flagging in case you want to re-open or confirm. Did not touch the gender rule's live behavior.
- **Next session:** (1) Alexander reviews S3 diffs + confirms Vercel env + runs the S3 live test; (2) flip SCHED-EDIT-1 → DONE after the live round-trip; (3) on your go, build the Homebase coverage-flag renderer (precondition for the sex_coverage flip).

### 2026-06-09 (session 2) — standardization sweep + finished the gender rule (code)
Theme: cleanup + drift reconcile + finish sex_coverage. Posture: fix-now if safe; prepare-don't-execute anything that writes production or pushes/deploys. All `tsc` runs clean (both repos).
- **PART A — cleanup (done).** Deleted 3 stale Desktop orphans (`CLAUDE_for_Aegis.md`, `CLAUDE_for_Homebase.md`, `DEV_ROADMAP.md` — confirmed older than the in-repo canonical copies). Deleted an empty botched-`mkdir` junk tree in Homebase (`{src/{app/...}` — zero files, untracked). Standardized `.claude/settings.local.json`: gitignored in both repos, `git rm --cached` in Aegis (was tracked). Kept the two diagnostic harnesses (`scripts/diagnose-s1.ts`, `scripts/verify-sex-coverage.ts`) and tracked them (they match the existing `scripts/` harness pattern). Left gitignored artifacts (`dist/`, `dry-run-output.json`) in place.
- **PART B — drift audit (diagnosed; safe reconciles done, rest presented).** Reconciled NOW (Homebase, tsc-clean): `FlaggedIssue` → Aegis union; `ScheduleData.summary` → optional; `ScheduleGap` += `description?/start_time?/end_time?`. Presented for go (rippling): adding `employees.sex` + `shift_requirements.accepted_roles` to Aegis `src/db/types.ts` (ripples into ~15 `smoke.ts` fixtures). Deferred: shared `StaffingReport` contract (producer is untyped `Record<string,unknown>`; shapes currently agree on consumed fields). Full details + stale-doc list in `SCHEMA_DRIFT_LOG.md` (2026-06-09 entry).
- **PART C — gender rule (code DONE; flip pending).** (a) Homebase renders coverage flags (`CoverageFlags` component, mounted in the current-schedule view + history report detail). (b) Aegis `sex-coverage.ts` coalesces contiguous same-missing-sex segments → one flag; verified by synthetic check (PASS). (c) Policy-flip SQL prepared, presented to Alexander (NOT executed). Live verify harness blocked in-sandbox (no Supabase egress) — run it where the DB is reachable.
- **PART D — S3 live verification: SKIPPED (could not confirm prereqs).** `AEGIS_URL`/`AEGIS_INTERNAL_SECRET` absent from Homebase `.env.local`, Vercel env not inspectable from the agent, and network egress is blocked — so the sandbox TO round-trip and the SCHED-EDIT-1 DB round-trip could not run. Both remain manual steps for Alexander.
- **PART E — self-enforcing protocol baked into BOTH `CLAUDE.md`** (session-start read; fix-now bias; defer-only-with-logged-reason; session-end write-back). Also fixed Homebase `CLAUDE.md`'s `src/db/types.ts` path (Homebase has none — types live in `src/lib/types.ts`).
- **PART F — logged + committed locally in logical groups; NOT pushed.** Commits + the policy-flip SQL presented for Alexander. Pushes + the Supabase flip are his to run.
- **Deferred (with reasons, logged):** db/types.ts column add (ripples into test fixtures → SCHEMA_DRIFT_LOG); shared StaffingReport contract (medium → SCHEMA_DRIFT_LOG); reference-doc refresh for gender_requirement dormant→live + Soteria prompt's attribute_mix model (large → SCHEMA_DRIFT_LOG); S3 + SCHED-EDIT-1 live round-trips and the sex_coverage live verify (no DB/network in sandbox → above); the policy flip itself (Alexander/management call).

### 2026-06-09 (session 3) — SECRET INCIDENT: GitHub push-protection block + history scrub
**Trigger:** GitHub secret scanning blocked the Aegis push — a Twilio **Account SID** in `docs/01_Business_Overview.md` (original scaffold commit, was `16018a5`). Stop-the-line: no push/force-push; prepared a clean state for Alexander to push.
- **Audit (offline; no network — gitleaks/trufflehog/PyPI all unreachable, built a regex/entropy scanner over full object history of BOTH repos).**
  - **No TRUE secrets in history → NO ROTATION REQUIRED.** Every credential-shaped string is a placeholder in `.env.example` (`ACxxxx`, `your_token`, `SG.xxx…`, header-only `eyJhbG…` JWT stub len 39, `sk-ant-…` len 13). Real keys are 100–220 chars; all matches are short examples.
  - **No `.env` (non-example) was EVER committed** in either repo.
  - **Flagged identifiers (scrub, no rotation):** Twilio **Account SID** (`AC…`) and **Messaging Service SID** (`MG…`) — in exactly two files ever: `docs/01_Business_Overview.md` and `docs/04_Aegis_Reference.md`. The Aegis SMS number (`+1616…`) is a known public-ish identifier (already in TEST_IDENTITIES) — left as-is.
  - **Homebase history: CLEAN** of credentials/identifiers (only code false-positives) — no scrub needed.
  - **Bonus finding (NOT a secret, flagged for Alexander):** `node_modules/` was committed in 2 historical commits on Aegis (~142 MB of bloat that will push). Out of scope for this secret task; recommend a separate `node_modules` history strip before/with the push if the size matters.
- **Scrub (Aegis only):** `git filter-repo` couldn't be installed offline (PyPI proxy-blocked), so used the built-in equivalent: `git filter-branch --index-filter` (no working-tree checkout → node_modules not materialized) scoped to the two doc files across `--branches`, replacing the SID literals with placeholders (`AC••• — redacted (see Railway env / password manager)`). Dropped `refs/original`, expired reflogs, `git gc --prune=now`.
- **Verified clean:** re-scan of full Aegis history = **0** AC/MG SID occurrences; working tree clean (redaction baked into every commit, so no separate redaction commit exists/needed); `tsc` clean both repos; origin remotes intact on both (filter-branch keeps them — no re-add needed).
- **Push is a FAST-FORWARD (no force needed):** the SID scaffold commit was never on origin (it lived in the unpushed range), so the rewrite produced new commits atop the unchanged origin base. `git push origin main` works normally for both.
- **PART 2:** added a "no secrets/sensitive identifiers in committed files — reference docs included" hard rule to BOTH `CLAUDE.md`.
- **Standing action for Alexander:** decide whether to also strip the committed `node_modules` from Aegis history (separate rewrite) before pushing.

### 2026-06-09 — Sprint go-live (in progress) + ENGINE-2 + secret scrub
- Aegis pushed & live (46eaa70) after a GitHub secret-scan block; Twilio SIDs scrubbed from history (docs 01/04), no .env ever committed, no rotation. node_modules committed in 2 Aegis commits (~142MB) — flagged, deferred.
- ENGINE-2: bimodal Headguard hours root-caused to the post-fill per-shift attribute_mix swap (not a fairness bug); replaced with sex_coverage (validate-and-flag). Policy flipped; confirmed live (hours flattened + flag in manager email).
- S1/ENGINE-1 ruled out as engine bug (Erin availability fixed; JL-zero-hours is structural -> Role Groups; 2 decisions pending).
- S2 (f28cb30) and S3 (f8e2505) committed, IN REVIEW pending Homebase push + live verify.
- Homebase ahead 8, UNPUSHED — S2/S3/CoverageFlags renderer/type reconcile/CLAUDE.md rule all dark until pushed (why the flag shows in email but not the Homebase schedule view).
- New goal logged: coverage-flag resolver (Tier 2).
- Next: push Homebase -> verify S2/S3 + flag display -> confirm Vercel env -> doc refresh; ENGINE-1 residual + forward-plan A/B/C await Alexander's call.

### 2026-06-09 (cont.) — Go-live verified: gender rule + SCHED-EDIT-1
- Homebase pushed (29ed00e): CoverageFlags mounted in the UpcomingCard preview. VERIFIED — the 6/15 sex-coverage flag renders in Preview & Edit. Root cause was a missing panel mount in the upcoming-week view, not persist/type drift (both confirmed correct).
- ENGINE-2 / gender rule -> DONE (policy flipped, hours flat, flag in email + Homebase).
- S2 / SCHED-EDIT-1 -> DONE (live manual move persists corrected hours).
- S3 -> IN REVIEW; Vercel env confirmed; pending sandbox approve-TO round-trip.
- Schema drift logged (3f57b30): two FlaggedIssue formats coexist in schedules.data.
- New Tier-2 item: decide fate of the inert per-shift attribute-mix swap.
- Remaining to close sprint: S3 round-trip, then S3 -> DONE.

### 2026-06-09 (cont.) — 48-hour sprint CLOSED
- S3 verified in sandbox (notify + decided_by + toast) -> DONE. All sprint items closed: ENGINE-2/gender rule, S2/SCHED-EDIT-1, S3 DONE; S1/ENGINE-1 closed-as-diagnosed.
- Sandbox corrected: created a dedicated sandbox manager login (the 'Bubba = sandbox manager' claim was wrong — 1:1 auth↔users↔company). Documented Test Guard A/B; seeded transient test TO 13759531.
- Watermark live on concurrent_coverage gender rule, persistent manual edits, notifying in-tab TO approvals.
- Next: Cowork operating model; then forward plan (PRIORITY2_ANALYSIS options A/B/C).

### 2026-06-09 (session 4) — doc alignment + Forward Build Sequence
Documentation-only pass (no code touched, no build run). Brought all six reference docs (01–06) + the live trackers to post-sprint reality and recorded the new forward direction.
- **Forward Build Sequence (Phases 1–4)** transcribed into this roadmap as its own section (Phase 1 harden/fix · Phase 2 comms loop · Phase 3 configurable rules · Phase 4 experience/leverage), with effort `[S/M/L]` and lane (⚙️ safe-lane / 🔒 human-gated) tags. It supersedes the A/B/C option framing in `PRIORITY2_ANALYSIS.md` §5. The end-state product vision was added here and to doc 01.
- **11 source notes folded in, deduped.** ~9 of 11 map onto items already in the Active backlog or bug board (note-map column ties each one); only **note 2's full-schedule email** and **note 9b's user guides** are genuinely new — both added to the backlog. Existing backlog items tagged `[P1]`–`[P4]`.
- **EMAIL_WORKFLOWS_TRACKER.md:** noted that the remaining TODO/UNTESTED intents + risky fan-outs belong to **Forward Build Sequence Phase 2**; refreshed the stale ENGINE-1 entry (was OPEN with the ruled-out hypothesis) to the closed-as-diagnosed reality.
- **Reference docs:** 01 end-state vision + Watermark-live facts; 02 `FlaggedIssue` → discriminated union + two coexisting `schedules.data` formats + gender rule now live concurrent_coverage; 03 §7 types path corrected (`src/lib/types.ts`; `src/db/types.ts` is an Aegis path) + CoverageFlags and its three mount views documented + SCHED-EDIT-1/in-tab `decided_by` marked resolved; 04 constraint vocabulary updated (`sex_coverage`/`concurrent_coverage` validate-and-flag, gender rule LIVE, `enforceAttributeMixForShift` retained-but-inert, email renderer handles both variants); 05 §6 active state rewritten (ENGINE-1/SCHED-EDIT-1/S3/ENGINE-2 all resolved) + Phase 1–4 + per-shift-swap decision + Cowork model reflected; 06 §9 limitations (gender_requirement live, ENGINE-1 resolution, per-shift swap inert/decision-pending) + new sex-coverage internals (`evaluateSexCoverage`, concurrent_coverage timeline segmentation, contiguous-flag coalescing).
- **CLAUDE.md (both repos):** confirmed the Cowork operating model is present; added a brief "design north-star" pointer (Phase 1–4 + end-state vision). Refreshed stale SCHED-EDIT-1 "OPEN" mentions in the Homebase CLAUDE.md to DONE.
- **SCHEMA_DRIFT_LOG.md:** marked the "Stale reference-doc sections" entries (gender_requirement dormant→live; Soteria attribute_mix model) RESOLVED 2026-06-09, entries left in place. **Did NOT touch** the live "two FlaggedIssue formats coexist" caveat.
- **Tier-3 "Reference-doc refresh" backlog item → DONE** (this pass).
- **Committed locally per repo, NOT pushed** (Aegis: `docs: align reference docs + roadmap to post-sprint reality and Phase 1–4 direction`; Homebase: `docs(claude): design north-star + operating model alignment`). Left for Alexander's review.
- **Flagged for human review:** the Soteria system-prompt's attribute_mix vocabulary (`homebase/src/app/api/soteria/route.ts`) still documents only per-shift scopes — a *code* change, out of scope for a docs pass, left for a Phase 3 Rules build. doc 04 §1.2 and §6 still call the wax-seal replay-window + IP-allowlist removal a "fast-follow"; they are now Phase 1 security work (cross-referenced, not rewritten).
- **Next:** Alexander reviews the two doc commits; begin Phase 1 (security audit, schedule download, DELIV-1).

### 2026-06-09 (session 5) — Cowork autonomous batch: Phase 1 security + schedule download + Phase 3 Role Groups draft
Autonomous SAFE-LANE batch (feature branches only; no main commits, no pushes, no prod/sandbox DB writes, no messaging). Built & unit-tested against fixtures; **no live-data verification** (egress allowlist exposes only `cowork_ro` read host; Supabase REST not allowlisted; the code isn't wired to `cowork_ro` yet). All three branches are committed locally and **NOT pushed** — `gh` is unavailable and the sandbox has no git push credentials, so Alexander pushes + opens the PRs.

- **TASK 1 — Homebase `/api/*` security audit → IN REVIEW.** Branch `security/api-auth-audit` (Homebase). Audited all 15 routes; wrote `SECURITY_AUDIT_API.md` (per-endpoint auth/exposure/risk). Finding: 6 routes already use the standard cookie+`company_id` guard; 2 use correct webhook-signature / single-use-token models; **4 had no auth** and got the **same standard guard** applied (unambiguous): `soteria-validate-assignment`, `soteria-validate-schedule`, `payroll/test-payroll-provider`, `payroll/test-timeclock` (all read company-scoped data via the service-role key off a body `company_id`). `tsc` clean. **Flagged, NOT changed (need a role/product decision):** `create-user` (HIGH — no auth at all; service-role creates an auth user + `users` row with body-supplied `role`+`company_id` ⇒ privilege escalation + cross-tenant; needs role gate + caller-company binding); `stripe` billing route (unscoped checkout/portal/customer creation — owner-only?); `stripe/webhook` (correct signature auth, but it's **not** in the middleware `isPublic` allowlist, so the middleware may 307-redirect Stripe's unauthenticated POST to `/login` — functional bug, verify against Stripe delivery logs); `aegis-action` token TTL/entropy confirm; and a strategic note to add **RLS** so a forgotten guard fails closed (the durable fix, tracked under the Dedicated security track). NOTE: the work order's premise that `notify-assignment` "was found open" no longer holds — it currently has the cookie+company guard; the Phase-2 "route notify-assignment through Aegis" item is about compliance/opt-in, not auth.
- **TASK 2 — Schedule download `xlsx → exceljs` → IN REVIEW.** Branch `schedule-download-exceljs` (Homebase). Re-implemented `renderScheduleGridXlsx` on **exceljs** (added dep) so real fills/fonts/borders/merges/frozen-panes/column-widths/row-heights reach the file — the SheetJS community build silently dropped cell styles, leaving only the cell text. Renderer is now async; the excel route awaits it. Both downloads still walk the same `buildScheduleGrid`, so Excel and the print/PDF HTML stay in lockstep (the PDF route returns landscape print-HTML from the same grid — unchanged). Updated `scripts/smoke-schedule-grid-download.ts`: awaits the async renderer, adds exceljs style assertions (gap fill `FFFDECEC`, gap text `FFB91C1C`, title fill, frozen `xSplit:1/ySplit:3`) + Excel↔PDF parity checks; uses `os.tmpdir()` for scratch. **Smoke test passes; `tsc` clean.** Live-data download (real schedule round-trip) is Alexander's.
- **TASK 3 — Role Groups engine (Phase 3) → DRAFT (exploratory, engine-only, NO Homebase UI).** Branch `role-groups-engine` (Aegis). `shift_requirements.accepted_roles` now drives eligibility: an employee qualifies for a slot if `qualified_roles ∩ accepted_roles ≠ ∅` (fill ANY accepted role). Added `qualifiesForSlot` + a deterministic **role-preference rule** `resolveAssignedRole` (named slot.role if held → else employee primary_role if accepted+held → else first accepted role held; accepted_roles order = manager preference). Wired: `CanvasSlot.accepted_roles` (canvas falls back to `[role]` for legacy reqs — byte-for-byte back-compat), `buildEligibility` + the two gap-reason filters use `qualifiesForSlot`, the disposition classifier gets the real accepted_roles, the fill loop records `resolveAssignedRole(chosen, slot)`. Resolves the **ENGINE-1 Junior-Lifeguard structural miss** when a Lifeguard requirement also accepts Junior Lifeguard. New `runRoleGroupsSmoke()` (eligibility, 3 preference branches, legacy regression, 2 `runScheduleBuild` e2e incl. the JL case) — **full engine smoke suite passes; `tsc` clean.** **DRAFT — not complete:** no Rules-tab UI; `shift_requirements.accepted_roles` added to `db/types.ts` as **OPTIONAL** to avoid the ~15-fixture ripple (NOT NULL in DB — flip to required once fixtures migrate, see SCHEMA_DRIFT_LOG); accepted_roles seeding/migration + live-data verification are Alexander's.
- **Pre-existing issue surfaced (NOT mine, NOT fixed):** `scripts/test-cascade.ts` no longer compiles against the current engine — it references `WeekState.assignmentsByDate` (removed) and an old `ResolverDeps` shape (missing `settings`). Stale harness, unrelated to Role Groups (the canonical engine test is `__tests__/smoke.ts`, which passes). Flagged for a separate cleanup.
- **Sandbox/git note:** the agent's mounted filesystem blocks `unlink`, so git left stale `*.lock` files and npm couldn't clean temp dirs (both worked around; harmless `.git/**/*.lock.stale.*` and `node_modules/.stray-*` artifacts may remain — safe to delete). An untracked `SECURITY_AUDIT_API.md` sits in the Homebase working tree on non-security branches (committed only on `security/api-auth-audit`).
- **Next (Alexander):** push the 3 branches + open PRs (`security/api-auth-audit`, `schedule-download-exceljs` in Homebase; `role-groups-engine` in Aegis); decide the `create-user` + `stripe` auth models; confirm the `stripe/webhook` middleware allowlist; live-verify all three against the sandbox tenant before merge; for Role Groups, decide seeding/migration + the `db/types.ts` required-flip.

### 2026-06-09 (session 6) — SEC-1 create-user authz → IN REVIEW
- Fixed the `create-user` route (Homebase branch `security/create-user-authz`): require sign-in; only `owner`/`quria` may create; role capped at the caller's level (quria>owner>manager); `owner` forced to their own `company_id` (body ignored), `quria` may target any. Diagnosed first — sole caller is the signed-in Access UI, no automated caller exists, so the sign-in gate breaks nothing. `tsc` clean; not pushed. (Spec's `quria_admin` = the live role `quria`.)

### 2026-06-09 (session 7) — Phase-1 tracker reconciliation (docs-only, branch `session-log-phase1-batch`)
Docs-only pass on the existing `session-log-phase1-batch` branch — no code touched, no push, no merge, `main` untouched in both repos. Reconciled the tracker docs to the true current state so "DONE" only means live-verified on Watermark.

- **Honest-status pass.** Confirmed the 48-hour sprint items already documented as DONE on Watermark — SCHED-EDIT-1 (manual edits persist), S3 in-tab TO approval (notify + `decided_by` + toast), ENGINE-2 / gender rule (concurrent-coverage validate-and-flag) — and that **ENGINE-1 stays CLOSED-AS-DIAGNOSED, not "fixed"** (Junior-Lifeguard residual routes to Role Groups; **two product decisions remain open: [decision 1 — to be named], [decision 2 — to be named]** — explicit placeholders, not invented).
- **Phase 1 batch status (branch-only, NOT live).** Re-affirmed: `security/api-auth-audit` IN REVIEW (4 routes guarded, `tsc` clean — pending live anon=401 / cross-company=403 / same-company happy-path on the 4 routes); `schedule-download-exceljs` IN REVIEW (`tsc` clean + smoke green — pending a real `.xlsx` opened in Excel to confirm fills/colors render uncorrupted); `role-groups-engine` DRAFT (NOT a merge candidate — two merge gates remain: (a) flip `db/types.ts` `accepted_roles` optional→required + migrate ~15 engine fixtures, (b) ratify the `resolveAssignedRole` preference rule before it goes live; capability is NOT a fix for the ENGINE-1 JL residual until live + Watermark flex groups configured + verified). All four branches confirmed clean read-only; `scripts/test-cascade.ts` is **pre-existing-broken on `main`** (references removed field `WeekState.assignmentsByDate` ×9) — not from this work.
- **Security findings SEC-1..SEC-4 formalized** in the Phase 1 table area (above): SEC-1 IN REVIEW with access model DECIDED (owner own-company / quria_admin cross-company / manager none / role capped at creator's privilege); SEC-2 NOT STARTED (decision-gated, likely owner-only + company-bound); SEC-3 DIAGNOSED (Stripe webhook may be 307-redirected by middleware — verify against Stripe delivery logs; fix = `isPublic` add or matcher exclusion); SEC-4 NOT STARTED (verify TTL/entropy on `aegis_action_tokens`, single-use already confirmed).
- **EMAIL_WORKFLOWS_TRACKER.md reconciled.** Three Tier-2 items updated in place: "Stripe webhook middleware verification" → DIAGNOSED 2026-06-09 (cross-ref SEC-3); "audit all Homebase `/api/*` for missing auth" → substantially complete via Phase-1 audit (cross-ref `SECURITY_AUDIT_API.md` + SEC-1..SEC-4); "Role Groups `accepted_roles` audit" → AUDITED 2026-06-09 (engine does NOT read it on `main`; branch `role-groups-engine` DRAFT does — cross-ref).
- **SCHEMA_DRIFT_LOG.md.** Appended a 2026-06-09 (session 7) entry stating **no new production schema surprise** this session, and restating the branch-only `role-groups-engine` drift as unchanged (live column NOT NULL; branch types it OPTIONAL; merge gate is flip-to-required + fixture migration). Old entries untouched (append-only).
- **TEST_IDENTITIES.md.** Reviewed; **no identity/tenant changes this session — no edits made.**
- **Reference docs (`docs/01–06`) — NOT touched** this pass. They describe the live system; Phase 1 work is branch-only and not live, so the reference docs stay as-is until live-verification flips the status.
- **Next:** live-verify the security audit (anon=401, cross-company=403, same-company happy-path on the 4 guarded routes); live-verify the exceljs download (open a real schedule `.xlsx` in Excel); implement + verify SEC-1 end-to-end; decide SEC-2 access model; verify and ship SEC-3 fix; complete SEC-4 verification; name the two open ENGINE-1 product decisions.

### 2026-06-10 (session 8) — Phase-1 branch verification (docs-only, branch `session-log-phase1-batch`)
Docs-only on `session-log-phase1-batch` — no code touched, no push, no merge, `main` untouched in both repos. Recorded two verification results from this session's read-only/sandbox checks; "DONE" still reserved for live-on-Watermark.
- **Schedule download (`schedule-download-exceljs`) → still IN REVIEW, now VERIFIED WORKING.** A sample `.xlsx` was generated by the new exceljs renderer and opened/inspected; the styling is genuinely in the file — dark header row, red `UNFILLED` gap cells, grey merged `CLOSED` column, and frozen top rows + left column. Remaining gate to DONE: merge + deploy (gated).
- **Security audit (`security/api-auth-audit`) + SEC-1 create-user (`security/create-user-authz`) → still IN REVIEW, now LOGIC VERIFIED.** An automated test (vitest, added on the throwaway `test/security-verify` branch; mocked Supabase auth + users lookup + captured `users.insert`; no DB, no network) exercised each route's `POST` directly — **22/22 cases pass**: the 4 guarded routes (anon=401 / own-company=allowed / cross-company=403) and `create-user` (anon=401, no-users-row=403, manager=403, role-cap-above-own=403, owner forced to own company with foreign id ignored, quria may target any company, garbage role=400). The two security branches were proven a **clean merge via `git merge-tree`** (the sandbox FS blocks a live `git merge` commit). Remaining gate to DONE: merge + deploy + the deploy-time real-login smoke.
- **`test/security-verify` is THROWAWAY — do NOT merge it** (logged as a Tier-3 warning). Merge the two original branches (`security/api-auth-audit`, `security/create-user-authz`).
- **New Tier-3 quality item:** Homebase has no test runner — adopt vitest in Homebase and port `src/app/api/__tests__/security-authz.test.ts` so this auth coverage survives discarding the throwaway branch.
- **EMAIL_WORKFLOWS_TRACKER.md** kept in sync (2026-06-10 verification note on the `/api/*` audit + `xlsx → exceljs` items). **No reference docs (01–06) touched; SCHEMA_DRIFT_LOG untouched this pass** (no new schema finding).

### 2026-06-10 (session 9) — Phase-1 PRODUCTION DEPLOY + two diagnosed bugs (docs-only, branch `session-log-phase1-batch`)
Docs-only on `session-log-phase1-batch` — no code touched, no push, no merge, `main` untouched in both repos. Logged the first Phase-1 production deploy and two post-deploy diagnoses.
- **DEPLOYED to Watermark production 2026-06-10 via PR into Homebase `main`.** `security/api-auth-audit` (4 route guards) + **SEC-1** `create-user` authz → **DONE** (deployed + app verified working). `schedule-download-exceljs` (exceljs renderer) → **DEPLOYED but not DONE** — styling not observable in prod because the schedule download 500s on real data (blocked by `DOWNLOAD-500`).
- **Operating-model change: Homebase `main` is now PROTECTED** — changes require a PR, not a direct push (push branch → PR → merge → Vercel). **`05_Development_Guide` §4 (Deployment) still says "push to main" and is now wrong — flagged for the next reference-doc refresh** (not edited; docs 01–06 out of scope).
- **`DOWNLOAD-500` (PRE-EXISTING, not the deploy) — DIAGNOSED, fix in flight.** Excel AND PDF download 500 on real data; the throw is in shared `buildScheduleGrid` (PDF uses zero exceljs), prime suspect a null/empty `employee_name` (`.trim()/.split()` on null) — likely SCHED-EDIT-1 residue. The new exceljs renderer was verified robust to every grid shape/content tried, so it is NOT the cause.
- **`DELETE-USER` (PRE-EXISTING, not the deploy) — DIAGNOSED, lower priority.** Access-page `handleRevoke` uses the anon client, ignores the error, deletes only `public.users`; old users blocked by FK constraints (`schedules.generated_by` NOT NULL, `time_off_requests.decided_by`, `activity_log`) so it silently fails; new users delete fine. Needs a soft-delete-vs-reassign decision + a server route + `auth.admin.deleteUser`.
- **SCHEMA_DRIFT_LOG.md** — appended a 2026-06-10 note that the exact `ON DELETE` FK clauses on `users.id` references were **inferred from the schema** (`schedules.generated_by` NOT NULL), not read from the live DB — confirm before building the `DELETE-USER` fix.
- **EMAIL_WORKFLOWS_TRACKER.md** kept in sync (deploy status + the two new bugs). **Reference docs 01–06 NOT touched.**

### 2026-06-10 (session 10) — Recovery build: `max_consecutive_days_worked` engine constraint
Attended recovery session on `feature/max-consecutive-days` (Aegis). NOT pushed, NOT merged, NOT deployed.

- **`max_consecutive_days_worked` engine constraint — IN REVIEW.** Enforcement implemented on branch `feature/max-consecutive-days` (Aegis). Status: **enforcement implemented; `tsc` + new tests + existing engine smoke all green locally; IN REVIEW; off-by-default; NOT merged, NOT deployed.**
- **State on entry.** Branch had 7 docs-only commits ahead of `origin/main`, 3 engine files modified but uncommitted (plumbing only — no enforcement), `tsc` red with 2 errors, no tests, no BUILD_NOTES. **No prior commit logged this feature as DONE** — the 7 docs commits don't mention `max-consecutive-days` at all (grep across full commit history of the branch returned 0 matches). Nothing to correct.
- **What landed this session (4 new commits on the branch):**
  - `a7bc87f` — preserve the uncommitted plumbing as-is (parser key + alias + `parseIntegerInRange`(1..7), `EngineSettings.maxConsecutiveDaysWorked: number | null` default `null`, disposition union member). Plumbing-only, still no enforcement.
  - `530105d` — mechanical `tsc` restoration: add `max_consecutive_days_reached` to `REASON_LABELS` + `REASON_ORDER` in `dispositions.ts`, add the matching `case` to `dispositionLabel()`'s exhaustive switch in `schedule-build-email.ts`.
  - `f05df7a` — enforcement: new `consecutiveDaysRunIncluding(empId, date, weekState)` helper in `eligibility.ts`; three short-circuit checks in `schedule-build.ts` (slot-eligible filter, conflict-only cascade filter, veteran-mode swap-pass candidate filter) mirroring the existing `max_weekly_hours` style; classifier emits `'max_consecutive_days_reached'` between the hours and doubles branches. Plus `src/lib/engine/__tests__/max-consecutive-days.ts` with (a) regression (default null cap → 7-day fixture unchanged, 0 max-consec dispositions) and (b) enforcement (1 employee, 7-day demand, `max=5` → Mon–Fri filled, Sat gap with `'max_consecutive_days_reached'` disposition, Sun filled because Sat broke the run).
  - `921fbc6` — `BUILD_NOTES.md` with full notes, semantics, the verbatim `tsc`/test output, the prior-week limitation, and a flagged discrepancy between the step-4 brief's expected outcome (days 6–7 both gapped) and the step-3 spec ("a day with no assignment breaks it" → day 7 placed). Implementation follows the spec.
- **Run semantic (locked into the tests).** A "worked day" = the employee has ≥ 1 assignment with that `date`. The "consecutive run including `date`" = the longest contiguous calendar-day window centered on `date` such that every day in the window is in the employee's worked-day set (with `date` itself added). Reject iff `settings.maxConsecutiveDaysWorked != null` AND that run > the cap. A day with no assignment **breaks** the run.
- **Prior-week scope.** Out of scope. The run is computed strictly from `weekState.assignments` (this build only). `TODO` preserved next to `EngineSettings.maxConsecutiveDaysWorked` in `types.ts` and next to `consecutiveDaysRunIncluding` in `eligibility.ts`.
- **Gates (all green locally).** `npx tsc --noEmit` = 0 errors. New test = 13/13 ✓. Existing `src/lib/engine/__tests__/smoke.ts` = 67 ✓ / 0 ✗ (unchanged). Verbatim output pasted into `BUILD_NOTES.md`.
- **Off-by-default.** Default `EngineSettings.maxConsecutiveDaysWorked` is `null`. With no tenant policy row configured for `max_consecutive_days_worked` / `max_consecutive_days` / `max_consecutive_work_days`, prod behavior is byte-identical to today.
- **Open / follow-up.** ~~(1) Confirm the run semantic~~ **CLOSED 2026-06-10 — Run semantic DECIDED = A (resets on day off); Semantic B (weekly worked-days cap) explicitly OUT OF SCOPE.** If B is ever wanted, it's a separately-named constraint (e.g. `max_worked_days_per_week`), not a tweak to this one — see `BUILD_NOTES.md` for the rationale. (2) Prior-week carryover if cross-week enforcement becomes important. (3) No production `policies` row exists for this constraint yet — needs a tenant configuration step before any client sees the effect.

**Enforcement-gap fix (2026-06-10) — both uncovered cap-bypass paths now enforced + tested; all 5 assignment-mutation sites covered.**
- `a6ce636` — `cascade.ts:legalToPlace` and `attribute-mix.ts` `replacement` filter both check `settings.maxConsecutiveDaysWorked` alongside the existing `max_weekly_hours` short-circuit. Uses the `viewState` (displaced-row hidden) pattern. Null setting → no-op.
- Two new test fixtures in `src/lib/engine/__tests__/max-consecutive-days.ts`:
  - **(c) Cascade** — direct unit test of `resolveBannedPairConflict`. Fixture tuned so cap=null and cap=2 produce DIFFERENT moves (cap=null picks Mon swap that would push emp-X's conservative run to 3; cap=2's `legalToPlace` rejects it and picks the Fri swap). Asserts: path entered, MUST-PASS invariant (no employee > cap), DIFFERENTIATING moves, cap=null deterministic.
  - **(d) Attribute-mix** — direct unit test of `enforceAttributeMixForShift`. Two males in the list; cap=null picks `maleAdjacent` (would extend his run to 3); cap=2 rejects him and picks `maleFresh`. Asserts: path entered + swap applied, MUST-PASS invariant, DIFFERENT swap targets.
- Plus 4 new parser-chain smoke asserts in `smoke.ts` (`max_consecutive_days_worked` happy/fraction/0/8).
- **Gates (all green locally).** `npx tsc --noEmit` = 0 errors. `max-consecutive-days.ts` = 28 ✓ / 0 ✗ (was 13). `smoke.ts` = 71 ✓ / 0 ✗ (was 67; +4 parser asserts).
- **All 5 assignment-mutation sites covered:** main fill push, cascade apply (via `legalToPlace`), veteran swap, attribute-mix swap, cascade internal mutations on cloned state. Verbatim test output + per-site coverage table in `BUILD_NOTES.md`.

### 2026-06-10 — SEC-3 fix: Stripe webhook isPublic (Homebase)
- **SEC-3 (Stripe webhook 307'd by middleware) — IN REVIEW.** Homebase branch `security/webhook-ispublic-authz` (cut from `origin/main`, HEAD on that branch is `9bb092d`). One-line fix in `src/middleware.ts`: added `pathname === '/api/stripe/webhook'` to the `isPublic` allowlist as an EXACT match (not a prefix) so the SEC-2 billing route at `/api/stripe` stays middleware-gated. The webhook itself is auth'd by Stripe signature verification in `src/app/api/stripe/webhook/route.ts:44` (`stripe.webhooks.constructEvent` against the raw body) — unchanged on this branch.
- **Verified locally via curl matrix (no Stripe CLI needed; off-prod):**
  - POST `/api/stripe/webhook` no signature → **400** `{"error":"Missing stripe-signature header"}` (was 307→/login pre-fix → handler reached, signature gate fires)
  - POST `/api/stripe/webhook` invalid signature → **400** `{"error":"Webhook Error: ..."}` (constructEvent rejects)
  - GET `/api/stripe` (SEC-2 billing route) no cookie → **307 /login** (SEC-2 still gated — exact-match fix did NOT widen exposure)
  - GET `/api/stripe/anything-else` no cookie → **307 /login** (no prefix spill)
  - GET `/` no cookie → **307 /login** (isPublic not over-broadened)
- **Pre-deploy dependency:** `STRIPE_WEBHOOK_SECRET` must be confirmed set on Vercel prod before merge — without it the failure mode shifts from today's 307 to a 400 signature failure. Verifying Vercel env state is gated (NOT done here). Local probe showed the secret isn't in `.env.local` either, which is fine for the curl matrix — the 400 still confirms the handler runs and the signature gate fires.
- **`tsc --noEmit` clean** on Homebase at this branch's HEAD.
- **Stripe CLI not available locally** → optional end-to-end positive case (`stripe trigger checkout.session.completed`) SKIPPED. Static analysis + the curl matrix already prove the handler is reached and signature-gated; the positive case can run on the live Stripe dashboard after merge to confirm 200 acknowledgments replace the historical 307s.
- **Doc-state coordination note (at the time of this log entry):** the formalized `SEC-3 — DIAGNOSED` entry in this roadmap (under "Phase 1 — Security findings") was on the still-unmerged `feature/max-consecutive-days` branch, NOT on `origin/main`. Status flip from `DIAGNOSED → IN REVIEW` on that formalized line was a follow-up pass once `feature/max-consecutive-days` merged. **Update:** `feature/max-consecutive-days` is now merged to `origin/main` (PR #2, merge commit `0128523`); the formalized line was flipped in the same commit that brought this Session Log entry onto trunk.
- **Next:** confirm `STRIPE_WEBHOOK_SECRET` env on Vercel prod (gated); push branch + open PR; live dashboard verify after merge.

### 2026-06-10 (end-of-session consolidation) — max-consec prep-to-push + SEC-3 fix + SEC-4 verified + SCHED-EDIT-1 re-verified + TEMPLATE-EDIT-1 lead
Multi-stream attended session, two repos. End state: 3 branches ready for review on GitHub (gated, not auto-merged); 2 empty diagnostic branches to be discarded; no prod env / DB / Vercel touched.

**max_consecutive_days_worked engine constraint — IN REVIEW, prep-to-push ready.**
- Branch `feature/max-consecutive-days` (Aegis), 20 commits ahead of `origin/main`, clean-merges (`git merge-tree --write-tree` exit 0, zero conflict markers). HEAD `fbe896d`.
- Verification stack (all green this session): `tsc` 0 / max-consecutive-days fixtures 28✓ / smoke 71✓. Sandbox real-data verification on the live sandbox tenant — fully-reversible seed (net-new shift_type + role + temporary policy + one snapshotted qualified_roles mutation; cleanup verified byte-identical to pre-seed state); with-cap vs no-cap on the target's 7-day demand differed on every axis (filled 6 vs 7, gaps 1 vs 0, longest-run 5 vs 7, cap dispositions 1 vs 0) — DIFFERENTIATING (not vacuous). Real assignments respected the cap end-to-end through `loadBuildData → parseConstraints → runScheduleBuild`.
- At-home helper prepped: `~/push-maxconsec.sh` (chmod +x, bash -n clean) and `~/maxconsec_pr_body.md`. Helper pushes the branch + opens a DRAFT PR via `gh` (falls back to printing the compare URL if `gh` is unavailable).

**SEC-3 — Stripe webhook isPublic (Homebase) — IN REVIEW.**
- Diagnosis pass (read-only) confirmed the formalized SEC-3 root cause: middleware matcher catches `/api/stripe/webhook` but `isPublic` didn't list it, so Stripe's session-less POST 307'd to `/login` and the event silently dropped. Signature verification at `route.ts:44` is correct and unchanged.
- Fix pass: Homebase branch `security/webhook-ispublic-authz` (off `origin/main`); one-line `pathname === '/api/stripe/webhook'` exact-match added to the `isPublic` allowlist (NOT a prefix, so SEC-2 billing route stays middleware-gated). Homebase commit `9bb092d`. `tsc` clean.
- Verified locally via 5-row curl matrix (off-prod): (a) no-signature POST → 400 "Missing stripe-signature header" (was 307→/login pre-fix); (b) invalid-signature POST → 400 (constructEvent rejects); (c) GET `/api/stripe` → 307 (SEC-2 still gated); (d) GET `/api/stripe/anything-else` → 307 (no prefix spill); (e) GET `/` → 307 (no over-broadening). Stripe CLI not available locally → positive case (signed event → 200) deferred to post-deploy live dashboard check.
- **Pre-deploy gate (NOT done — gated):** confirm `STRIPE_WEBHOOK_SECRET` is set on Vercel prod. Without it the failure mode shifts from today's 307 to a 400 signature failure.

**SEC-4 — `aegis_action_tokens` TTL / entropy — VERIFIED no-defect.**
- Read-only audit cross-repo (Aegis `src/lib/aegis-actions/tokens.ts` for generation; Homebase `src/lib/aegis-actions/tokens.ts` + `src/app/api/aegis-action/route.ts` for verify+consume).
- Entropy: `crypto.randomBytes(32)` → 256 bits CSPRNG, base64url-encoded, SHA-256 hash-stored (plaintext NEVER persisted). ✓
- TTL: explicit `expires_at` on every row; default `DEFAULT_TTL_MINUTES = 72*60 = 4320` (72h); every production caller (Aegis `schedule-build-email.ts:484`, `time-off-manager-email.ts:411,419`) explicitly passes 4320. ✓
- Enforcement: TTL checked at read-time (`verifyToken` Homebase tokens.ts:67-69 → `'expired'`) AND atomically at consume-time (`consumeToken` tokens.ts:87 — `.gt('expires_at', nowIso)` is a WHERE-clause predicate of the same UPDATE that flips `consumed_at`). Single-use enforced in the same atomic UPDATE (`.is('consumed_at', null)`). On-success `activity_log` row written. ✓
- **OPEN — product decision only (NOT a security defect):** is 72h the right TTL value? Informal recommendation: tighten to **24h for TO/availability magic-links** (1440 min) for tighter blast radius; keep **72h** (or bump to 120h) for `confirm_distribution` because managers may queue Thursday-for-next-week. Implementation when decided: one-line change in Aegis `src/lib/aegis-actions/tokens.ts:9` to `DEFAULT_TTL_MINUTES`, plus optional per-call overrides at the 3 call sites. Cross-repo coordination NOT needed — Homebase verification already enforces whatever `expires_at` sits on the row.
- No code shipped this session for SEC-4.

**SCHED-EDIT-1 — re-verified DONE on `origin/main`.**
- Verification pass (Homebase branch `fix/sched-edit-1` off `origin/main`; ended with 0 commits — discarded). Confirmed `f28cb30 fix(SCHED-EDIT-1): recompute shift times/hours on manual move` IS on `origin/main`.
- Static end-to-end trace of the persistence path on `origin/main`: drag → `moveAssignment` → `resolveAssignmentForSlot` (sibling-first, shift_types fallback, source-time defensive landing pad with warning) → `onAssignmentChange` → `setPendingAssignments`. Save: `ScheduleReviewPanel.save()` fetches FRESH shift_types and re-normalizes every pending row through the same resolver before writing `schedules.data` via `.update().eq('id', schedule.id)`. Load: `fetchSchedules` is a verbatim `SELECT` — no engine regenerate; `enterEditMode` reads `schedule.data?.assignments ?? []` verbatim. **No live defect.**
- **DOWNLOAD-500 "SCHED-EDIT-1 residue" hypothesis is retired.** Static trace confirms no manual-edit path produces null `employee_name` (move preserves source, add-shift validates employee, gap-fill passes the candidate name). DOWNLOAD-500 was independently fixed at `a3464bc fix(download): null-guard buildScheduleGrid + try/catch routes` — already on `origin/main`.

**TEMPLATE-EDIT-1 — strongest lead banked.**
- Overlap check against SCHED-EDIT-1: **DISTINCT path.** Schedule edits go to `schedules.data` (JSONB, keyed by id) via `ScheduleReviewPanel`. Template edits go to `schedule_templates` table via `useScheduleTemplate()` hook (`src/lib/hooks/useScheduleTemplate.ts`). No shared code; one fix won't clear both.
- **Strongest lead:** `useScheduleTemplate.ts:67` — `if (!error && data) { setTemplate(data) }`. The error branch does NOTHING. `TemplateEditorPanel.handleSave` (`TemplateEditorPanel.tsx:186-191`) `await`s `saveTemplate(local)` and then `onClose()` regardless. **On-site symptom ("edits don't take effect / won't save, no error shown") matches this exactly.**
- Existing Tier-3 `saveTemplate id:''` bullet's id-strip is OK — `id ? next : rest` correctly handles `id === ''` (falsy → uses `rest`). The failure is downstream.
- Secondary lead: `.upsert(payload, { onConflict: 'company_id' })` requires a UNIQUE constraint on `schedule_templates.company_id` — see SCHEMA_DRIFT_LOG 2026-06-10 entry. Verify before any fix.

**Open follow-ups (carried forward):**
- CASCADE-1 (cascade `legalToPlace` over-rejection — signature change, safe direction); CASCADE-2 (pre-existing cascade double-book-at-apply — NOT from max-consec branch); SANDBOX-SEED-1 (seed ≥1 active `shift_types` on sandbox); POLICY-JSON-SHAPE-1 (canonical `policy_value_json` shape before Rules-tab UI ships); SEC-4 TTL value decision (24h vs 72h vs 120h per action_type).
- **Formalized-line status flips, post-merge:** after `feature/max-consecutive-days` merges, the Phase-1 security block on that branch becomes `origin/main`'s. At that point: flip `SEC-3 — DIAGNOSED → DONE`; flip `SEC-4 — NOT STARTED → VERIFIED-no-defect (TTL decision open)`; close `SCHED-EDIT-1` as already-DONE-on-prod. This Session Log entry is the authoritative status until then.
- **Repo housekeeping:** local Homebase `main` reconciliation pending (tracked Tier-3); discard the two empty diagnostic branches (`fix/sched-edit-1`, `security/aegis-action-token-ttl` on Homebase).

**Pushed/PR'd at session end (this session):** see "GITHUB TO-DO" in the chat session report — 3 branches up for draft-PR review (max-consec, this docs branch, SEC-3 webhook). No merges.

**Next:** human review of 3 PRs on GitHub; merge gating; confirm `STRIPE_WEBHOOK_SECRET` on Vercel prod before SEC-3 merge; off-by-default verify on Watermark after max-consec merge; SEC-4 TTL product decision.

### 2026-06-10 (cont.) — branch reconciled to post-max-consec `origin/main`; formalized SEC-3 / SEC-4 / SCHED-EDIT-1 flips
Docs-only pass, no code. Triggered by `feature/max-consecutive-days` merging to `origin/main` via PR #2 (merge commit `0128523`), which moved the formalized Phase-1 security block onto trunk and put the prior `docs/sec-3-status-update` branch in conflict with `main`.
- **Branch rebased onto new `origin/main` by reset + fresh re-apply** (per instructions: keep all content from both sides, drop nothing). The prior branch's 3 commits (`a5a57de` SEC-3 IN REVIEW Session Log, `dc29197` TEMPLATE-EDIT-1 Tier-3, `d195d51` end-of-session consolidation + `schedule_templates.company_id` UNIQUE drift) were combined with this session's formalized-line flips into a single commit on top of `origin/main`. Branch is now clean-mergeable into `origin/main` (verified via `git merge-tree --write-tree` — 0 conflict markers).
- **Formalized status lines flipped (Phase 1 — Security findings block):**
  - **SEC-3 — DIAGNOSED → fix merged (Homebase) + deploying, LIVE-VERIFY PENDING.** Homebase `security/webhook-ispublic-authz` (`9bb092d`) merged into `main` and deploying to Vercel. Verified off-prod via 5-row curl matrix. **NOT DONE** until (a) `STRIPE_WEBHOOK_SECRET` confirmed on Vercel prod and (b) Stripe dashboard shows recent webhook events 200 instead of 307.
  - **SEC-4 — NOT STARTED → VERIFIED, no security defect (TTL value = OPEN product decision).** 256-bit CSPRNG generation, SHA-256 hash-stored, TTL enforced atomically at both read-time and consume-time, single-use enforced in the same atomic UPDATE. No code defect. Whether the 72h default is the right value is a product call, not a security one.
  - **SCHED-EDIT-1 → DONE re-affirmed** (commit `f28cb30`, live on Watermark prod, re-verified this session via static end-to-end trace of the persistence path on `origin/main`). The `DOWNLOAD-500` paragraph's "SCHED-EDIT-1 residue" hypothesis is retired in the same edit — DOWNLOAD-500 was independently fixed at `a3464bc` and the static trace proves no manual-edit path produces a null `employee_name`.
- **Stray `PRIORITY2_ANALYSIS.md`** in the working tree was left untouched per instructions (still untracked).
- **No merge, no Vercel touch, no Supabase touch.** Force-with-lease push only because the reset rewrote branch history; updates the existing `docs/sec-3-status-update` PR safely.
- **Next:** human review + Squash-merge the existing `docs/sec-3-status-update` PR; live-verify SEC-3 on the Stripe dashboard after deploy + confirm `STRIPE_WEBHOOK_SECRET` on Vercel prod (then SEC-3 → DONE); SEC-4 TTL product decision when convenient.

### 2026-06-11 — SCHED-DELETE-1 diagnosed + decisions locked
- Read-only diagnosis: delete already live but UI-only; RLS permits DELETE for any same-company user; zero FKs ref `schedules.id`; `users.role` is `quria` not `quria_admin`.
- Gate is temporal: managers delete current+upcoming, owner/quria also past. Soft delete confirmed. Distributed warning preserved. Build issued (branch off origin/main; DDL apply, RLS apply, and main merge are human-gated).

### 2026-06-11 — Environment + branch-base note
- ~/Desktop is a fuse mount that blocks unlink; git ops that unlink (reset --hard, checkout <branch>, lock cleanup) fail. Use no-unlink ops.
- origin/main did NOT carry the NOW/NEXT sprint block (SCHED-DELETE-1, AEGIS-EMAIL-1); it lived on docs/sec-3-status-update. Diagnosis was stacked on that branch, so merging docs/sched-delete-1-diagnosis brings both in.

### 2026-06-11 — SCHED-DELETE-1 DONE
- Live-verified: /api/schedule/delete soft-deletes; deleted_at set; schedule hidden from all
  Homebase surfaces; reversible. Migration 014 applied. Both repos merged + deployed. RLS
  FOR DELETE USING(false) live — client can no longer hard-delete.
- Lesson: first attempt was pushed-not-merged — prod served old code while the new RLS lock
  was live, causing silent delete failure. Always confirm the prod deploy reflects the MERGE
  commit on main, not just a branch preview.

### 2026-06-11 — AEGIS-EMAIL-1 diagnosed + comms target state set
- 3 of 8 email-actions wired; 5 dead stubs; approve_to prod-verified, deny_to email-path
  unverified, confirm_distribution sandbox-only + status-clobber bug.
- Target state set: distribute = command + Homebase button (no magic link); availability gets
  approve/deny buttons; distribution email redesign. Decision: drop the magic-link distribute
  path (resolves the status conflict). Findings logged to EMAIL_WORKFLOWS_TRACKER + SCHEMA_DRIFT_LOG.

### 2026-06-11 — AEGIS-EMAIL-1: distribute redesign + Aegis personability pass (built on branch)
Branch feat/distribute-email-redesign (commits 19a815b, c3e749b, 3ef9dbd). tsc clean,
greeting check green. NOT merged, NOT live-verified — pending review/merge/verify (DONE-rule).

Decisions:
- AEGIS-EMAIL-1 design rule: notification-RESPONSE actions (approve/deny TO + availability) =
  magic-link buttons; manager-INITIATED actions (distribute, build, coverage, queries) =
  conversational commands. Distribute becomes a command + a (net-new) Homebase button, NOT a
  magic link.
- Full-schedule attachment = OPTION D: Aegis self-renders a full-week all-staff HTML grid from
  schedules.data (assignments + gaps) it already loads — no Homebase call, no exceljs/PDF dep,
  no renderer fork. Chosen over (c) internal Homebase route (couples distribution to Homebase
  uptime at the 30-person fan-out) and (b) replicate-renderer (forks the canonical grid → drift).

Findings (build audit):
- No Homebase "Distribute" button exists — distribution fires only via the magic-link
  confirm_distribution email + the SMS-reply path. Building one is NET-NEW (work-list item 2).
- /api/schedule/download/pdf returns HTML, not a real PDF (no PDF lib). Doc drift vs
  03_Homebase_Reference §4.3 — fold the correction in at the next doc refresh.
- Homebase download routes are session-gated (Supabase auth.getUser) — no server-to-server path
  (the reason option D beat option C).
- Aegis sendEmail/EmailOptions had no attachments field — now added (generic SendGrid attachments).

Built (on branch):
- Distribute email: warm per-employee copy + the option-D full-week all-staff HTML grid attached
  to every employee email (gaps "UNFILLED — {role}", closures greyed).
- Aegis personability pass: shared greeting seam src/messaging/greeting.ts (firstName/greeting;
  full name → first token; null/empty → "there"); automated check scripts/check-greeting.ts
  (npx tsx scripts/check-greeting.ts — 6 cases, green) replaces eyeballing. Greetings now on ALL
  employee-facing messages and ALL manager notifications (each by their own first name); manager
  Homebase CTAs / magic-links preserved. Excluded: opt-in/onboarding (TCPA), HELP/INFO/STOP
  compliance, general_query.

Next: Homebase Distribute button + remove the magic-link confirm_distribution path + key the
re-distribution guard on distributed_at (kills the status-clobber re-fan-out bug); then
availability approve/deny buttons.

### 2026-06-12 — AEGIS-EMAIL-1 distribute redesign + personability: DONE
Merged PR #5 (feat/distribute-email-redesign) + PR #6 (fix/distribute-inline-grid). Deployed
Railway 7c7f158e [SUCCESS], commit 6d02665. Live-verified via rendered preview (desktop + phone
via AirDrop). DONE.

Shipped: warm per-employee distribute email + "Hi {first}," greeting; full-week all-staff schedule
shown INLINE in the email body (email-safe table); Aegis personability pass — shared greeting helper
(src/messaging/greeting.ts) + automated check (scripts/check-greeting.ts, 6 cases) + greetings on all
employee- and manager-facing messages.

Finding: .html email ATTACHMENTS don't render in Gmail (clients show raw source / force download),
so the full schedule moved from an .html attachment to an INLINE body table. Generic
EmailOptions.attachments infra (PART 1) kept but unused for distribution. Inline grid was assessed
on a real phone (AirDrop) and deemed acceptable; a day-by-day stacked fallback was speced, not needed.

Local-env note: local .env SENDGRID_API_KEY is stale (401) while prod is valid — refresh before
running local send harnesses.

### 2026-06-12 — download 500 fix, distribute week+outputs, template-unification defined
- Fix #1 (Homebase, MERGED): schedule download 500 for BOTH Excel + PDF. Root cause: `shift_types.days_active` is `number[]` but `buildScheduleGrid`'s `ShiftMeta` typed it `string[]` and `compactDaysLabel` called `.toLowerCase()` → TypeError swallowed by both routes. Fixed `compactDaysLabel` + retyped `ShiftMeta`; added a `number[]` regression case to the download smoke (old fixture used strings → why it shipped). `deleted_at` sweep ruled out.
- Fix #2 (Aegis, MERGED): `distribute_schedule` sent the wrong week. Root cause: classifier extraction spec was `{}` AND handler did `void extracted` + picked the latest schedule. Fixed: classifier extracts `target_week` (this|next, default next); handler resolves via `parseTargetWeek` → `getWeekBounds`, selects by `week_start` match (no schedule for that week → clear reply); ack/result/already-distributed name the actual week. PART B: distribute email gains a "This week:" special-notes/events section above the full grid.
- Finding: download rendered all-blue, not matching screen/template → Piece 1 of template-unification.
- Finding: `getWeekBounds` not company-tz-aware (Tier-1 fast-follow).
- Defined the unified-schedule-rendering initiative (Tier-2).

### 2026-06-12 (cont.) — template-unification Piece 1 built (download template colors)
- Piece 1 (Homebase, **IN REVIEW — committed `fix/download-template-colors` @ `4a813e5`, NOT pushed/merged**; `tsc` clean; download smoke green): extracted `resolveCellAppearance` as the single shared cell-color resolver across the on-screen grid + both download renderers; deleted the forked kind-only palettes → fixes the all-blue download (per-day header colors + per-day cell tints now match the screen).
- Diagnosis correction (changes the contract): the on-screen render colors from the TEMPLATE (day/shift via `getColor`), NOT role. The "template-first → role-color fallback" framing in the initiative was wrong — there is no role-color path on any surface; the template is already live on-screen. Resolver is role-free / template-driven. Surfaced before building; Alexander chose the role-free contract.
- On-screen render is a pure refactor (calls the resolver; rendered output unchanged). Closed-cell background still owned by `DroppableCell`.
- Tests: fixed the now-stale gap-fill assertion (gap cell carries the day tint + red TEXT, matching the screen, not a flat red box); added a two-distinct-color regression guard (orange Mon vs dark-blue Thu must reach BOTH download files as their own colors — neither the old shared navy; a single-column check would have passed while broken); added a direct `by:'shift'` contract case (template color wins over day color; falls back when absent).
- Out-of-scope, untouched, noted: `saveTemplate`/`TemplateEditorPanel` (Piece 2) and the Aegis distribute email renderer (Piece 3). No new bug found in either beyond the known framing.
- Next: Alexander pushes `fix/download-template-colors` + opens PR (PR body prepared); then Piece 2.

### 2026-06-12 (cont.) — Piece 1 MERGED + live; download name/role regression discovered
- **Piece 1 → `DONE` (merged 2026-06-12 to Homebase `main`, branch tip `4a813e5`; deployed to Watermark prod via Vercel).** The all-blue download fix is confirmed live: download per-day header colors and cell tints now match the on-screen schedule. PUSH-STATE banner updated to reflect.
- **Regression discovered post-deploy (live on Watermark):** the schedule download (both Excel and PDF) drops every employee's **last name** and **role** text. On-screen render is fully correct (first/last name, role, colors). Download colors are correct. The download is missing the last-name + role TEXT only — isolated to the download renderers; on-screen and the Aegis distribute email are unaffected.
- Suspected area (UNCONFIRMED, not investigated this session): Piece 1's download-renderer / `buildScheduleGrid` content-assembly change. Logged as a Tier-3 follow-up ("Piece 1 follow-up — download drops last name + role", status `DIAGNOSING`, severity LIVE IN PROD) — diagnose-fix is a separate Homebase task.
- **Next:** Homebase diagnose-fix of the name/role regression — this preempts Piece 2 until it's cleared.

### 2026-06-12 (cont. 2) — doc reconciliation: post-download state + email-workflow direction
Doc-only pass on `docs/template-unification-and-session` (no app code, no push, no merge). Reconciled a small set of stale entries so the cowork agents about to build the email workflows aren't working from a wrong map. This is **not** the comprehensive system-doc rewrite — that is deliberately deferred until the remaining email workflows are built + verified live.
- **Download last-name/role item: reframed.** Originally logged as a "Piece 1 regression / follow-up"; reframed as a **PRE-EXISTING download content gap, NOT caused by Piece 1.** Evidence on record: `git diff 4a813e5^ 4a813e5` (the Piece 1 commit) touches zero text-path lines, and the first-name-only display + missing per-assignment role text predate Piece 1. Tier-3 entry updated; Piece 1 "carries one known live regression" caveat removed.
- **Schedule download → DONE on Watermark (2026-06-12).** Piece 1 colors (merged) + download full-name/role content fix (merged 2026-06-12) — live-verified working on real Watermark data today. Phase 1 table row + PUSH-STATE banner updated.
- **`DOWNLOAD-500` → RESOLVED (symptom gone in prod 2026-06-12).** Cross-referenced the name/role fix, which reworked the suspected `employee_name` path in `buildScheduleGrid`. Mechanism caveat recorded: the exact root-cause line was **not** independently re-confirmed against the post-fix path — only the symptom-gone-in-prod fact, not over-claiming which line cured it.
- **DELIV-1 → downgraded BLOCKER → MONITOR / client-education.** The full ~30-person `distribute_schedule` fan-out ran in production 2026-06-12 with no spam problem observed. Kept as a watch item, not a gate. Updated in the Phase 1 table, the Tier-2 backlog entry, and the EMAIL_WORKFLOWS_TRACKER (Phase 6.5 + Phase 7 + Tier 0 table).
- **`distribute_schedule` → live success recorded (2026-06-12).** Full Watermark roster received the warm per-employee shift-assignment message + the inline full-week schedule. Only remaining piece on distribute is a visual-consistency fix so the emailed schedule matches the Homebase render exactly = template-unification **Piece 3**. Tier-2 "Two deliverables on distribute" backlog entry updated.
- **NOW / NEXT direction note added.** Active priority is building + testing the remaining Aegis email workflows at Watermark: verify `deny_to` via the email-button path; build `approve_availability` / `deny_availability` magic-link buttons + the shared employee-notify-back; emergency coverage accept/decline; `request_additional_batch`; manager labor-force queries; employee shift queries; employee shift-swap. The comprehensive system-document rewrite is deferred until those workflows are verified live.
- **Out of scope this pass:** any app code, any reference doc (`docs/01–06`), SCHEMA_DRIFT_LOG, TEST_IDENTITIES (no schema or identity change this pass). Single doc-only commit on `docs/template-unification-and-session`; **not pushed, not merged** — Alexander's call.

### 2026-06-16 — new batch scoped (voice/branding + design + current-week-ops), trackers updated
Session kickoff. Alexander directed the next batch: voice + branding pass first (sample-first rollout), then the Homebase rules-management UI + the veteran visual indicator, plus new current-week-operations scope. Decisions captured: (1) voice/branding rolls out sample-first — one workflow email rebranded + rewritten for sign-off, then applied across all; (2) veteran indicator = small brand-orange "VET" badge beside the name on the schedule grid (source `employees.is_veteran`); (3) mid-week changes to a published schedule notify affected people only.
- **NEXT BATCH updated** with a 2026-06-16 directive block + new items 7–11: Homebase rules-management UI, Soteria schedule-build trigger, Publish button (= distribute + published status; supersedes old item 5), published-schedule-as-live-source-of-truth (closed-day texts / swap shift-adjust / emergency-coverage edits; affected-only notify), and a general Homebase UI overhaul (parked at end of roadmap).
- **EMAIL_WORKFLOWS_TRACKER.md** to mirror the voice/branding + publish-status reconciliation rows.
- **No app code yet** this entry; building starts with the voice + branding sample workflow next. No push, no merge, no DB writes.

### 2026-06-16 (cont.) — voice + branding pass BUILT across all Aegis emails + magic-link pages (NEXT BATCH items 1+2 → IN REVIEW)
Built on branch `feat/voice-pass-1` (Aegis) + `feat/veteran-shift-rules` (Homebase). NOT pushed/merged — handed to Alexander. tsc clean + all 47 vitest green in Aegis; Homebase tsc clean.
- **New brand kit (Aegis):** `src/messaging/brand.ts` — single source of truth for email branding: `brandedEmailShell` (dark Quria frame: black header w/ the Aegis "A" monogram + "Aegis" wordmark, orange `#f97316` underline, footer "Aegis · Quria Solutions · <club>"), `brandedButtonRow` (orange primary / silver-outline secondary), `brandActionCard` (the distinct "Action needed · <topic>" panel), `brandCard`, `brandSectionLabel`, `BRAND` palette mirroring `homebase/globals.css`. Logo is embedded as an inline **CID attachment** (`src/messaging/brand-logo.ts`, base64 of `aegis-icon.jpg`) — `email.ts sendEmail` auto-attaches it whenever the HTML references `cid:quria-logo`, so it renders in Gmail/Apple Mail/iCloud without external hosting. (Hosted-URL logo was the original approach and did NOT render — switched to CID.)
- **Approved template + voice decisions (Alexander, this session):** header wordmark says **"Aegis"** (not "Quria Solutions"); message body text bumped to 16px; the request/buttons live in a visually distinct **action card** set apart from Aegis's conversational text; **conclusion-first** — greeting + what-it-is + what-to-do + "I'll let them know" all sit ABOVE the card so nothing critical is below it. Veteran badge / affected-only-notify decisions already logged above.
- **Emails rebranded + voiced** (all on the dark shell + action-card pattern + warm assistant-manager voice, conclusion-first): time-off manager (`time-off-manager-email.ts`, the gold-standard sample), time-off SMS-channel path (`time-off.ts buildManagerEmail`), manager build/distribute report (`schedule-build-email.ts`, Distribute action card), employee "your shifts this week" distribution + the team week grid (`schedule-build.ts`), availability + custom/rotating availability manager email (`employee-onboarding.ts buildAvailabilityManagerEmail` — test-asserted substrings `>Approve</a>`/`>Deny</a>`/`reply YES` preserved), emergency coverage blast + "no one available" branch (`emergency-coverage.ts`), shift swap (`shift-swap.ts`), payroll reconciliation (`payroll.ts`). Every plain-text reply already inherits the dark shell via `htmlFromText`.
- **Homebase magic-link pages:** `src/app/api/aegis-action/route.ts` — Approve/Deny/expired/error landing pages rebranded to the Quria dark look via a single `renderActionResultPage({title,message,tone})` helper (success green / error red / info orange), same-origin `aegis-icon.jpg` logo header. All token/redirect/status logic unchanged. (`time-off-decision/route.ts` is JSON-only — no HTML page to brand.)
- **Verification:** previews rendered to static HTML and reviewed (`scripts/preview-branded-email.ts`): time-off, distribute report, simple reply — logo + dark theme + action card confirmed. `npx tsc --noEmit` clean (both repos); `npx vitest run` 47/47 green.
- **NOT done in this entry:** push/merge/deploy (Alexander, gated), and a live sandbox round-trip (send a real sandbox email to eyeball in Gmail) — recommended before merge.

### 2026-06-16 (cont. 2) — INBOUND-SIG-1: inbound replies failing ECDSA verification — ROOT-CAUSED + FIXED (branch `feat/voice-pass-1`)
**Repo:** Aegis · **Status:** `FIXED (IN REVIEW — not pushed)` · **Severity:** HIGH (blocks the entire reply-based workflow for any inbound email carrying non-UTF-8 bytes)

**Surfaced by the live sandbox branding test (2026-06-16).** Branding verified live in Gmail ✅. But the employee's confirmation reply ("Yes!") never processed. Railway logs: the original request (5,311 B) verified; the reply (12,297 B) failed **every** retry with `[sendgrid-verify] invalid ECDSA signature`, and `bodyBytes` equalled `content-length` — so the raw body was captured in full (capture-raw-body is byte-exact; not the cause).

**Root cause (confirmed in library source):** `@sendgrid/eventwebhook`'s `verifySignature` does `Buffer.isBuffer(payload) ? payload.toString() : payload` — decoding the body as **UTF-8** — then `crypto.createHash('sha256').update(string)` re-encodes as UTF-8. SendGrid signs sha256 over the EXACT request bytes (`timestamp` || `rawBody`). Any inbound body containing non-UTF-8 bytes (invalid sequences → replaced with U+FFFD) hashes to the wrong digest → verification fails. Text-only emails are valid UTF-8 → pass; a **reply that quotes a prior email carrying an inline image** (or any non-UTF-8 charset part) contains binary bytes → fail. The voice/branding pass added an **inline CID logo** to every email, so quoted replies now reliably carry binary — which is why this surfaced now (the inbound code itself was never touched by the branding pass).

**Fix:** new `src/security/sendgrid-signature.ts → verifySendGridSignature(pem, rawBody, signature, timestamp)` hashes the exact bytes (`Buffer.concat([Buffer.from(timestamp), rawBody])`) using the SAME proven primitives (`PublicKey.fromPem` / `Signature.fromBase64` / `Ecdsa.verify` with a Buffer message → raw-byte hashing, no string round-trip). For valid-UTF-8 bodies this is byte-identical to the old path; for binary-bearing bodies it is correct where the old path failed. `verify-signature.ts` now calls it; `@sendgrid/eventwebhook` is no longer used for inbound verification. Ambient types: `src/types/starkbank-ecdsa.d.ts`.

**Test:** `src/security/__tests__/sendgrid-signature.test.ts` (5 cases) — generates an EC keypair, signs the exact bytes, asserts: plain ASCII verifies, a **binary/non-UTF-8 body verifies**, tampered body + wrong timestamp reject, and a **regression case proving the OLD `@sendgrid/eventwebhook` path returns false on the same binary input while the new path passes**. `npx tsc --noEmit` clean; `npx vitest run` **52/52 green**.

**Sweep (other rabbits — all clear):** no other caller of the lossy `verifySignature` / no other inbound `rawBody.toString()` anywhere in `src`. SMS verification uses `twilio.validateRequest` over parsed params (unaffected). `captureRawBody` is byte-exact (prod `bodyBytes == content-length`) and the sole consumer is the signed email route. Verified messages return HTTP 200 *before* async processing, so SendGrid won't re-deliver a verified message (no duplicate-processing from this fix). **Watch item (pre-existing, not fixed):** confirm-reply idempotency if two retries ever land simultaneously — low risk, logged for later.

**Handoff note for Alexander:** after this deploys, SendGrid may have already exhausted its retries on the original "Yes!" — so **re-send the confirmation reply** (trimming the quoted text is no longer required, but fine) to complete the sandbox round-trip. Deploy is Aegis-only (Railway); no Homebase or DB change.

### 2026-06-16 (cont. 3) — TO-RERUN-1 BUILT: re-check stale time-off recommendations (3 surfaces) + sandbox seed
**Repos:** Aegis + Homebase · **Status:** `BUILT (IN REVIEW — not pushed)` · addresses the lead issue `TO-REC-STALE`.

Surfaced by Alexander: TO approval recommendations are frozen at submit time, so queued requests are blind to each other and can collectively breach staffing if batch-approved. Short-term guidance = approve as they come in. Built increment = a re-check surfaced three ways.
- **Engine (Aegis `time-off.ts`):** `recomputeTimeOffRecommendation(requestId)` — reloads the request, re-runs stage1/stage2 `runSimulation` (which re-reads the live approved-TO baseline via `loadApprovedTimeOff`, so it sees everything approved since) + `generateTimeOffRecommendation`, updates `aegis_recommendation`/`aegis_reasoning`, returns `{ status, recommendation, reasoning, coverage_gap_count }`. Decision-status untouched.
- **Internal endpoint (Aegis `webhooks/internal.ts`):** `POST /internal/recompute-to-recommendation` ({ time_off_request_id }) → bearer-gated, returns the result.
- **Surface 1 — Homebase button:** `'/internal/recompute-to-recommendation'` added to the `AegisInternalEndpoint` union; new route `src/app/api/time-off/recompute/route.ts` (cookie-session auth + company-scope, mirrors `/api/time-off-decision`); "Re-run check" button + `handleRecheck` on each pending request in `TimeOffTab.tsx`, refreshing the badge with a toast ("…now recommends: Don't approve (1 coverage gap…)").
- **Surface 2 — email-card magic-link:** new `recheck_to` ActionType (Aegis + Homebase token unions); `buildTimeOffManagerEmail` mints a 3rd token + renders a secondary "Re-run check" link in the action card (HTML + text); Homebase dispatcher `case 'recheck_to'` → calls the internal endpoint → branded landing page shows the refreshed recommendation. (Token single-use → one re-check per email link; Homebase button + command allow repeats.)
- **Surface 3 — conversational command:** new `recheck_time_off` manager intent (`ai/claude.ts` classifier + `MANAGER_ONLY_INTENTS` + `intent-router.ts` dispatch); handler `handleRecheckTimeOff` finds the matching pending request (by employee name / date, company-scoped), recomputes, and replies in warm voice.
- **Sandbox test seed:** `SANDBOX_RERUN_SEED.sql` raises the sandbox PM Lifeguard `required_count` 1→2 (3 guards), so one off is fine but two off creates a gap — the clean approve→deny flip. Includes a revert.
- **Verification:** Aegis `tsc` clean + `vitest` 52/52; Homebase `tsc` clean. Built by this session (engine + Homebase button) plus two subagents (email magic-link, conversational command), centrally re-verified.
- **Deploy order:** Homebase before Aegis (email-button feature touching the aegis-action dispatcher). Then run `SANDBOX_RERUN_SEED.sql` in Supabase (sandbox) and the flip-test (see handoff). **Long-term (still open):** auto-recompute on view / re-check at approval time.

### 2026-06-17 — TO-RERUN-1 shipped + verified live; resolution notices, click-guards, in-thread reply, fast-response polish; docs swept
Live-verified on the sandbox by Alexander ("test worked great"). All of TO-RERUN-1 is now merged + deployed except the final fast-response polish (built + green, pending one more deploy/re-test).
- **In-thread email re-run.** The email-card "Re-run check" now recomputes and REPLIES in the original manager thread with a refreshed action card (deterministic per-manager `Message-ID` via `toThreadMessageId` on the original send + `In-Reply-To`/"Re:" on the reply; `email.ts` gained `message_id` + `in_reply_to`). Endpoint `/internal/recheck-to-reply` (Aegis) ← Homebase dispatcher `recheck_to`.
- **Resolution notices.** On approve/deny via ANY channel (`sendDecisionNotification` → `sendManagerResolutionReplies`), Aegis posts a "✓ Resolved" reply into each manager's thread (`buildTimeOffResolutionEmail`) — caps the conversation and notifies the OTHER managers it's handled. This is the answer to "notify managers when action takes place" without extra texts.
- **Click-guards.** Approve/Deny on an already-decided request → "already \<status\> by \<name\> on \<date\>" (Homebase `decideTimeOffRequest`, covers in-tab AND magic-link); Re-run on a decided request → "already decided — no re-check needed" (recompute guard + dispatcher); the magic-link "already used" page softened to reassure.
- **Fast-response polish.** `/internal/recheck-to-reply` now responds INSTANTLY (cheap status read only) and runs the recompute (AI) + threaded reply in the BACKGROUND — fixes the slow/hung landing page that made managers re-click into "link already used." (Built + tsc/tests green; on branch `fix/recheck-fast-response`, pending deploy + a quick re-test.)
- **Root-cause learning (TO-REC-STALE, resolved as not-a-bug):** the engine counts only APPROVED time-off and DOES honor custom/rotating availability. The earlier "wrong recommendation" in the sandbox was stale rotating-availability test data on Shmubba (pool was really 2, not 3), not an engine fault — confirmed via `activity_log` metadata (`stage1_coverage_after`). `SANDBOX_RERUN_SEED.sql` now also deactivates stale custom availability.
- **Branding + voice pass and INBOUND-SIG-1 are live + verified** (branded emails confirmed in real Gmail; inbound reply verification fixed). NEXT BATCH items 1–2 (branding, voice) → DONE.
- **Docs swept this session:** `docs/03_Homebase_Reference.md`, `docs/04_Aegis_Reference.md`, `docs/05_Development_Guide.md`, `WORKFLOW_TEST_PLAN.md` (new §9), `WATERMARK_MANAGER_GUIDE.md`, plus this roadmap, `EMAIL_WORKFLOWS_TRACKER.md`, `TEST_IDENTITIES.md`, `SESSION_HANDOFF.md`. No schema migrations this session.
- **Next up (unchanged priority):** veteran VET badge on the schedule grid → Homebase per-shift veteran-rules management page → Soteria schedule-build trigger → Publish button (distribute + published status) → general Homebase UI overhaul.

### 2026-06-17 (cont.) — VETERAN feature shipped (config UI + grid badges) — for Watermark's next build
NEXT BATCH items 2 (visual indicators) + the rules-management UI are BUILT (Homebase-only; tsc clean). Priority was getting managers able to CONFIGURE per-shift veteran rules + see them, ready for tomorrow's schedule build.
- **Configure UI:** new `src/components/rules/ShiftVeteranRulesSection.tsx`, mounted on the Rules page. View/create/edit/delete `shift_experience_rules` directly (RLS allows company-scoped client CRUD — one policy "Company shift experience rules access", cmd=ALL). Modal: shift picker (shift_types), mode (all veterans / at least N), optional role, day-of-week multi-select (none = every day), optional season, active toggle. Writes the exact engine shape — `mode` ∈ {`all_veterans`,`min_veterans`}, `min_count` only for `min_veterans`. The Aegis engine already enforces these rows (experience-rules.ts), so a UI-created rule staffs the next build.
- **Grid badges:** `ScheduleRenderer` gained `veteranIds?: Set<string>` (orange "VET" pill beside veteran names, `employees.is_veteran`) and `shiftRuleLabels?: Record<shiftName,string>` (per-shift "Veterans only" / "≥N veterans" tag). `schedule/page.tsx` fetches veteran ids + active rules + shift_types and threads both into all 3 ScheduleRenderer usages.
- **Data note:** `mode` is `min_veterans` (NOT `min_count`); `min_count` is the column. Documented in SCHEMA_DRIFT_LOG.
- **Remaining veteran piece (not yet built):** the same rule indicator in the Aegis **build/distribute email report** (`schedule-build-email.ts`) — supplementary visibility; the grid + config cover tomorrow's need.
- **Deploy:** Homebase-only (Vercel). Eyeball the grid badges + Rules page after deploy. Branch: `feat/veteran-ui`.

### 2026-06-17 (autonomous build run) — Publish button (item 9) + Republish/swap (item 12) BUILT — IN REVIEW, not pushed
Autonomous overnight build; SAFE LANE only (no merge, no deploy, no prod-DB write, no real sends). Both repos tsc-clean; Aegis vitest 61/61 (was 52, +9 new). Nothing pushed — handed to Alexander as a review packet with copy-paste git steps + a sandbox test plan. **Source-of-truth reconciliation:** `published_at` is now the authority for "this is the live published schedule for its week" (the ambiguous `status` enum is no longer load-bearing for logic); the re-distribution guard continues to key on `distributed_at` (already correct in `distribute-guard.ts`). Confirmed via live DB read: `schedules` already had `distributed_at`/`approved_at`/`deleted_at`; it had NO `published_at`/`archived_at`/`superseded_by` and NO unique constraint on (company_id, week_start), so multiple rows per week were already allowed.
- **DB (GATED — migration written, NOT applied):** `homebase/migrations/016_schedules_publish_republish.sql` — adds `published_at`, `archived_at`, `superseded_by` (FK→schedules.id ON DELETE SET NULL); backfills `published_at = distributed_at` for already-distributed rows; adds partial unique index `schedules_one_live_published_per_week` (one live published schedule per company/week); adds `publish_schedule_swap(p_new_id, p_old_id)` SECURITY DEFINER function that ATOMICALLY archives+unpublishes the old row (sets archived_at, superseded_by, clears published_at, strips ONLY `staffing_report.estimated_wages`) and publishes the new row. Verified no existing week has >1 distributed schedule, so the unique index + backfill apply cleanly. Apply SANDBOX FIRST.
- **Item 9 — Build + Publish (Homebase):** new routes `src/app/api/schedule/build/route.ts` and `src/app/api/schedule/publish/route.ts` (authz mirrors the SEC-1 / schedule-delete pattern: sign-in → role manager/owner/quria → tenant binding → temporal gate; quria may act cross-company). Build reuses a new Aegis `/internal/build-schedule`; Publish writes a `schedule_published` activity_log row and fans out via the existing `/internal/distribute-schedule`. UI: Build buttons in the empty states + a Publish button on the current-week header and each upcoming card; status badge + list logic now key on `published_at`. Aegis refactor: extracted `buildScheduleAndSave()` build-core out of `handleBuildSchedule` (no behavior change to the email path) so the Build button and the email command share one core.
- **Item 12 — Republish/swap (Homebase + Aegis):** Publish detects an existing live published schedule for the same week and switches to the republish path — calls `publish_schedule_swap` (atomic), logs `schedule_republished` + `schedule_superseded`, then calls a new Aegis `/internal/notify-schedule-changes` which notifies ONLY the employees whose shifts actually changed (changed-only, confirmed), each with their NEW shifts. Old schedule is ARCHIVED (kept, marked superseded) and only its wage/hours estimate is cleared. Diff core extracted to dependency-free `Aegis/src/lib/schedule-diff.ts` (`computeChangedEmployeeIds` / `employeeShiftSignature`) with a 9-case unit test (`republish-diff.test.ts`). UI: same-week alternates render as cards under "This Week" with a "Publish & Replace" button + a "Build alternate" button; archived schedules are hidden from current/upcoming and show a "Superseded" badge in history.
- **Changed files — Aegis:** `src/workflows/schedule-build.ts` (build-core extraction + `notifyScheduleChangesCore`), `src/lib/schedule-diff.ts` (new), `src/webhooks/internal.ts` (+2 endpoints), `src/workflows/__tests__/republish-diff.test.ts` (new), `SANDBOX_PUBLISH_SEED.sql` (new), `DEV_ROADMAP.md`. **Homebase:** `src/lib/types.ts`, `src/lib/aegis-internal.ts`, `src/app/api/schedule/build/route.ts` (new), `src/app/api/schedule/publish/route.ts` (new), `src/app/(app)/schedule/page.tsx`, `src/components/schedule/TemplateEditorPanel.tsx`, `scripts/smoke-schedule-grid-download.ts`, `migrations/016_schedules_publish_republish.sql` (new).
- **NOT done / open:** migration not applied (gated — sandbox first); `EMAIL_WORKFLOWS_TRACKER.md` work-list #2 ("remove the magic-link `confirm_distribution` path") left untouched to stay in scope — its dispatcher still writes `status='distributed'`, harmless now that the guard keys on `distributed_at`; an archived schedule for a still-current week is hidden until the week ends (row preserved, reappears in Past as "Superseded"); deploy order Homebase-before-Aegis as usual but Aegis must be deployed too (new internal endpoints) — see packet.

---

### 2026-06-23 (cont.) — SOTERIA pillar 2 completed + TEMPLATE-EDIT-2 started

- **Richer roster import (MERGED).** New pure `src/lib/soteria/rosterImport.ts` (`planRosterImport`) canonicalizes role names against the company's defined roles, coerces messy veteran flags, sanity-checks weekly hours, and de-dupes against the existing team + within the batch. Wired into the `import_employees` executor (loads existing names+roles, inserts only new, returns imported/skipped/warnings). 16-check test. Fixes blind duplicate inserts + role-spelling drift on re-import.
- **Existing-schedule ingestion (BUILT, pending merge).** New pure `src/lib/soteria/scheduleInference.ts` (`inferShiftStructureFromSchedule`) turns extracted schedule rows into a ConfigBundle: distinct shifts with modal hours, union of days, and per-role required_count = busiest single day. New executor action `import_schedule_structure` runs inference → `planConfiguration` → applies only role + shift_type steps (own guarded loop; `apply_setup_plan` untouched). Prompt section "IMPORTING AN EXISTING SCHEDULE" + action spec added. 15-check test. Branch `feat/soteria-schedule-import`. ⇒ Soteria can now be set up from a handbook, a roster, OR an existing schedule.
- **TEMPLATE-EDIT-2 — STARTED (item 3).** Diagnose-first underway on the banked save-error in `useScheduleTemplate.ts` + `TemplateEditorPanel`. See the in-progress branch for the template editor + live preview work.

### 2026-06-23 — SOTERIA 4-PILLAR SPRINT (Cowork) — pillars 1/3/4 merged, pillar 2 first slice built

Focused Soteria session against the PURPOSE & DEFINITION-OF-DONE block. Diagnose-first throughout; safe-lane only (branches + sandbox + read-only DB; no merges/prod-writes/real sends by the agent). All work in Homebase. Each increment puts its logic in a pure, ts-node-tested module with thin route wiring; tsc kept clean.

- **Pillar 3 — Persistence (MERGED).** Audit found the chat client already surfaces failures, but 5 executor actions swallowed their Supabase `{ error }` and returned `{ success:true }` regardless — same class as the template-editor swallowed-save. Fixed `delete_employee` (both deletes), `update_profile` (update + insert), `delete_policy`, `update_availability` (replace-all wipe), `set_custom_availability` (deactivate step) via new `src/lib/soteria/persistGuard.ts` (`throwOnWriteError`); also tenant-scoped the availability delete. Test: `persistGuard.test.ts` (17 checks). Branch `fix/soteria-persist-save-errors`.
- **Pillar 4 — Explains everything (MERGED).** Soteria could edit but not SEE recurring `availability`, active `custom_availability`, and active `shift_experience_rules`. Added all three to `getCompanyContext` + rendered plain-English sections via pure `src/lib/soteria/contextFormatters.ts` (wording mirrors `schedule/page.tsx`); summary counts added. Test: `contextFormatters.test.ts` (16 checks). Branch `feat/soteria-read-availability-rules`.
- **Pillar 2 — Document ingestion, first slice (BUILT, pending merge).** Handbook/policy doc → reviewed setup plan. New pure `src/lib/soteria/ingestionPlanner.ts` (`planConfiguration` / `summarizePlan`) orders + validates + de-dupes a bundle into steps (roles → wages → shift types[+role reqs] → policies → veteran rules), flagging unresolved references. New executor action `apply_setup_plan` runs the planner against live state and applies in order, threading created shift-type IDs, every write persist-guarded. Prompt section "CONFIGURING FROM A DOCUMENT" + action spec added; shared `capabilities.ts` deliberately left untouched (mirrored with Aegis, which can't ingest docs). Test: `ingestionPlanner.test.ts` (26 checks). Branch `feat/soteria-document-setup`. Known limit: apply is not a single DB transaction (partial-apply possible on unexpected error; planner pre-validates so rare). Employees stay on `import_employees`; conflicts deferred (need employee IDs).
- **Pillar 1 — Foolproof (MERGED earlier this date):** SOTERIA-CHECK-1 validator (`validateScheduleEdit`).
- **Net:** all 4 pillars substantially addressed. Soteria follow-ons: existing-schedule ingestion, richer roster import (in progress). Next non-Soteria item: TEMPLATE-EDIT-2.

---

### 2026-06-24 — TEMPLATE-EDIT-2 finished across all four surfaces + COVERAGE-FLAG-DISMISS queued

- **TEMPLATE-EDIT-2 DONE (item 3).** The template editor's configured look now appears identically in the Homebase preview, the drag-and-drop content editor, the Excel/PDF download, AND the emailed-to-employees schedule. Slices this stretch: (4) downloads honor `layout_type` via `buildScheduleGrid` re-pivot using the shared pure `layoutGrids` model, preserving the no-veteran download invariant — test `buildScheduleGrid.layout.test.ts`; (5) editable content editor in any layout — pure tested `applyAltMove` (employee-rows reassigns person+day, role-rows changes role+day) + `AltLayoutGrid` made editable, reusing the existing dnd + `onAssignmentChange` so save + Soteria checks are identical; edit dispatch now uses the chosen layout (shift-rows path untouched = live-club safe); (6) **emailed schedule** — new self-contained Aegis `src/workflows/templated-grid.ts` renders the team grid in the template's layout/day-colors/column-order/shown-fields; wired into both distribute call sites in `schedule-build.ts`, used only when a `schedule_templates` row exists, else the legacy renderer (zero regression). Aegis tsc clean + 124/124 vitest. **Branch `feat/email-schedule-template` — DEPLOY = Alexander (Railway/live); Aegis-only, reads the existing table, no new deps (don't full-reinstall Supabase).** Also added `tsconfig.scripts.json` baseUrl/paths so ts-node tests resolve `@/` at runtime.
- **Earlier this date:** template save fix (RLS policy migration 017 + surfaced swallowed save), layout-support gate, employee/role view layouts — all merged in Homebase.
- **NEW — COVERAGE-FLAG-DISMISS (item 9.5)** queued at Alexander's request: subtle semi-transparent trash-can button inside each coverage-review notice with an "are you sure?" confirm, so managers can hide advisory coverage flags; persists, doesn't touch schedule data. Homebase-only, ~3–4h.
- **July 1 readiness:** the EMAIL system + template editor demo end-to-end. Remaining email-workflow items for the demo: emergency-coverage-by-email (6), Quria Gmail fix (7), veteran guardrail (8). Next build picked up after this log update.

### 2026-06-24 (cont. 2) — emergency-coverage email buttons (item 6) + Quria Gmail fix (item 7)

- **Item 6 — emergency-coverage Accept/Decline email buttons (BUILT, branch `feat/coverage-email-buttons`, deploy = Alexander).** Diagnosis: the fan-out engine was already merged + working via reply-YES/NO on email & SMS; the only gap vs the other email workflows was branded buttons. Added `decision_type:'coverage'` to `/webhooks/decision` (Accept→approve, Decline→deny) + an isolated `processCoverageButtonDecision` core in `emergency-coverage.ts` that reuses the in-module helpers (schedule swap, shift-filled fan-out, manager notify, batch-exhaust) WITHOUT touching the inbound-reply handler or the time-off/swap decision branches. `classifyCoverageButton` pure + 4 tests; test setup seeds `BASE_URL`. tsc clean + 128/128. **Round-trip (real email click) still needs a sandbox eyeball.** Walked Alexander through the full workflow (manager-in-the-loop: list → manager picks ALL/names → blast batch of 5 → first-yes-wins → 30-min timeout scheduler asks manager for next batch; 30-min + batch-size-5 are hardcoded constants, NOT yet Homebase settings — deferred per Alexander).
- **Item 7 — Quria Gmail fix (DONE).** The time-off resolution-notice fan-out selected all company `users` incl. the `role='quria'` admin row → personal Gmail got manager notices. Added `.in('role',['manager','owner'])`. Audited all 7 manager-email user-queries; this was the only leak. The fix is the uncommitted edit in `time-off.ts`.
- **Roadmaps updated:** items 6 & 7 marked; estimate ≈97h remaining. Next: veteran guardrail (#8) or schedule ordering/parity (#9).

### 2026-06-24 (end of day) — audit + clean-slate handoff

- **Full deploy audit done (verified against Vercel + Railway, not just git):** EVERYTHING built this stretch is LIVE except one item. Homebase production = the editable-content-editor merge → all of Soteria (4 pillars + doc ingestion) and all of TEMPLATE-EDIT-2 (save fix/RLS, layout gate, alt-layouts view, editable content editor) are live; the `schedule_templates` RLS migration was applied in Supabase. Aegis production = the coverage-buttons merge → Twilio-optional, emailed-schedule template parity, and coverage Accept/Decline buttons are live. **ONLY pending:** the **Quria Gmail fix** (item 7) — pushed to `feat/coverage-email-buttons` but landed after that branch's PR #54 merged, so it needs ONE more PR/merge to deploy. (Local sandbox git showed a stale Homebase `main` — ignore; the deployments are the truth.)
- **Clean-slate steps handed to Alexander** (to run next login): commit the roadmap docs onto `feat/coverage-email-buttons`, open + merge that PR (lands Quria fix + docs, Railway redeploys), `git restore` the 4 cosmetic Twilio-comment files, then `git checkout main && git pull` in BOTH repos; Homebase: `rm -f a.out` + sync. Leftover Desktop worktrees (`Aegis-audit`, `-bug5`, `-waxseal`, etc.) + old merged branches are harmless clutter — declutter together next session.
- **NEXT SESSION ORDER (Alexander):** (1) branch/worktree cleanup + confirm everything pushed/clean + local repos current; (2) THEN resume July-1 build. Next build candidates: veteran guardrail (#8, ~3h), schedule ordering/parity (#9), coverage-flag dismiss (#9.5). New far-future concept logged as item 16 (per-client monitoring/observer inboxes — post-UI-overhaul).

### 2026-06-28 — Item #13 (custom availability by email) verified-in-code + intake test added

- **Diagnose-first finding:** #13 was already built end-to-end — it shipped piecemeal across the earlier date-limited build and the voice/branding pass, but the roadmap line was never closed. Traced the whole chain live across BOTH repos: Aegis classifier (`src/ai/claude.ts`) emits `extracted.end_date` for an "until/through" boundary (with examples + current-year resolution) → `intent-router` `case 'update_availability'` forwards `extracted` → `handleUpdateAvailability` (`employee-onboarding.ts`) branches **rotating** (`isRotatingAvailabilityRequest` → `parseRotatingAvailability`, Sunday-anchored) / **date-limited** (`/^\d{4}-\d{2}-\d{2}$/` on `extracted.end_date` → `custom_end_date`) / permanent → employee YES → `buildAvailabilityManagerEmail` mints `approve_/deny_custom_availability` magic-link tokens carrying the full approval snapshot → Homebase `dispatcher.ts` has explicit cases for both → `POST /internal/apply-custom-availability-decision` (validates date-limited AND rotating) → `applyCustomAvailabilityDecision` deactivates prior overrides + inserts the `custom_availability` row (date_limited or rotating) + notifies the employee with **no Homebase link**.
- **Schema check (hard rule):** queried live `information_schema` for `custom_availability` — every written column (`company_id, employee_id, type, end_date, cycle_weeks, cycle_start_date, patterns, active`) exists and all NOT-NULLs are satisfied. No drift.
- **Gap closed:** the one untested link was the **intake** — that an employee email actually *feeds* the custom system. Added 3 tests to `src/workflows/__tests__/custom-availability-magic.test.ts` (now 10): an "until \<date\>" email → date-limited pending (`custom_end_date` set, temporary framing in the confirm, permanent table untouched); a plain change → `custom_end_date` null; a non-date `end_date` value → ignored. Made the file's `withAnthropicRetry` mock controllable to drive `parseAvailabilityIntent`.
- **State:** Aegis tsc clean; full suite **143/143 green**. Change is test-only (no production code touched) on a working copy — needs Alexander to push the branch + open a PR, then one live sandbox smoke (employee → manager approve) to flip #13 to fully DONE. Feature code itself is already deployed.

### 2026-07-13 (session 12) — D19 FIXED: coverage sessions are now per-call-out (the last functional bug).

**The bug:** coverage sessions were keyed by COMPANY (`coverage_session:<company_id>`), so a second employee calling out **deleted** the first manager's session. The manager's "show me more names" thread on the first call-out went dead the instant a second came in. (The schedule was always correct — each `ActiveOutreach` is self-contained and carries its own call-out info — so no employee was ever misled; the damage was confined to the manager's control of the first call-out.)

**The fix — call-outs are now first-class objects:**
- Every `CoverageSession` gets a unique `session_id` and its own stored row (`coverage_session:<session_id>`). Two (or more) coexist without collision.
- `ActiveOutreach` and the `coverage_batch` decision token both carry `session_id`, so an employee's acceptance / a manager's button resolves the EXACT call-out — `getCoverageSessionById` replaces the old "the manager's session" lookup on both employee-accept paths and the email-button path.
- New `routeManagerCoverageReply` (the single router entry): lists a manager's open, reply-able sessions; 0 → fall through; 1 → route it (unchanged UX); >1 → match the reply against each call-out's absent-person/shift, and if still ambiguous, **ask which one** rather than guess. A reply that's itself a fresh call-out starts a new flow.
- Storage helpers reworked: `listActiveCoverageSessions`, `getCoverageSessionById`, `clearSessionById`, and `clearSession(session)` (was `clearSession(companyId)`, which is what made it delete the wrong thing). All ~9 clear sites updated to pass the session object. The timeout scheduler's local copies key by `session_id` too (with legacy per-company fallback). Legacy pre-D19 rows still resolve via fallbacks, so nothing in flight breaks on deploy.
- **This is the groundwork Alexander flagged for the employee-initiated call-out feature** — that feature *generates* concurrent call-outs, so it depended on this. Recommended sequence: D19 (done) → ship contest → build the employee route (framed as a same-day time-off request that also triggers coverage, so it doesn't fork the workflow).

- **State:** `tsc` clean, **253 tests** (was 247; +6 D19 regression tests with a driveable in-memory `aegis_memory` store proving two sessions coexist and each resolves by id), engine smoke green. Files: `src/workflows/emergency-coverage.ts`, `src/router/intent-router.ts`, `src/webhooks/decision.ts`, `src/scheduler/coverage-timeout.ts`. **No migration.**

**ROADMAP AFTER THIS: no known functional bugs remain.** Left: D22 (Soteria memory written from an LLM tag with no confirmation — low blast radius, no engine reads it), D14 (dead override code — `events.shift_overrides` NULL everywhere), the undo button (UI), and the from-scratch-tenant test (the real repeatability proof; do before Telnyx). Then SMS re-verification once A2P clears.

### 2026-07-13 (session 11) — D11 RESOLVED by removal: partial_shifts + conflict_resolution pulled from every user surface.

Alexander's call: neither will come up often, and shipping a rule the engine ignores is the exact "manager sets something that silently does nothing" failure. **Removed from every surface a user touches; scaffolding kept for a future add-on.**
- **Homebase Rules UI** — both entries removed from `CATEGORY_LIST` (`lib/rules/categories.ts`), so the cards no longer render. `categorizeByKey`, the `formatPolicySummary` cases, and the modal components (`ConflictResolutionModal`, `PartialShiftsModal`) are LEFT as dormant scaffolding.
- **Soteria** — removed from the policy vocabulary in `soteria/route.ts`; prompt now tells her they're not supported and to decline if asked.
- **Aegis email** — `coercePolicyWrite` now REFUSES both keys with a plain-English "not available yet" rather than writing an inert row.
- **Parser scaffolding kept** — `PARTIAL_SHIFTS_KEYS` / `CONFLICT_RES_KEYS` still recognised by `constraints/parser.ts` (so legacy rows parse without noise), and `EngineSettings.partialShiftsAllowed` / `.conflictResolution` still exist but are read by nothing. Re-enabling = wire the reader, not rebuild the plumbing.
- **To re-enable later:** restore the two `CATEGORY_LIST` entries, restore the Soteria vocabulary lines + the `coercePolicyWrite` branches, and add the engine reader. Comments at each site say so.
- Test flipped: `policy-write.test.ts` now asserts both keys are REFUSED. Aegis `tsc` clean, **247 pass**. Homebase `tsc` clean.

**D11 is now CLOSED** (resolved by removal). Partial shifts remains a possible future feature — real engine work (slot splitting, partial assignments, wage/coverage), tracked as a side project, not a hole.

### 2026-07-13 (session 10) — D8, D15, D16, D18 fixed in BOTH channels. D17 closed as not-a-bug.

**D8 — the last channel-parity gap. A manager could set a rule in Homebase that they could NOT set through the AI employee they're paying for.** `employee_conflicts` ("never schedule these two together") was settable in the UI and via Soteria, but **not by emailing Aegis**.
- New `banned_pair` entity + `handleBannedPairEdit` (needs its own parse — it names **two** employees, so it can't ride the single-entity edit path). Classifier examples added so the intent is actually recognised.
- Writes the **same `employee_conflicts` table** the UI and Soteria write and the engine reads (hard `never` block at build + swap cohabitation; soft `avoid` flag on swap approval, per D13). **A new channel, not a new concept — it writes nowhere else.**
- Unordered-pair matching (A,B ≡ B,A) so it can neither duplicate a rule nor fail to delete one. Existing pair → severity is **updated**, not duplicated.
- **Severity defaults to `never`** unless the manager's wording is clearly hedged ("try not to", "if possible"). Rationale, stated in the prompt: over-restricting is visible and trivially undone; under-restricting **silently** puts two people together the manager wanted apart.
- Name resolution refuses to guess: an ambiguous name returns null rather than picking one of two people when setting a rule *about* them.

**D15 — deleting an employee left their rules behind. FIXED IN BOTH CHANNELS.** The cascade covered `availability` only. Their approved **time-off** kept being loaded by `to-window.ts` (a ghost employee still affecting coverage maths) and a **banned-pair** rule kept being enforced about someone who no longer existed. Now cascades to `availability`, `custom_availability`, `time_off_requests`, `employee_conflicts` (**both** id columns) — dependants first, employee **last**, so a failure part-way through never leaves the person deleted but their rules alive.

**D16 — an employee could be unschedulable for their own job. FIXED IN BOTH CHANNELS.** The engine matches on `qualified_roles` (Rule 0b); `primary_role` only breaks ranking ties. So "make Jordan a Headguard" set the title and left the engine **refusing to schedule Jordan as a Headguard** — with a gap reason saying *"not qualified"* about the exact role written on their record. **Healed rather than rejected:** a manager promoting someone plainly means they can work the role, so it's added to `qualified_roles`. **Aegis had the identical hole** — fixing only Soteria would have recreated the very disconnect we're eliminating.

**D18 — coverage left the wage estimate lying.** Emergency coverage rewrote `schedules.data` but never recomputed `staffing_report.estimated_wages`, so after a call-out the manager's cost figure described a schedule that no longer existed — and the coverer may earn a different rate than the person they replaced. Both swap paths already did this; coverage was the odd one out. Also carried `accepted_roles` through `findShiftInfo` so coverage can't silently re-narrow to a single role (Rule 0b).

**D17 — CLOSED, NOT A BUG.** The register said `time_off_requests.decided_by` is "never written". It **is** written wherever we legitimately know who decided: `lib/aegis-actions/dispatcher.ts` writes it from the token's `issued_to_user_id`. It's `NULL` only on the older **shared** magic-link, which isn't tied to one manager — and **inventing an attribution there would be worse than an honest null**. `decided_at` + the activity log still capture the decision.

- **State:** Aegis `tsc` clean, **246/246**, engine smoke green. Homebase `tsc` clean.

**STILL OPEN after this (deliberately, with reasons):**
- **D19 — concurrent call-outs.** Two employees calling out at once creates two coverage sessions for the same manager, and `storeSession` deletes the company's existing session before inserting. **The schedule still ends up correct** (each `ActiveOutreach` is keyed per-employee and carries its own `callout_employee_id` + `shift_info`, so whoever accepts fills the right shift) — what breaks is the manager's session state on the FIRST call-out. The proper fix keys sessions by id and **routes the manager's reply by which session contains the employee they named**; it touches ~8 `clearSession` call sites. **Not rushed at the tail of a long session — a half-done concurrency fix is worse than a known one.**
- **D11 — `partial_shifts_allowed` / `conflict_resolution_preference` are settable and read by NOTHING.** Alexander's call to defer. Still a live Rule 0 violation. Build them or strip them.
- **D22 — `soteria_memory` is written straight from an LLM `<memory>` tag** with no confirmation card. Low blast radius (it only shapes Soteria's own conversational context — no engine reads it), but it is the one Soteria write with no human gate.
- **D14 — `applyShiftOverrides` / `events.shift_overrides` is dead code.** The column is NULL on every row; the function is inert. Cleanup only — it's woven through the manager email and staffing report, so it deserves its own pass.

### 2026-07-13 (session 9) — RULE 0b established. D10 finished properly. Client names ripped out of code.

**Alexander stopped the session and was right to.** I had reported D10 as fixed; it was fixed in **2 of 8 places**. He asked the question that caught it: *"anytime a dual requirement is added to a shift, it should update everywhere automatically — right?"*

**The real diagnosis.** *"Can this employee work this slot?"* was answered in **eight** places, each with its own copy of the logic. **Six compared against a single role string.** So a manager configuring *"Lifeguard **or** Headguard"* got: the engine **scheduling** a Headguard onto the shift, the swap workflow telling that same Headguard they were **"not qualified"** for it, the time-off simulator warning about coverage gaps that didn't exist, and the Homebase gap-resolver **hiding qualified staff from the manager trying to fix the gap.** The column existed, the UI wrote to it, and seven of eight readers ignored it.

**This is Rule 0's twin, for LOGIC instead of DATA** — now written into the contract as **Rule 0b: one question, one function.** Duplicated logic rots exactly like duplicated data.

**Fixed — every qualification decision in the product now routes through `src/lib/qualification.ts`:**
- `engine/eligibility.ts` (build) · `workflows/schedule-build.ts` (cascade pool **and** gap reasons) · `lib/schedule-simulator.ts` (time-off coverage — **both** loops) · `workflows/shift-swap.ts` (`validateSwap` **and** `buildSwapCandidates`) · `workflows/emergency-coverage.ts` · Homebase `GapResolverPanel.tsx`.
- **Verified: zero remaining single-role qualification checks in Aegis** (grep-clean; the one surviving `qualified_roles.includes` is onboarding adding a new hire's own role to their own list, which is correct).
- `accepted_roles` is now carried on **`ScheduleAssignment`** and **`ScheduleGap`**, so downstream workflows read the same truth the engine used instead of re-deriving it. Both **optional** — schedules published before this parse fine and fall back to `[role]` (exactly the old behaviour, never "nobody qualifies"). Swap paths additionally fall back to a **DB lookup** (`resolveAcceptedRoles`) so even a legacy schedule gets the manager's real configuration.
- **Nothing is client-specific.** No role vocabulary, no fixed count, no assumption about what a business calls its jobs. Tests cover a country club, a restaurant kitchen and a hotel front desk.

**HARDCODED CLIENT NAME REMOVED — this was a hard blocker on selling.** `employee-onboarding.ts` had *"Welcome to Watermark — Let's get you set up"* and *"Aegis — Watermark Country Club"* as literal email subjects. **Every future client's employees would have been welcomed to Watermark Country Club.** A `loadCompanyName()` helper already existed 200 lines above it. Now driven from the client's own `companies` row. Contract corollary added: **if you are typing a client's name into a string, it is a config value.** Grep-verified: zero hardcoded client references left in Aegis source.

- **State:** Aegis `tsc` clean, **246/246** (was 228), engine smoke + max-consecutive harnesses green. Homebase `tsc` clean. **18 new tests**, including a **cross-workflow agreement test** that asserts all six workflows return the SAME answer for the same employee and shift — if any of them ever disagrees, that test fails.

### 2026-07-13 (session 8) — D10: the engine was ignoring a role the manager configured. LIVE AT WATERMARK.

**Rule 0, inverted.** The earlier violations were *hidden data the manager couldn't see*. This is the mirror image: **visible data the engine ignored.**

**Watermark's `Flex` shift is configured to accept "Lifeguard OR Headguard".** The manager set that deliberately, in the shift box. The engine read only `shift_requirements.role` — the *first* accepted role — so **a Headguard could never fill that slot.** The build reported a **gap** while a qualified Headguard sat available and unscheduled. No error, no warning; the shift just quietly went short and the manager's stated intent was discarded.

- **Fix:** `CanvasSlot` now carries `accepted_roles`; eligibility uses new `isQualifiedForSlot()` (qualified for **any** accepted role). Ranking still **prefers** `role`, so single-role slots — every other Watermark requirement — behave exactly as before. Gap copy uses `slotRoleLabel()` so the manager reads *"Lifeguard or Headguard slot unfilled"*, not a misleading *"no Lifeguard available"* when a Headguard would have done.
- **Emergency coverage had the SAME blindness** and is fixed in the same pass (`shiftAcceptedRoles()`). Leaving it would have meant the schedule builder and the call-out workflow **disagreeing about who is qualified for the very same shift** — precisely the channel-disconnect this whole audit exists to kill.
- **Event shifts** deliberately do NOT inherit the wider set: an event spec names one role explicitly ("4 Lifeguards on the Afternoon"), so `accepted_roles: [ro.role]`. Inheriting the template's wider list would quietly let someone else fill a slot the manager explicitly asked a Lifeguard for.
- **`src/db/types.ts` corrected** — it never declared `accepted_roles` (the documented "types file is incomplete" trap) and still declared the four dropped mirror columns. `shift_requirements` now types as `{ id, company_id, shift_type_id, role, accepted_roles, required_count }`, matching the live schema exactly.
- **State:** `tsc` clean, **228/228** (was 219), engine smoke + max-consecutive-days harnesses green. 9 new tests pin both directions — a wider set must be honoured, and someone qualified for *none* of the accepted roles must still be rejected.

**D12 — `doubles_policy` now means something. FIXED.** It had three values but only two behaviours: the build treats `emergency_only` as `never` (correct), and **emergency coverage ignored the policy entirely** — always offering "Already Working" staff. So `never` and `emergency_only` were **identical everywhere**.
- **Alexander's call: `never` HARD-EXCLUDES already-working employees, even in an emergency.** Implemented.
- **Safety valve (flagged to him, he can veto):** a strict reading can leave a shift with **no candidates at all** when the only able person is already on. Handing the manager an empty list and an uncovered shift is worse than the rule it protects. So the exclusion is absolute **unless** it would leave zero candidates across every tier — then they're surfaced, explicitly labelled *"your rule says never schedule doubles… picking one means breaking that rule — your call."* `never` still means never by default; it just never silently hands back nobody.
- Read via `parseConstraints` — the **canonical** `policy_value_json`, not the display text (D1's lesson applied).

**D11 — accepted as KNOWN INERT, deliberately not fixed (Alexander's call).** `partial_shifts_allowed` and `conflict_resolution_preference` are parsed into `EngineSettings` and **never consulted anywhere**. A manager can set either one; Soteria confirms it; Aegis confirms it by email; **the schedule comes out identical.** Logged and moving on for the contest. ⚠️ **This is a live Rule 0 violation and must not be allowed to rot** — it is exactly the *"manager sets a rule that silently does nothing"* failure. Fix is either build them (partial shifts = real engine work: slot splitting, partial assignments, wage/coverage implications) or strip them from every manager surface. **Revisit right after the Watermark contest.**

### 2026-07-13 (session 7) — RULE 0 COMPLETE (columns dropped) + D6/D7 (Soteria stops claiming Aegis's job)

**RULE 0 for shifts is DONE — the copies no longer exist.** Verified live: `shift_requirements` is now `id, company_id, role, required_count, shift_type_id, accepted_roles` — **no shift attributes at all**, and `shift_type_id` is `NOT NULL`. A shift is defined in exactly one place: the box the manager edits. *The schema now enforces the rule; it is no longer a convention someone can break.*
- Found and fixed **five** remaining touchpoints, including **two writers I'd missed** (`apply_setup_plan` stamped the copies in two places). The worst reader was **`ManualScheduleBuilder`** — it used the stale hours to **write assignments into the schedule**, so a manager hand-building a week baked the wrong times into what employees received. `GapResolverPanel` (the employee "you've been added to the AM shift (11:30–15:30)" email) and `TimeOffTab` (partial-day picker) now join `shift_types`.
- **Deleted the D4 cascade I added earlier the same day.** It was the right patch for a schema that shouldn't exist — but keeping a hidden duplicate *honest* is still keeping a hidden duplicate. A comment now warns that re-adding a cascade there means someone re-introduced a copy; delete the copy instead.
- **Template recorded in the contract** for the next Rule 0 violation: (1) readers → the manager's row, (2) stop the writers, (3) enforce the FK, (4) **drop the copy** — don't settle for syncing it.
- **Housekeeping owed:** Aegis's generated `src/db/types.ts` still *declares* the four dropped columns. Nothing reads them (tsc clean), but the types file now lies about the schema — regenerate.

**D6 — Soteria was claiming Aegis's job, and the cause was a shared list.** `capabilities.ts` describes the **product** (Soteria + Aegis) — correctly. But it was injected into her system prompt under the heading *"WHAT YOU CAN DO FOR THIS USER"*, so the model read *"approve or deny time-off"*, *"arrange emergency coverage"* and *"swap a shift"* as **her** abilities. She has **no executor for any of the three**. A manager who asked hit a dead end or watched her improvise.
- **We did not build those actions. We stopped claiming them** — per Alexander: *"Soteria is just for back-end stuff and configuring information inside Homebase so Aegis works correctly… the language should make it explicitly clear that she can ASK AEGIS to do those things, not that she does them for you."*
- New **`soteriaScopeSection()`** injects a **hard boundary**: what she performs herself, what she may **ASK AEGIS** to do, and what is **Aegis's alone** — with the exact hand-off script ("That one's Aegis's job — email him and say 'approve Jordan's time off for the 14th'…"). Deliberately blunt: an LLM improvises around a soft hint. The product list is kept but re-labelled *"WHAT THE PRODUCT CAN DO… NOT you alone."*
- The boundary is enforced by **vocabulary, not just prompt**: there is still no executor for those three, so even a jailbroken model has nothing to call.

**D7 — Soteria can now ask Aegis to send the schedule.** `distribute_schedule` added as a **thin pass-through** to the same `/internal/distribute-schedule` endpoint the Homebase Distribute button already uses — no second implementation, no fabricated message. **It emails every employee (~30 at Watermark)**, so: the schedule is re-looked-up scoped to the company (never trusts the LLM's `schedule_id`), explicit manager confirmation is required, and Aegis's existing `already_distributed` guard means an accidental second confirm **cannot** spam the team — Soteria reports it was already sent and does nothing. All copy reads *"I've asked Aegis to…"*, never *"I've done it."*
- Also fixed stale UI copy promising *"you'll receive a **text** when it's ready"* — SMS is disabled under EMAIL_ONLY, so that was a promise we don't keep.

- **State:** Homebase `tsc` clean. Files: `src/lib/soteria/capabilities.ts`, `src/app/api/soteria/route.ts`, `src/app/api/soteria/execute/route.ts`, `src/components/layout/SoteriaPanel.tsx`, `src/components/schedule/{GapResolverPanel,ManualScheduleBuilder}.tsx`, `src/app/(app)/data/tabs/{ShiftRequirementsTab,TimeOffTab}.tsx`.

### 2026-07-13 (session 6) — D20 + D21 + D13: the employee-facing bugs (contest prep)

Chosen ahead of the remaining Rule-0 and Soteria work because these are what 30 Watermark employees will actually touch in the upcoming test-and-win contest.

**D20 — availability writes could silently corrupt themselves.** `writeEmployeeAvailability` (used by onboarding AND the manager availability edit) deleted by `employee_id` with **no `company_id` predicate**, and — the real defect — **neither the DELETE nor the INSERT checked for an error.** Failure modes: a failed DELETE + successful INSERT leaves the employee holding **both** their old and new availability (overlapping windows → the engine thinks they're free on days they just told us they aren't); a successful DELETE + failed INSERT leaves them with **no availability at all**. Both paths then replied *"got it, saved."* Now scoped by `company_id` and both writes throw so the caller can tell the truth. *(Note: the missing `company_id` could not actually cross tenants — `employee_id` is a globally-unique UUID — so this is defence-in-depth. I overstated it as a live cross-tenant leak earlier; correcting that here.)*

**D21 — a new hire could not ask for a half day.** Onboarding's `claudeExtractDates` extracted only `start_date`/`end_date`, and `time_off_requests.time_off_type` **DEFAULTS to `'full_day'`**. So *"I need the afternoon of July 20 off"* silently became a **whole day** off — the engine then refuses to schedule them at all that day and the coverage simulator counts them fully unavailable. Over-blocking a brand-new employee's first request. Onboarding now extracts `time_off_type` / `period_label` / `start_time` / `end_time` and resolves the window through the **same exported `resolvePartialWindow` the real time-off flow uses**, so the two surfaces cannot drift on what "afternoon" means. An unresolvable partial falls back to `full_day` rather than dropping the request (losing it is worse than over-blocking, and the manager still sees it).

**D13 — "avoid" pairs were invisible to every swap path.** `loadHardConflicts` filtered `.eq('severity','never')`. A manager could mark two people as "try not to schedule them together" and a swap would cheerfully put them on the same shift **without a word**. Now both severities load; `bannedCohabPartnerName` (hard) and the new `avoidCohabPartnerName` (soft) split them, and the manager's approval email carries a softer heads-up for 'avoid'. **Flag-don't-force is preserved — neither blocks the swap; the manager keeps final authority.** A row with no severity is treated as HARD: over-warning beats under-warning. 5 regression tests pin both failure modes (an 'avoid' must not become a hard ban, and must not vanish).

- **State:** `tsc` clean, **219/219** (was 214), engine smoke green. Files: `src/workflows/employee-onboarding.ts`, `src/workflows/shift-swap.ts`, `src/workflows/__tests__/shift-swap.test.ts`. **No migration needed.**
- **D19 deliberately NOT bundled.** Two employees calling out at once creates two coverage sessions for the same manager, and `storeSession` deletes the company's existing session before inserting — so the first manager session's state is lost. **Re-checked the blast radius: the schedule still gets corrected properly**, because each `ActiveOutreach` record is keyed per-employee and carries its own `callout_employee_id` + `shift_info`, so an employee who accepts still fills the right shift. What breaks is the manager's session UX (asking for "more names" on the first call-out). No employee is misled, no data goes wrong. The correct fix requires Aegis to **ask the manager which shift they mean** when they reply with two sessions open — a real UX change, not a key rename. It gets its own pass rather than being rushed.
- **Still open for the contest:** the last Homebase readers of the shift copies (esp. `GapResolverPanel`, which is what emails an employee *"you've been added to the AM shift (11:30–15:30)"* — the final path by which a wrong hour reaches a human).

### 2026-07-13 (session 5) — RULE 0 established; Aegis engine can no longer read a hidden copy

**Alexander set the governing rule** (now Rule 0 at the top of `docs/07_Data_Contract.md`, above the other three): *"Whatever the manager sees, updates, and saves needs to be what the entire system uses. There shouldn't be anything saved anywhere other than what the manager directly sees and interacts with."* Config-over-code multi-tenancy is meaningless otherwise — a hidden copy the manager never touched is, by definition, not the client's data.

**Applied to shifts.** The manager defines a shift's name/hours/days once, in the shift box (`shift_types`). `shift_requirements` should say only *"this shift needs N of role X."* It was also storing copies of `shift_name`/`start_time`/`end_time`/`days_active`, stamped at creation (`ShiftRequirementsTab.tsx:229-232`) and never shown to the manager again. They drifted (D4).

**What changed (Aegis) — the engine is now structurally incapable of reading the copy:**
- New **`CanvasRequirement`** type (`lib/engine/canvas.ts`): `{ id, shift_type_id, role, required_count, days_active }` — and `days_active` there is an **engine-internal date-scope stamp**, not a DB read. It carries **no shift attributes at all**. `buildCanvas` now takes this instead of a raw `ShiftRequirement` row, so a copied column physically cannot cross into the engine. The smoke harness pins the signature — widening it back to `ShiftRequirement` is now a compile error.
- **Removed the `req.shift_name === st.name` fallback** in both `canvas.ts` and `schedule-build.ts`. Requirements match their shift by **`shift_type_id` only**. Matching staffing to a shift by a copied name string meant a rename could silently detach a shift's own staffing.
- **`schedule-simulator.ts` has no fallback left** — it reads name/hours from the `ShiftType` or skips the requirement loudly. It can no longer produce a coverage verdict from stale hours.
- `tsc` clean, **214/214**, engine smoke harness fully green (including the stale-`days_active` case, which now proves the engine ignores the copy).

**⚠️ MIGRATION REQUIRED BEFORE MERGE — `~/Desktop/Link_Shift_Requirements_To_Shift_Types.sql`.** The engine now matches by id only, so an **unlinked** requirement is skipped. **Quria Sandbox has 2 unlinked rows** (`AM`/`PM` Lifeguard); Watermark has 0. The script links them (verified lossless — both match a shift type by exact name and their hours already agree) and then sets `shift_type_id NOT NULL` so an unlinked requirement can never be created again. **Deploy the code without this and sandbox builds lose those slots.**

**Still to do to finish Rule 0 for shifts** (the columns are still there, just unread by the engine):
1. **Homebase — stop writing the copies:** `ShiftRequirementsTab.tsx` insert, and `soteria/execute` `add_role_requirement`. Also **remove the D4 `update_shift_type` cascade** — it keeps the copies in sync, which becomes pointless (and will error) once they're dropped.
2. **Homebase — move the 3 remaining readers onto `shift_types`:** `GapResolverPanel.tsx:139` (this one builds the employee-facing *"you've been added to the AM shift (11:30–15:30)"* message — it is the reason an employee could be told the wrong hours), `ManualScheduleBuilder.tsx:169`, `TimeOffTab.tsx:305`.
3. **Then DROP** `shift_name`, `start_time`, `end_time`, `days_active` from `shift_requirements` and regenerate `src/db/types.ts`.

**Separately noted, not bundled:** `applyShiftOverrides` + `events.shift_overrides` (D14) is the *other* Rule 0 violation — a column the manager never sets. It is now **dead code** (the column is NULL on every row after the undo), so it's a cleanup, not a live bug. It's woven into the manager email and the staffing report, so it gets its own pass rather than riding along with this one.

### 2026-07-13 (session 4) — D4 + D5 FIXED: cascade bugs. D4 was ALREADY DRIFTED IN PRODUCTION.

Both are the same disease: an edit that changes one table and forgets the others that depend on it. A role is a free-text string matched by exact equality across **five** tables; a shift's hours live canonically in `shift_types` but are **copied** onto `shift_requirements`.

**D5 — a role rename silently orphaned its wage rate and every veteran rule.** `update_role` cascaded to `employees.primary_role`, `employees.qualified_roles`, `shift_requirements.role`/`accepted_roles` — but **not `wage_rates.role`** and **not `shift_experience_rules.role`**. Rename "Lifeguard" → "Guard" and the Lifeguard pay rate points at a role that no longer exists, and every "Saturday nights must be all veterans" rule scoped to Lifeguard **stops being enforced**. No error. No flagged gap. The schedule just quietly gets worse. `delete_role` didn't check either table, so a role could be deleted out from under its own wage rate.
- **Fix:** cascade extended to both tables; `delete_role` now blocks and names the offending references.
- **Also corrected a dangerous comment.** The cascade labelled `shift_requirements.role` a *"legacy column"*. It is not — **it is the column the schedule engine matches on** (eligibility compares `employees.qualified_roles` against `slot.role`). `accepted_roles` is the inert one (D10, Role Groups unbuilt). Anyone trusting that comment and dropping the `role` cascade would have broken every build.

**D4 — the shift-hours copies are STALE IN PRODUCTION RIGHT NOW.** `update_shift_type` never updated the `shift_name`/`start_time`/`end_time`/`days_active` copies on `shift_requirements`. I checked the live data rather than assuming: **Watermark has 8 of 12 requirement rows disagreeing with their shift type.** Worst case `Day / Greeter` — requirement says **12:00–18:00**, the actual shift type is **11:00–19:30**. Two and a half hours.
- **The schedule engine was never affected** — `canvas.ts` builds slots from `shift_types`. So schedules are correct.
- **But two live readers were using the stale copy.** (1) The Aegis **time-off coverage simulator** (`lib/schedule-simulator.ts`) used `req.start_time/end_time` to decide whether approving a time-off request opens a coverage gap → it could **clear a request that actually leaves a shift short**, or flag one that doesn't. That is a workflow Watermark employees use. (2) Homebase **`GapResolverPanel`** builds the employee-facing *"you've been added to the AM shift (11:30–15:30)"* message from these columns → **told the employee the wrong hours.**
- **Fix (three parts):** (1) **Aegis** — `schedule-simulator.ts` now resolves start/end/name from the parent `shift_type`, falling back to the local copy only for legacy rows with no `shift_type_id`. Same rule the `days_active` gate already used. This is the canonical read and it makes the simulator correct **regardless** of the mirror. (2) **Homebase** — `update_shift_type` now cascades to the mirrors, keeping every remaining mirror-reader honest. (3) **`~/Desktop/Repair_Shift_Requirement_Mirrors.sql`** repairs the rows that are already wrong.
- **Why keep the mirror at all** (it violates "one concept → one canonical column"): the columns are **NOT NULL** and several Homebase UI components read them, so they can't be dropped today. The engine and simulator now read canonical; the mirror is display-only and kept in sync. **End state: drop the mirror columns** — logged.

- **State:** Aegis `tsc` clean, **214/214**. Homebase `tsc` clean. Files: Aegis `src/lib/schedule-simulator.ts`; Homebase `src/app/api/soteria/execute/route.ts`. **Two SQL scripts to run** (`Repair_Shift_Requirement_Mirrors.sql`).

### 2026-07-13 (session 3) — D2 FIXED: a swap could be "approved" while the schedule never changed

**⚠️ SHIP ORDER: run `~/Desktop/Add_Swap_Requests_Schedule_Id.sql` BEFORE merging this branch.** The code writes `swap_requests.schedule_id`; without the column the write fails.

**The bug was worse than the register said.** In `decision.ts` the approve path (a) set `status='approved'` FIRST, (b) applied the swap inside `if (schedRow && receiver)`, and (c) emailed **both employees "Your shift swap has been approved!" OUTSIDE that guard.** So with no published schedule covering the date — or an assignment that didn't match — the row read `approved`, both people were told it was done, and **the schedule was never touched.** The employee who believed they were covered simply doesn't show up. There were three silent exits (schedule not found, no matching assignment, unchecked write), and the no-matching-assignment path *wrote the unchanged assignments back* — a **no-op write that was indistinguishable from success**.

- **Fix — the schedule write is now authoritative.** `executeScheduleSwap` / `executeScheduleTrade` return `SwapApplyResult` (`{ok:true, schedule_id}` | `{ok:false, code, reason}`) instead of `void`. **A caller may not announce an approval it did not get.**
  - `decision.ts` approve path: resolve receiver + published schedule → **apply the change** → only if `ok`, write `status='approved'` **together with `schedule_id`** (the receipt) in one update → consume tokens → notify. On any failure the row **stays `pending_manager`**, nobody is notified, and the manager sees a page saying exactly why (e.g. *"there's no published schedule covering that date yet — publish the week, then approve again"*). Deny path unchanged in behavior (no schedule claim to earn).
  - Auto-approve path (`shift-swap.ts`): applies first; if it can't land, records `pending_manager` and sends **honest** messages ("agreed to cover, but it isn't on the schedule yet — your manager will confirm") instead of a false confirmation.
- **New column `swap_requests.schedule_id`** (nullable uuid → `schedules.id`, indexed). It's the receipt: `approved` is now only ever written alongside the schedule it landed on. The migration's final query lists historical `approved` rows with `schedule_id IS NULL` — **those are the swaps that may have hit this bug in the wild; worth eyeballing against the live schedule before the next distribution.**
- **7 new tests** (`swap-apply-authority.test.ts`) with a driveable Supabase stub. They assert the failure paths return `ok:false` **and write NOTHING** — the no-op-write regression is pinned explicitly.
- **State:** `tsc` clean, **214/214** tests (was 207). Files: `src/webhooks/decision.ts`, `src/workflows/shift-swap.ts`, `src/workflows/__tests__/swap-apply-authority.test.ts` (new).
- **Known gap (logged, not built):** there's no *deferred* application — a swap agreed before the week is published can't auto-land when it publishes. It parks at `pending_manager`. That's honest but manual; worth a follow-up.

### 2026-07-13 (session 2) — D1 + D3 FIXED. The `policies` table is TWO tables wearing a trench coat.

**D1 (P0) — manager email policy edits were landing in a column the engine doesn't read.** `executeEdit` did `update({[field]: value})`, so "set doubles policy to never" wrote `policy_value` (text) and left `policy_value_json` — the ONLY column `parseConstraints` reads — stale. Manager told "done"; engine kept enforcing the old rule.

**The fix surfaced something much worse, and it would have made the obvious fix a NEW P0.** `policies` has **two reader families with opposite conventions**:
- **Engine family** (`lib/constraints/parser.ts`): canonical = `policy_value_json`. `policy_value` display-only, `policy_type` ignored.
- **Time-off family** (`lib/time-off-policies.ts`): canonical = **`policy_value` TEXT** (read via `parseInt10`), and **`policy_type='time_off'` is LOAD-BEARING** — the loader `.eq()`s on it, so losing it means the row is never SELECTed at all.

Had I "fixed" D1 by converting all policy writes to JSON + a human display string, a manager emailing *"set the notice period to two weeks"* would have written `policy_value='Two weeks'` → `parseInt10` → `NaN` → `null` → **the time-off rule silently switches OFF.** Closing one P0 by opening another.

- **Fix:** new `src/lib/policy-write.ts` → `coercePolicyWrite(policy_key, raw)`. Resolves the family, validates, and returns the **exact column patch that the reader for that key actually consults**. It **imports the key sets from `constraints/parser.ts`** (now exported) so the writer and reader physically cannot drift. It refuses: keys no reader consults (configured-but-inert), values the parser would silently drop as out-of-range (e.g. `max_consecutive_days_worked = 9`), English numerals ("two weeks" — guessing a number that then gets *enforced* is unacceptable), and free-text `attribute_mix`/`gender_requirement` (a structured object; guessing it silently changes who's required on every shift → bounced to Homebase).
- **13 new unit tests** and they're the good kind: they take the patch `coercePolicyWrite` produces, build the row, and push it through the **REAL `parseConstraints`** to assert the engine reads the value back. A test that only checked the patch shape would still pass if the engine ignored the column.

**D3 (P0, security) — the email-edit column was never allow-listed.** `pending.field` came from the LLM straight into `.update({[field]: value})`, and `create_fields` was arbitrary LLM JSON. Any column on an allowed table was writable — **including `employees.company_id`, a cross-tenant write.** Only `shift_requirements.days_active` was ever blocked. Fixed with `EDITABLE_COLUMNS` / `CREATABLE_COLUMNS` allow-lists (verified against `information_schema`), enforced **twice** — at confirmation time and again at the write — with `company_id` always forced from the verified contact.

**D3b (found while fixing D3) — every write in `executeEdit` was unchecked.** `insert`/`update`/`delete` never inspected `error`, so a DB-rejected write fell through to the *"Done — updated"* reply. Orphan output on the manager's most trusted surface. All three now throw, and the manager-facing reason is passed through instead of a generic dead end.

- **State:** `npx tsc --noEmit` clean. **207/207 tests pass** (was 194). Files: `src/lib/policy-write.ts` (new), `src/lib/__tests__/policy-write.test.ts` (new), `src/lib/constraints/parser.ts` (key sets exported — no behavior change), `src/workflows/operational-query.ts`. **Not yet pushed.**
- **Contract corrected:** `docs/07_Data_Contract.md` said *"`policy_value` is display-only and NEVER read by the engine"* — **that was wrong** and is now the "⚠️ The `policies` trap" section. The next agent self-briefs from that file; a wrong contract is worse than no contract.
- **Next:** D2 (a swap can be `approved` while the schedule never changes) — needs a `swap_requests.schedule_id` column, so it starts with a migration.

### 2026-07-13 — INCIDENT: the retracted backfill WAS executed against live data — undone; `event_shifts` is now the single source of truth

The `Backfill_Event_Shift_Overrides.sql` retracted in the 2026-07-12 entry below **was run** before the retraction landed. It populated `events.shift_overrides` (derived from `events.event_shifts`) on **3 events**.

- **Was the schedule wrong? No.** `applyEventShifts` runs POST-canvas and *rebuilds* the role group — it removes the existing slots for a role and creates exactly `roles[].count`. So it overwrites whatever `applyShiftOverrides` did PRE-canvas. Final head-counts were identical either way. **No live schedule was corrupted.**
- **But it still had to be undone — it planted a landmine.** Soteria does **not** sync `shift_overrides` (the dual-write was reverted, correctly). So a populated `shift_overrides` is a stale, unsynced mirror. If an event is later edited to a **time-only** stretch (no `roles`), `applyEventShifts` does **not** rebuild the role group — and the **stale backfilled head-count would silently persist**, overriding what the manager actually asked for. Second hazard: `applyShiftOverrides` **early-returns on the first event matching a shift-type name**, so two events on one date would silently drop the second's overrides.
- **Undo shipped:** `~/Desktop/Undo_Event_Shift_Overrides_Backfill.sql` — `set shift_overrides = null where shift_overrides is not null and event_shifts is not null`. **Safety proven before writing it:** a live count returned `has_shift_overrides=3, both_populated=3, legacy_overrides_only=0` → **nothing anywhere depends on `shift_overrides` alone**, so nulling it cannot lose information. The `where event_shifts is not null` guard preserves a hypothetical legacy-only event.
- **The bad file is neutralized** — `~/Desktop/Backfill_Event_Shift_Overrides.sql` overwritten with a RETRACTED / DO-NOT-RUN banner; the SQL body is gone so it cannot be re-run.
- **Push is safe.** Homebase `tsc` clean; only `src/app/api/soteria/execute/route.ts` + `CLAUDE.md` modified. Grep confirms **zero** writes to `shift_overrides` anywhere in Homebase (`SpecialNotesTab.tsx` declares it on a type but never renders it → nulling has no UI impact). `trigger_schedule_build` confirmed on `postToAegisInternal('/internal/build-schedule')`, not the fabricated SMS.
- **Contract:** `events.event_shifts` is CANONICAL; `events.shift_overrides` is LEGACY and **must stay NULL**. Never dual-write. Logged in `SCHEMA_DRIFT_LOG.md` (2026-07-13) and as **D14** in `docs/07_Data_Contract.md`. **Follow-up:** delete `applyShiftOverrides` from the Aegis engine so the column is unreadable, not merely empty.
- **Process lesson (the real one):** before asserting a column is unread, grep the whole engine for the column name — not just the file already open. The reader (`src/lib/engine/event-shifts.ts`) existed the entire time.

### 2026-07-12 — BUG-8 (Soteria: special-event schedule build) — TWO bugs, both fixed (IN REVIEW)

Watermark: a manager configured a special event with Soteria (extra lifeguards on a shift) — action cards + confirmations worked, event saved — then asked Soteria to build the schedule with it. The build card appeared, he pressed Confirm, and **nothing happened**. Two independent bugs:

- **(1) `trigger_schedule_build` was wired to a dead legacy path.** It FABRICATED an SMS to Aegis `/webhooks/sms`, using an `employees` row with `primary_role='Manager'` AND a phone on file to authenticate. Managers are `users`, so that employee row often doesn't exist → hard 400 ("No manager with a phone number on file"). Worse, the SMS webhook is now inert under EMAIL_ONLY and returns 200 regardless — so the old code could log `schedule_build_triggered` and report SUCCESS while doing nothing. **Fix:** use `postToAegisInternal('/internal/build-schedule', { company_id, target_week, veteran_preference })` — the SAME Bearer-auth'd endpoint the Homebase **Build** button already uses (proven path; no SMS, no manager-phone dependency). Now logs `schedule_build_triggered` with real schedule_id/filled/gaps, or `schedule_build_failed` with the reason.
- **(2) RETRACTED — I was WRONG; events were NEVER broken.** I initially claimed Soteria's events were invisible to the engine because the engine reads `events.shift_overrides` (via `applyShiftOverrides`) while Soteria writes `events.event_shifts`. **That was an incomplete read.** The full audit found `src/lib/engine/event-shifts.ts` → `applyEventShifts()`, called from `canvas.ts:116`, which reads **`events.event_shifts`** post-canvas and is a strict SUPERSET of the legacy column: it handles per-role count overrides (`stretch` + `roles[].count`, event-shifts.ts:116-131), time changes, AND brand-new one-off shifts (`mode:'add'`, :134-149). **Soteria's events were fully honored.** The `deriveShiftOverrides()` dual-write I added was therefore unnecessary AND an anti-pattern (mirroring one concept into two columns — the very drift disease). **It has been reverted, and the backfill SQL is retracted — DO NOT RUN IT.** Corollary: `events.shift_overrides` is **legacy** and should be retired in favour of `event_shifts` (logged as D14 in the new data contract).
- **Net: BUG-8 was ONE bug, not two — the build never fired.** The special-event configuration was always correct.
- **Not done (optional, per Alexander):** giving Soteria a `distribute_schedule` action card. Publish/Distribute already exists as a button on the schedule page, so this is a convenience, not a blocker. Would need an executor case (`postToAegisInternal('/internal/distribute-schedule')` + publish semantics) **and** an addition to the planner's action vocabulary in `soteria/route.ts`.
- **State:** Homebase tsc clean. Files: `src/app/api/soteria/execute/route.ts`. Deploys via Vercel.

### 2026-07-12 — BUG-7 (Homebase Soteria "All clear" + contradictory conflict text): LLM-hallucination, not a validator miss (IN REVIEW)

Watermark screenshot: `Soteria Review` showed a green **"✓ All clear"** header with Save enabled, directly above text *"The swap cannot be approved because Lucas Witham is on approved time off on 2026-07-15 … a direct time-off conflict."* First read: Soteria cleared a real TO conflict. **Verified against the live DB — the opposite is true:** Lucas Witham has exactly one record and **NO time-off of any status covering 2026-07-15** (his approved TO is Jul 12 and Jul 17–25; Jul 15 is a gap). So the deterministic **"All clear" was CORRECT**, and the scary line was an **LLM hallucination**.
- **Root cause:** `soteria-validate-schedule/route.ts` runs the deterministic `validateScheduleEdit` (authoritative, correct here) for hard rules and an LLM pass for soft warnings — but it (a) printed the LLM's free-form `summary` verbatim when there were no deterministic errors, so a hallucinated "cannot be approved" leaked under "All clear", and (b) the prompt asked the LLM to detect hard rules (TO/qualification/hours/pairs), which invited exactly this hallucination.
- **Fix (schedule route):** the verdict AND summary now come ENTIRELY from the deterministic checks (dropped `llmSummary`); the LLM is scoped to SOFT warnings only (prompt now says the hard rules are already validated deterministically and to never assert a TO/qualification/hours/availability/pair conflict). LLM output stays warning-only and never blocks or contradicts the verdict.
- **Audit of "any other Soteria check items":** `soteria-validate-assignment/route.ts` (used by GapResolverPanel) was WORSE — it computed the hard facts but let the LLM decide `valid` outright. Fixed: the hard verdict (`valid` + issues) is now recomputed deterministically from the loaded facts (qualification, availability-for-day, approved time off, max hours) and OVERRIDES the LLM; the LLM's text is kept only as soft suggestions. `validateScheduleEdit.ts` itself was audited and is correct/complete — unchanged.
- **State:** Homebase tsc clean. Files: `src/app/api/soteria-validate-schedule/route.ts`, `src/app/api/soteria-validate-assignment/route.ts`. Deploys via Vercel. **Correction for the record: the manager was NOT shown a validator that cleared a real conflict — the validator was right; the LLM narrative lied.** (If Lucas genuinely needed Jul 15 off, that TO was never entered — worth confirming with the manager, but it's a data-entry gap, not a Soteria bug.)

### 2026-07-12 — BUG-6 (bare-confirmation misrouting): verified core ALREADY FIXED; hardened the residual (IN REVIEW)

An external Cowork session (no repo mounted) produced a "BUG-6 fix packet" proposing a new `src/router/affirmation.ts` deterministic affirmation gate, from June 30 Watermark screenshots where a bare "Yes" confirming a TO returned "I don't have an active swap request pending for you." **Verified the packet against actual source — its central premise (affirmations routed by the stateless classifier) is NOT true of current code, so the packet was NOT applied.** What actually exists:
- `intent-router.ts` runs an **ordered deterministic pre-classification gate BEFORE the LLM** (lines ~155–198). For an employee, `getPendingTimeOff` is the FIRST check → `handlePendingTimeOffConfirmation` + EARLY RETURN. Store/lookup keys match (`aegis_memory` `source='pending_to:{employee_id}'`). The confirm handler matches "yes" via a **regex** and only consults the classifier to detect a *new* submit — so even a mislabeled bare "yes" is consumed correctly. **BUG-6 as diagnosed cannot reproduce for an in-window confirmation.** (Corroborated by the 2026-07-08 live TO test: request → confirm → created → approved.) The packet's guessed names were also wrong for this repo (`swaps`→`swap_requests`, memory shape/keys differ).
- **Real residual (the actual cause of that exact screenshot line):** the pending-TO had a **1-hour TTL**; a "yes" sent after expiry (or with nothing pending) falls through to the classifier, can be labeled `respond_swap_accept`, and hits `handleRespondSwap`'s fallback = the "no active swap request pending for you" line.
- **Targeted fix (adopted; ~12 lines, not the 200-line packet):** (1) `time-off.ts` pending-TO TTL **1h → 24h** so email-latency confirmations stay on the deterministic path; (2) `shift-swap.ts` `handleRespondSwap` now detects a **bare affirmation/negation** and replies gracefully ("nothing pending to confirm right now… just resend the details") instead of implying a phantom swap — content-bearing swap replies keep the original message. Trade-off noted: a longer TTL means an unconfirmed pending can nag on unrelated messages for up to 24h (BUG-5 territory; acceptable).
- **State:** tsc clean; **full suite 194/194** (added 2 `handleRespondSwap` regression tests). Working tree. The fresh live bare-"yes" repro was **queued but blocked by the SendGrid inbound backlog** (known infra lag — see Phase-7 notes), so it's proven by code + the earlier live test + the unit tests rather than a fresh round-trip.
- **Next:** apply doc edits to `EMAIL_WORKFLOWS_TRACKER.md` (BUG-6 = mitigated-at-core + residual-hardened, NOT the packet's framing) and `04_Aegis_Reference.md §1.6` (document the existing deterministic gate). BUG-5 (stale pending short-circuits a new submit / nags on unrelated messages) — **FIXED same day** in `handlePendingTimeOffConfirmation`: new submit replaces the pending (captures new dates); a clearly-different actionable intent abandons the unconfirmed pending and re-routes (pending cleared before recursive `routeIntent`; dynamic import breaks the cycle). tsc clean, 194/194. See `EMAIL_WORKFLOWS_TRACKER.md` BUG-5.

### 2026-07-05 — Email-only mode: SMS temporarily disabled across all workflows (IN REVIEW)

Per Alexander: until Telnyx/A2P registration completes, every workflow must run email-only and nothing live may break. Implemented as a **reversible flag, not a deletion** — flip `EMAIL_ONLY=false` (+ configure Twilio) to restore SMS exactly. Branch: working tree (to become `feat/email-only-mode`). **tsc clean; full suite 192/192 green with EMAIL_ONLY on AND off** (proves both the email-only path and clean reversibility).

- **Foundation.** `config/env.ts` adds `EMAIL_ONLY` (string→bool, **default true**). `messaging/sms.ts` `sendSms` returns false immediately when `EMAIL_ONLY` — the single hard choke point guaranteeing no SMS is ever sent. `webhooks/sms.ts` inbound handler short-circuits (200 + ignore) under `EMAIL_ONLY`.
- **Channel selection gated to email** at every proactive/single-channel send so phone-on-file employees are **emailed, not silently dropped**: `day-closure` (was SMS-first), `webhooks/decision.ts` (time-off + swap approve/deny notices), `emergency-coverage`, `shift-swap` (manager alert + receiver outreach), `time-off` (manager alert), `schedule-build` (distribution + change notices), `broadcast` (an "sms"/"both" broadcast now falls to email), `payroll`, and `employee-onboarding` (`useSms`). Pattern everywhere: `if (!env.EMAIL_ONLY && <phone condition>) { sendSms } else { sendEmail }`.
- **No-throw safety:** `time-off.sendDecisionNotification` previously threw if an employee had a phone but no email and SMS failed; under `EMAIL_ONLY` a phone-only/no-email employee is now logged + skipped (the decision already succeeded) rather than throwing.
- **Conversational replies unaffected:** `messaging/reply.ts` follows the inbound channel (always email now, since the SMS webhook is inert) and already has an email fallback — no gate needed.
- **Scope note:** 12 files, +62/−21. `reply()` and the onboarding email path already resolve to email; the SMS code is dormant, not removed. **Design intent (Alexander): the future SMS build reuses the email foundation with added complexity; re-address this roadmap once Telnyx clears.**
- **Deploy:** `EMAIL_ONLY` defaults on, so **no Railway env var is required** — merging is sufficient. To bring SMS back later: set `EMAIL_ONLY=false` and add Twilio/carrier creds.

### 2026-07-05 — #19 Onboarding: DEPLOYED + live-tested (Test A & B PASS) + design clarified

Merged the email-onboarding fixes (Aegis `fix/pre-demo-bugs`, Homebase `fix/soteria-override-and-rules`) → deployed (Railway + Vercel). Ran live email tests on the SANDBOX (Claude drove Riley's + Sam's Gmail; Alexander sent the manager triggers from `sandbox-mgr@`):

- **Test A — targeted onboarding (Riley), PASS end-to-end.** Manager "Onboard Riley Brooks" → `onboarding_started` → email-first welcome (branded, correct company name "Quria Sandbox", **no SMS-consent text, no Homebase CTA**) → name confirmed (email + role auto-skipped, already on file) → availability NL parse ("weekdays after 3pm, all day Sat & Sun" → Mon–Fri 15:00–21:00, Sat/Sun 09:00–21:00) → confirm → time-off "no" → **complete**: 7 availability rows written, `onboarding_complete` logged, session cleared, manager notified ("54.0h/week saved"). Verified in Gmail + Supabase.
- **Test B — roster fan-out ("Onboard my team"), PASS.** All 6 sandbox employees set incomplete → manager trigger → multi-employee **confirmation gate** → manager YES → **6 sessions created** (all `email` channel, all at `name_confirm`); welcomes delivered to the two real inboxes (Sam confirmed 11:22), the four `*.demo@example.com` black-holed as expected.
- **Test C — mixed roster (skip already-onboarded), PASS.** 4 employees complete (phone + role + availability), Sam + Riley as new hires (`Aegis_Onboarding_Mixed_Test.sql`) → "Onboard my team" → the gate targeted **exactly `Riley Brooks, Sam Rivera` (2)**; the four onboarded employees were correctly excluded. Verified both in the DB (`onboarding_fanout` `target_employee_ids`) and in the manager gate email ("I'm about to start onboarding for 2 employees: Sam Rivera, Riley Brooks"). Confirms Alexander's spec: Aegis only contacts not-yet-onboarded staff. (Gotcha during the run: a trigger addressed to `sandbox-mgr@` instead of `sandbox@aegis.quriasolutions.com` is silently ignored — Aegis only processes mail to its inbound address.)
- **Design clarified with Alexander (governs the "incomplete" rule — do NOT loosen it):** *Reachable* = has ≥1 contact (phone OR email) — matches the shipped gate. *Fully onboarded* = **both** contacts + role + availability; onboarding's job is to collect whatever's missing. So the phone requirement in `getIncompleteEmployees` is **intentional**, not a bug — the earlier "phone-OR-email = complete" idea was **rejected**. End-state onboarding (once SMS is built) reaches out on whichever contact Homebase has, then collects: the **missing contact**, **availability**, **org role** (listed from Homebase's configured roles; ideally accept a free-text role that matches the list, else present options), and **immediately pressing time-off**. Built today: availability ✅, role-as-numbered-list ✅ (auto-assigns when only one role), time-off ✅. Not built (rides on SMS): collecting the missing contact.
- **Known interim artifact (not a bug to fix now):** until SMS onboarding collects the phone, an **email-only** completed employee stays phone-less → `getIncompleteEmployees` still flags them → "Onboard my team" re-pings them (that's why Test B re-onboarded Riley). Resolves when SMS collection lands. Demo guidance: onboard a specific **new hire**, don't re-run team onboarding against already-email-onboarded staff.
- **Cosmetic follow-up:** `getStepSubject` hardcodes "Welcome to Watermark" in the subject even though the body resolves the real company name — fix to use `companyName`.
- **Reusable Desktop SQL:** `Aegis_Onboarding_Demo_Setup.sql` (single new hire = Riley), `Aegis_Onboarding_Roster_Test.sql` (whole roster), `Aegis_Onboarding_Mixed_Test.sql` (4 onboarded + 2 new hires → exclusion test). Re-run `Aegis_Sandbox_Demo_Reseed.sql` to restore.

### 2026-07-05 — #19 Onboarding: email path unblocked + tested-in-code (IN REVIEW, not deployed)

Diagnose-first: the conversational onboarding flow (`src/workflows/employee-onboarding.ts`: opt_in → name_confirm → email → role → availability → availability_confirm → time_off → complete; router-wired via `initiate_onboarding` / `handleOnboardingResponse` / fan-out confirm) was **fully built but never run end-to-end** (Phase-7 deferred risky fan-out). Blocker for an email demo, confirmed against the live sandbox (only an **email** `company_channels` row exists, no SMS):

- **SMS-only gate (FIXED).** `handleInitiateOnboarding` hard-required an SMS channel and returned "contact support" otherwise — so onboarding couldn't even start on an email-only tenant. Now the SMS channel is optional (`?? ''`); an employee is onboarded over SMS only when they have a phone **AND** the company has an SMS channel, else over email; unreachable-by-either is skipped.
- **SMS-consent opt-in on the email path (FIXED).** Email has no TCPA SMS opt-in requirement, but every session started at `step:'opt_in'` and sent the SMS-consent text ("via SMS… msg & data rates… reply STOP") even to email employees. Email sessions now start at `name_confirm` with `opt_in_confirmed:true` and get a new `sendEmailWelcomeStep` (warm welcome → asks full name). SMS still gates on an explicit YES.
- **Homebase Onboarding tab (FIXED).** `data/tabs/OnboardingTab.tsx` marked an employee "Skipped" when they had no **phone**, ignoring email — an email-only new hire mislabeled before onboarding began. Now reachable-by-phone-OR-email → "Not Started".
- **State:** both repos **tsc clean**. Aegis change in `employee-onboarding.ts` (working tree on `fix/pre-demo-bugs`); Homebase one-liner in `OnboardingTab.tsx` (working tree). **IN REVIEW — not deployed.** Live email round-trip requires the Railway (Aegis) + Vercel (Homebase) deploys.
- **Demo setup:** new Desktop file `Aegis_Onboarding_Demo_Setup.sql` repurposes Riley into an email-reachable new hire (clears phone/role/availability, keeps the Gmail, clears stale onboarding state). Re-running `Aegis_Sandbox_Demo_Reseed.sql` restores Riley. Sandbox has a single role (Lifeguard) → onboarding auto-assigns it and skips the role step (expected).
- **Next:** Alexander merges the Aegis + Homebase changes → deploy → live email test (manager emails "Onboard Riley Brooks" → complete the flow from Riley's Gmail → watch the Homebase Onboarding tab). Cosmetic follow-ups noted: `getStepSubject` hardcodes "Watermark" in subjects; add a 2nd role only if you want the role-pick step on screen (it changes scheduling requirements).

### 2026-07-05 — Soteria/Rules production fixes (Homebase): manager override, Fix-Issues auto-solve, rule-edit json integrity

Live Watermark issue: Jack tried to manually schedule himself on two overlapping shifts (Afternoon 15:00–21:15 over AM Weekday 11:00–15:30 on Jul 6); the Soteria Review blocked it; he tried to "change the rule" via Soteria chat but it kept enforcing. Diagnosed in Homebase (`~/Desktop/homebase`), branch `fix/soteria-override-and-rules` (cut from main), **tsc clean + validator smoke test green — IN REVIEW (not deployed)**:

- **Root cause of the confusion:** the manual-edit validator (`src/lib/soteria/validateScheduleEdit.ts`) enforces only HARD structural constraints (qualification, approved time-off, availability incl. custom, max-hours, **time-overlap double-booking**, banned pairs). It reads **none** of the Rules-page policies — those feed only Soteria's soft LLM *warnings*. And the validate route reloads all data fresh each call, so there is **no stale cache**. Jack's block was the hard time-overlap, which no rule governs — changing `doubles_policy` could never clear it. The real fix for his case is the override.
- **Item 1 — manager override (BUILT).** `ScheduleReviewPanel.tsx`: when hard errors exist, an "Override & Save" button opens a delete-schedule-style confirmation requiring a typed reason; save proceeds past the errors and logs the override server-side via new route `/api/schedule-override-log` (auth-checked, service-role activity_log insert: who/when/reason/overridden issues). Overrides EVERYTHING at the manager's discretion (per Alexander).
- **Item 1b — "Fix Issues" auto-solve (BUILT).** New route `/api/soteria-fix-schedule`: DETERMINISTIC (not LLM) — repeatedly removes the specific edit-introduced assignment causing each hard error, re-validating with `validateScheduleEdit` until zero errors, preferring to undo the manager's just-added/moved shifts. Returns corrected assignments + a plain-English summary. Wired to the panel's Fix Issues button via new `onApplyFix` prop → `setPendingAssignments` in `schedule/page.tsx`; re-runs the check after.
- **Items 2 & 3 — the real latent rule bug (FIXED).** `soteria/execute/route.ts` `update_policy` (and the setup-plan policy path) only wrote `policy_value_json` when the LLM's action included that key; if omitted, it changed only the display `policy_value` and left the engine-read `policy_value_json` STALE — so the Rules page showed the rule changed while the engine (which reads only json — see Aegis `constraints/parser.ts`) kept enforcing the old value. Fix: a `STRUCTURED_POLICY_KEYS` guard (mirrors the parser's 7 keys) refuses a text-only write for structured rules (update_policy returns 422; setup-plan skips with a warning) — so a rule change either fully applies or fails visibly, never silently stale.
- **Note:** `homebase/CLAUDE.md` shows as modified in the working tree but is a **pre-existing** change (not from this work) — excluded from the commit.
- **Next:** push `fix/soteria-override-and-rules` → PR → Vercel; live-verify the override + Fix-Issues + a rule edit on the Watermark tenant. Then **#19 test & build onboarding**.

### 2026-07-01 (session 2) — Finished the email-workflow sweep (7/8) + fixed 4 pre-demo bugs

Completed the 11.7 remainder on the sandbox via real email round-trips (Claude drove Gmail + the `sandbox-mgr@` M365 mailbox, verified every step against Supabase). **7 of 8 workflows PASS end-to-end; #11 coverage sent from the manager but unconfirmed (SendGrid inbound backlog, not a logic bug).** Details + the results table are in `EMAIL_WORKFLOWS_TRACKER.md` (2026-07-01 session 2).

- ✅ Verified this session: **#14.5** availability≠time-off (full chain → `custom_availability` written), **#14.5b** partial time-off "after 4pm" (`partial_days 16:00–21:00`, approved), **#14.5c** combined message (time-off + P.S. asking for availability separately), **#12** "what are my shifts?", **banned-pair FLAG** on pickup (⚠️ flag-don't-force, both buttons live), **manager DENIAL** (both employees notified, `status=denied`). The `/webhooks/decision` pages are now **branded** (dark theme + logo + confirm interstitial) — the "unbranded" note in the prior entry is resolved.
- **Four bugs found + fixed** — branch `fix/pre-demo-bugs` (cut from main), tsc clean, **192/192 vitest**, **IN REVIEW** (coded + tested; NOT deployed/live-verified yet):
  1. **BUG A (weekday resolution) — NOW FIXED.** Bare weekday ("my Saturday shift") resolved to today. Root cause is LLM weekday arithmetic, not missing guidance. Fix (`src/ai/claude.ts` `upcomingWeekdayLines`): compute every weekday's date in code and hand the model a lookup table in the classifier prompt. (Closes the OPEN BUG A from the entry below.)
  2. **Approved swaps didn't persist to the schedule.** `public.schedules` had 3 `status='published'` rows for one week (publish never superseded the prior row), so the swap-writer and shift-reader could diverge onto different rows. Fix (`src/workflows/schedule-build.ts`): both publish paths archive any other published row for the week first; `executeScheduleSwap`/`executeScheduleTrade` (`shift-swap.ts`) now `console.warn` instead of silently returning. Schema fact in `SCHEMA_DRIFT_LOG.md` (2026-07-01 session 2). **Existing duplicate rows are pre-existing data — collapse during #18.**
  3. **Stale `avail_pending_mgr` hijacked the manager's next email** (magic-link approval didn't delete the pending record; reply-YES did). Fix (`employee-onboarding.ts`): cleanup moved into the shared `applyCustomAvailabilityDecision` so both paths self-clean.
  4. **`swap_denyd` typo** in the decision activity log. Fix (`decision.ts`): proper past-tense mapping (swap + time-off).
- **Owed for DONE:** push `fix/pre-demo-bugs` → PR → merge → Railway deploy, then a live re-verify of the two behavioral fixes (weekday + swap persistence) during the **#18 sandbox reset**. Then **#17 user handouts**.
- **Next:** #17 user handouts (starting now), then #18 sandbox reset + live re-verify of the bug fixes.
- **Update (later 2026-07-01):** #17 handouts **DONE** — two Watermark-specific, send-ready PDFs (employee + manager) with faithful reproductions of the real Aegis emails (built from `brand.ts` primitives: dark shell, real logo, action cards, buttons, decision pages). Saved to the project folder. Per Alexander: employee guide now covers **directed swaps** (name-a-buddy trade), and **payroll was removed** from the manager guide. Added roadmap **#19 (TEST & BUILD ONBOARDING — the new immediate next priority, ahead of #18)** and **#20 (PAYROLL SYSTEM BUILD — after the Homebase overhaul #15)**.

### 2026-07-01 — SWAP tests + real email infra; TWO bugs found (one fixed + verified)

Continued the 11.7 live sweep (Claude drove the browser + verified against the DB). **Email infra is now real M365 shared mailboxes** — `monitor1@` (Watermark monitoring) and `sandbox-mgr@` (sandbox manager) exist in Exchange with Full Access for `awdarling@`, mapped into Outlook; DB rows point at them. (Gmail-per-client plan abandoned — see corrected `TEST_IDENTITIES.md`.)

- **TEST 3 — undirected swap, full two-way trade: ✅ PASS.** Sam broadcasts Sat AM (willing Fri) → Riley two-button email → SWAP → Riley picks Fri PM → Sam Agree → manager Approve → **schedule traded** (Sam↔Riley), verified in `schedules`.
- **MULTI-DAY targeting: ✅ PASS.** Reseeded so Riley=Fri AM, Casey=Wed PM, Jordan=Thu PM; Sam offers Sat PM willing Fri/Wed/Thu. DB confirms `swap_trade_select` minted for exactly Riley+Casey+Jordan and `swap_pickup` for all 5; Morgan/Taylor (no willing-day shift) correctly got pickup-only. Aegis emails exactly the right people across multiple days.
- **⚠️ BUG A (OPEN) — swap requester-shift weekday→date resolution.** "my Saturday PM shift" resolved "Saturday" to TODAY (Tue Jun 30) and failed; "Saturday July 4 PM" (explicit date) worked. Single-weekday swaps (Test 3) were fine — the multi-weekday willing-days list corrupts the requester-shift date extraction. Willing-days themselves resolved fine ("Sam can work: Jul 1, Jul 2, Jul 3"). **Needs a classifier (`src/ai/claude.ts`) fix** so a bare weekday resolves to the correct upcoming date for `initiate_swap`.
- **✅ BUG B (FIXED + VERIFIED) — swap manager-approval left its request orphaned.** `swap_requests.decided_by` is a **UUID** column; `decision.ts` wrote the string `'manager'` (and `shift-swap.ts` auto-approve wrote `'aegis'`) → invalid-uuid throw, silently swallowed → the schedule traded but the `swap_request` stayed `pending_manager` forever (decided_at null). **Fix:** write `null` (nullable UUID; the decision is captured by `decided_at` + the activity log). Branch `fix/swap-decided-by-uuid`, tsc clean, merged + deployed. **Re-verified via a pickup → manager Approve: the `swap_request` now flips to `approved` with `decided_at` set.** Schema fact logged in `SCHEMA_DRIFT_LOG.md`.
- **Deliverability/UX findings:** Aegis email consistently lands in Gmail's **Promotions** tab (both Sam & Riley); the `/webhooks/decision` approval page is **unbranded** (plain white) unlike every other Aegis landing page; swap subject reads "a AM shift" (→ "an AM"); first delivery to a brand-new shared mailbox lagged ~1–2 min (one-time).
- **Still to test via email (11.7 remainder):** #11 coverage "send next batch" buttons, #12 "what are my shifts?" (sandbox), #14.5 availability-vs-time-off, #14.5b partial "after 4pm", #14.5c combined. Then #17 handouts → new **#18 sandbox reset for live demo**.

### 2026-06-30 (cont.) — LIVE SANDBOX TEST RUN: monitoring BCC + magic-link approve PASSED

Ran the 11.7 sweep live (Claude drove the browser; emails sent from the real test inboxes). Email infra verified first: both M365 **shared mailboxes** `monitor1@` (Watermark monitoring) and `sandbox-mgr@` (sandbox manager) exist in Exchange, are deliverable, have Full Access for `awdarling@`, and were mapped into Outlook. DB rows already pointed at them correctly (no UPDATE needed).

- **TEST 1 — Watermark monitoring BCC (item #16): ✅ PASS.** `aegisscheduler@gmail.com` (Watermark "Test Tester") emailed the Watermark Aegis address "what are my shifts this week?" → Aegis replied (warm, correct empty-state) AND the BCC copy landed in `monitor1@`'s inbox. Monitoring first slice is now **verified live end-to-end**, not just built.
- **TEST 2 — magic-link approve/deny (`approve_to` token): ✅ PASS.** Sam (sandbox) emailed "I need Thursday July 2 off" → Aegis confirm-asked → "Yes" → manager email with Approve/Deny rendered in `sandbox-mgr@` → clicked Approve → click-guard Confirm → token `7762c27d` `consumed_at` set, `time_off_requests` (Jul 2) → `approved` with `decided_by` = sandbox manager, Sam notified. The full manager card (coverage impact, Aegis recommendation, manager-only Homebase link) renders correctly.
- **Findings (for item #17 docs / deliverability):** (a) Aegis mail sometimes lands in Gmail's **Promotions** tab — employees may miss it; consider header/deliverability tuning or a "move to Primary / check Promotions" note in the employee guide. (b) **First** delivery to a brand-new Exchange shared mailbox lagged ~1–2 min (subsequent deliveries instant) — not a bug, just first-message latency.
- **Next:** TEST 3 — undirected swap smoke (item 10.5).

### 2026-06-30 — Git hygiene done + ⚠️ correction: swap Stage 4b is ALREADY LIVE (not held)

This entry supersedes the "Stage 4b held unmerged" claim in cont.7 / cont.8 / cont.14 below.

- **#14.5c is LIVE on `origin/main`.** It merged via **PR #70** from branch `feat/combined-timeoff-availability` (commit `ab253ef`) — NOT the recovery branch `feat/combined-availability-fix` the cont.15 note guessed. Confirmed: `availabilityFollowupNote` + `also_mentions_availability` present on `origin/main` across `claude.ts`, `time-off.ts`, `partial-window.test.ts`. The recovery branch was a redundant parallel copy of the same fix (byte-identical code).
- **Git hygiene:** local `main` had diverged onto the redundant recovery lineage (`27e6e1e` / `5df2132` / `f3e8c0a`). Reset local `main` to `origin/main` (`8503782`). Re-added item **#17** (Aegis user handouts) and the **cont.14 / cont.15** handoff notes (they existed only on the abandoned local lineage, never on `origin/main`). Pruned redundant duplicate branches.
- **⚠️ CORRECTION — swap Stage 4b cut-over is NOT held; it is ALREADY ON `origin/main` and LIVE.** Verified four ways: (1) the held branch's tip `df547ac` is a direct ancestor of `origin/main`; (2) `src/workflows/shift-swap.ts` is byte-identical between `origin/main` and the branch; (3) `origin/main`'s `shift-swap.ts` contains the cut-over — the "SIMULTANEOUS two-button broadcast (#10 Stage 4b)" facilitated branch calling `partitionSwapCandidates` + `buildSwapBroadcastEmail`; (4) it rode in with **PR #65** (`feat/swap-agreement-stage4a`) — the 4b commit was stacked on top of the 4a commit, so merging "4a" carried 4b. There was never a separate 4b merge, which is why it was believed held.
- **Consequence:** the undirected swap broadcast has been **live in production (untested) since ~2026-06-28**. No real employees use undirected swaps yet, so likely no harm — but the 10.5 / 11.7(b) sandbox smoke is now MORE urgent, and the planned "merge Stage 4b to go live" step is a **no-op** (already live). The `feat/swap-broadcast-stage4b-cutover` branch is redundant (fully merged) and deletable whenever Alexander wants.
- **⚠️ Aegis `main` is now GitHub branch-protected (discovered 2026-06-30):** a direct `git push origin main` is rejected with "Changes must be made through a pull request." Aegis now follows the same feature-branch → PR → merge flow as Homebase. `CLAUDE.md`'s "Deploy & danger zones" was corrected to match. These doc updates themselves had to land via a PR (`docs/session-2026-06-30`).
- **Item #14.7 (Telnyx) expanded:** the 10DLC-registration landing page currently shows a hardcoded dedicated number; since each client gets their own Aegis number, the page must be made number-agnostic / per-client to stay compliant. Folded into #14.7 (SMS-migration work), not a new item.
- **Email-account infra fix (Alexander, 2026-06-30):** the `sandbox-manager@quriasolutions.com` users-row email was never a real mailbox, so Aegis manager-approval emails went into a void (testing was blocked). Custom-domain inboxes need paid Google Workspace; decision = **one FREE `@gmail.com` per client/role**. Two DB changes prepared (Alexander runs SQL): (1) SANDBOX — `public.users.email` for `3aa0b57d-…` → **quriatesting@gmail.com** (login stays on the `auth.users` address; only Aegis delivery moves); (2) ⚠️ WATERMARK/PROD — `company_monitoring_inboxes` row `c110f0ee-…` email → **quriamonitor1@gmail.com** (the live monitoring BCC; production write, human-gated, Alexander runs it). `TEST_IDENTITIES.md` updated (manager email, monitoring-inbox section, and the stale sandbox roster corrected to the live Sam Rivera / Riley Brooks + demo guards).
- **Item #16 finding:** the monitoring "first slice" (table + `sendEmail` BCC fan-out) is already BUILT + LIVE — the roadmap had it as dormant. Corrected item #16 + logged the `company_monitoring_inboxes` table in `SCHEMA_DRIFT_LOG.md`.
- **Sandbox state for the 11.7 sweep (verified vs live DB):** active published schedule `669dc428` (week Sun 6/28–Sat 7/4). Sam Rivera = Tue 6/30 AM, Thu 7/2 AM, Sat 7/4 AM; Riley Brooks = Mon 6/29 PM, Fri 7/3 PM. The brief's swap script ("Sam's Sat PM, can work Monday") doesn't match — adjusted to "Sam gives away Sat 7/4 AM, can work **Friday**" so Riley (Fri 7/3 PM) gets both pickup + swap buttons.
- **Today's plan:** finish git hygiene → run the 11.7 sandbox sweep (5 live fixes + the swap broadcast end-to-end) → then item #17 handouts. Nothing new to build.

### 2026-06-28 (cont. 15) — ⚠️ NEXT SESSION — FIRST STEPS (git hygiene + desktop cleanup)

Do these before any new build:
1. **Confirm #14.5c landed.** It was committed to LOCAL main (commit `27e6e1e`) by accident (the `git checkout -b` failed because the branch already existed, so the commit fell onto main; the push pushed an empty old branch). Recovery handed to Alexander end of 2026-06-28: put `27e6e1e` on a fresh branch `feat/combined-availability-fix`, push, PR, merge. **Verify origin/main now contains `availabilityFollowupNote` + `also_mentions_availability`** (`git grep availabilityFollowupNote origin/main`). If missing, the #14.5c changes are still only on local — recover from commit `27e6e1e` or the branch. *(Resolved 2026-06-30 — landed via PR #70 from `feat/combined-timeoff-availability`; see the 2026-06-30 entry above.)*
2. **Un-diverge local main:** `git fetch origin && git checkout main && git reset --hard origin/main` (safe — the #14.5c work is preserved on the branch/PR). Local main had drifted ahead by the stray direct commit. *(Done 2026-06-30.)*
3. **Prune branches:** delete the stale/empty `feat/combined-timeoff-availability` (origin + local) and any already-merged feature branches (`git branch --merged main`, then delete). Leave `feat/swap-broadcast-stage4b-cutover` ALONE — it's the deliberately-held cut-over. *(Note 2026-06-30: that branch is actually already merged/live, not held — kept anyway per instruction.)*
4. **Desktop / repo folder cleanup (Alexander asked 2026-06-28):** the Aegis working dir has accumulated loose untracked files (SANDBOX_*.sql, WATERMARK_*/Watermark_* guides + PDFs, `outputs_build_handouts.py`, `preview/`, `scripts/preview-distribute.ts` + `send-test-distribute.ts` + `_distribute-fixture.ts`, a stray file literally named `bed74b9`, `emp_pg-1.png`/`mgr_pg-1.png`). Review with Alexander — keep what's wanted, move drafts out of the repo or `.gitignore` them, delete the stray `bed74b9`. Also the old Desktop worktrees from earlier (`Aegis-audit`, `-bug5`, `-waxseal`, etc.) — remove if still present. **Do NOT delete anything without confirming with Alexander first (his loose drafts live here).**

### 2026-06-28 (cont. 14) — END OF DAY: state of the world + a recovered-work flag

- **⚠️ #14.5c was reported "pushed and merged" but did NOT land** — main's tip is the #14.5b merge (PR #69); the #14.5c code + roadmap notes were absent from the repo AND the working tree (clean on main, zero grep hits). The git steps dropped it somewhere. **Re-created this session and verified (187/187 green, tsc clean); it is now in the working tree but UNCOMMITTED / unpushed.** First thing next session: push it (branch `feat/combined-timeoff-availability`) + these doc updates, then confirm PR #70-ish merged. *(Resolved 2026-06-30 — merged via PR #70.)*
- **MERGED & LIVE on main (Aegis):** #11 (coverage "send another batch" buttons), #12 (employee `query_my_shifts`), #14.5 (positive availability ≠ time-off), #14.5b (open-ended partial time-off). Swap Stages 1–4a are merged but **INERT**. Homebase: swap landing pages (3a/3b/4a) merged. *(Correction 2026-06-30: Stage 4b is also merged/live, not inert — see the 2026-06-30 entry.)*
- **HELD UNMERGED (deliberate):** swap **Stage 4b cut-over** (`feat/swap-broadcast-stage4b-cutover`) — the switch that activates the broadcast. Do NOT merge until the 10.5 sandbox smoke passes. All future work branches from `main` so it never rides along. *(❌ INCORRECT — see 2026-06-30: 4b was already on main via PR #65; the broadcast is live.)*
- **PENDING PUSH (re-created tonight):** #14.5c combined time-off + availability (claude.ts, time-off.ts, partial-window.test.ts, DEV_ROADMAP.md).
- **NOT verified live yet — the 11.7 sandbox checklist:** (a) #11 batch buttons, (b) #10 swap [+4b merge], (c) #12 shift query, (d) #14.5 availability, (e) #14.5b partial, (f) #14.5c combined. Run when Alexander is back at the sandbox (needs a fresh sandbox week with Shmubba + Test Guard B both scheduled; three real inboxes ready — see 10.5).
- **Build list status:** #10–#13 done (code-complete); #14.5/b/c (Quin's three reported bugs) done. **Remaining roadmap:** 10.5 + 11.7 (testing), #14 doc rewrite (waits for testing per North Star), #14.7 Telnyx privacy copy (separate marketing repo — not reachable from this workspace; needs the final Telnyx number), #15 Homebase UI overhaul (big, design-mode).
- Gotcha logged: the classifier spec lives INSIDE a template literal — never use backticks in that prose (breaks the build; hit + fixed this session).

### 2026-06-28 (cont. 13) — Item #14.5c: Quin M — combined time-off + availability email merged into one wrong range

- From Quin's June 15 screenshot: "Time off: June 18-21. Availability: 6/22 morning, 6/23 morning, 6/24 all day, 6/25 all day, 6/26 morning" → Aegis confirmed "June 18 through June 26 (09:00–13:00) off" — it merged the availability dates into the time-off range AND applied "morning" to the whole thing. Two distinct requests (time off + availability) in one email; the classifier returns one intent and mashed them together.
- **Fix (pragmatic, no multi-intent rearchitecture):** `claude.ts` — combined-message rule + `also_mentions_availability` flag + Quin's example: extract ONLY the time-off dates (18–21 full days), never extend the range with availability dates or apply availability times to off-days. `time-off.ts` — `handleSubmitTimeOff` appends a P.S. (pure `availabilityFollowupNote`) asking the employee to send their availability in its own message. So Quin gets a CORRECT time-off confirmation + a clear nudge for the availability half. 2 unit tests; 187/187 green, tsc clean. Aegis-only, branch `feat/combined-timeoff-availability`, independent of the held swap cut-over.
- **Still a follow-up (logged):** truly handling BOTH requests from one email (running time-off AND availability workflows on one inbound) is multi-intent routing — deferred; the nudge-to-resend is the interim.
- (Build gotcha noted: the classifier spec lives INSIDE a template literal — never use backticks in that prose; it breaks the string. Hit + fixed this session.)

### 2026-06-28 (cont. 12) — Item #14.5b: Quin M — open-ended partial time-off ("after 4pm") read as a full day

- From Quin's June 24 screenshots: "Time off June 29 after 4pm" → Aegis confirmed a FULL-day off. **Root cause (two halves):** (1) the classifier's submit_time_off spec had rules for full-day, named periods, and explicit ranges ("10am to 1pm"), but NONE for one-sided windows ("after 4pm", "before noon", "until 2pm"); (2) even if the model emitted a one-sided window, `resolvePartialWindow` (time-off.ts) required BOTH start and end, so a start-only window returned null → collapsed to full day.
- **Fix:** (1) `claude.ts` — new spec rules + examples for open-ended partials ("after 4pm" → start_time 16:00, end_time null; "until 2pm" → start_time null, end_time 14:00), with explicit "never collapse after/before <time> to a full day." (2) `time-off.ts` — `resolvePartialWindow` now fills the open side from the operating day (`DAY_OPEN` 09:00 / `DAY_CLOSE` 21:00, kept in sync with PERIOD_TIMES): start-only → through close; end-only → from open. Exported + 5 unit tests (incl. Quin's "after 4pm" → 16:00–21:00). 185/185 green, tsc clean. Aegis-only, branch `feat/partial-timeoff-fix`, independent of the held swap cut-over. Added to the 11.7 sandbox checklist.

### 2026-06-28 (cont. 11) — Item #14.5: Quin M bug — positive availability misclassified as time-off

- Scoped from Alexander's screenshots: Quin (Watermark) sent a positive availability statement ("…I can work Monday 11–3:30, Wednesday…, and Thursday") and Aegis replied with a time-off confirmation. Root cause: the classifier's "specific date wins → submit_time_off" rule + an "indirect partials" rule pushed positive "I can work …" into time-off.
- Fix in `src/ai/claude.ts`: new positive-availability prompt rule (always update_availability, even with dates; week-bound → date-limited end_date), clarified indirect-partials rule, examples incl. Quin's message, and a deterministic backstop (`looksLikePositiveAvailability` + `applyAvailabilityBackstop`) that reclassifies a mis-fired submit_time_off and abstains on any off/can't/mixed message. 8 unit tests, 180/180 green, tsc clean. Aegis-only, branch `feat/availability-vs-timeoff-fix`, independent of the held swap cut-over.
- Logged harder follow-up: combined "time off + availability in one email" (multi-intent) is NOT fixed; appended #14.5 to the 11.7 sandbox checklist (LLM classification needs a live check).

### 2026-06-28 (cont. 10) — Item #12: employee "what are my shifts?" (Aegis-only) + verification-pass item 11.7

- Added standing **item 11.7** (VERIFICATION PASS): batch the sandbox testing of build-build-build features; append each built-but-untested feature to its checklist. Currently listed: #11 batch buttons, #10 swap (via 10.5), #12 shift query.
- **#12 built:** new employee intent `query_my_shifts` → `handleMyShiftsQuery` (operational-query.ts) returns the employee's own upcoming shifts (or a specific day) from published schedules in a warm, no-Homebase-CTA reply. Pure `formatMyShiftsReply` + 5 tests; classifier disambiguation vs operational_query / query_my_time_off + examples. 172/172 green, tsc clean. Aegis-only, branch `feat/employee-shift-query`, independent of the held swap cut-over.

### 2026-06-28 (cont. 9) — Item #11: request-an-additional-batch (email-button), Aegis-only

- Built the email-button version of the coverage "send another batch?" prompt. `promptForNextBatchOrExhaust` now sends email managers a branded prompt with **Send next batch / No, I've got it** buttons (mints two `decision_token`s, `decision_type: 'coverage_batch'`); SMS managers + the reply-YES/NO path are unchanged (both still work).
- `/webhooks/decision` gains a `coverage_batch` branch → `handleCoverageBatchDecision` → `processCoverageBatchButton(companyId, managerContact, send|stop)`, which loads the live session, and for "send" reconstructs the manager context and reuses `blastNextBatch`; "stop" clears the session. New result page. Single-use tokens (consumed on click).
- Pure `classifyCoverageBatchButton(session, action)` → not_found / stopped / sent / exhausted, with 4 tests (`coverage-button.test.ts`). 167/167 green, tsc clean.
- **Aegis-only; does NOT touch `shift-swap.ts`** — branch `feat/coverage-batch-button` cut from `main`, safe to merge without shipping the held swap cut-over. Remaining: merge + live eyeball.

### 2026-06-28 (cont. 8) — Pivot: defer swap activation (10.5), branch-guard future work off main

- Alexander is away from the sandbox and can't test now. **Decision:** defer the undirected-swap sandbox smoke + Stage 4b merge to a new tracked item **10.5**; keep building other roadmap items meanwhile.
- **Branch-guard (standing rule):** Stage 4b (`feat/swap-broadcast-stage4b-cutover`) stays UNMERGED. All future feature branches are cut from `main` (which has Stages 1–4a, inert, but NOT the cut-over), so unrelated pushes/deploys can never ship the untested broadcast switch-on. Every handoff already does `git checkout main && git pull` before `git checkout -b`, which enforces this.
- Production swap state today: machinery present + inert; the live facilitated path still uses the old one-at-a-time outreach until 4b is merged. No live behavior change outstanding.

### 2026-06-28 (cont. 7) — Item #10 redesign: Stage 4b (LIVE CUT-OVER — broadcast fan-out is now on)

- **⚠️ First stage that changes live behavior. Aegis-only** (Homebase already shipped the pages/dispatchers in 3a/3b/4a). The undirected ("facilitated") swap path is rewired in `src/workflows/shift-swap.ts`:
  - `handleInitiateSwap` (facilitated): captures `raw.willing_days` into the `PendingSwap`; confirm copy explains trades will be offered on those days, or that it'll go out pickup-only if none were given.
  - `handleSwapConfirmation` (facilitated): the old one-at-a-time `SwapOutreach` is **replaced** by the simultaneous broadcast — `buildSwapCandidates` → filter reachable (`isReachableForOutreach`) → load the published schedule (assignments-by-employee + week dates) → `resolveWillingDates` → `partitionSwapCandidates` (pickup = all; swap = those with a tradeable shift) → `buildSwapBroadcastEmail` to EVERY reachable candidate (tailored buttons, email-first) → `storeSwapBroadcast` (status open) → requester gets a "emailed N teammates" confirmation. Pickup-only fallback when no willing-days.
  - **Directed (named-coworker) trades are untouched** — they still use `SwapOutreach` + `handleSwapOutreachResponse` (the reply-YES/NO path). That path's facilitated branch is now dead but harmless.
- **Whole feature now wired end-to-end:** requester broadcasts → teammates get the two-button email → PICKUP (→ manager → one-way reassignment) or SWAP (pick a shift → requester Agree/Decline → manager → two-way trade); first commit locks; requester-decline reopens.
- **Tests:** Aegis **163/163** green, tsc clean. The new orchestration leans on already-unit-tested helpers (`partitionSwapCandidates`, `resolveWillingDates`, `buildSwapBroadcastEmail`); end-to-end is covered by the sandbox smoke below.
- **DO NOT MERGE TO LIVE UNTIL A SANDBOX SMOKE PASSES.** Branch (Alexander to push): Aegis `feat/swap-broadcast-stage4b-cutover`. Smoke (sandbox tenant, TEST_IDENTITIES only): (1) a sandbox employee emails "anyone want my <shift> on <date>? I can work Mon/Tue" → confirm Aegis replies "emailed N teammates"; (2) a test-account teammate gets the two-button email → click **pick it up** → confirm page → manager gets approve/deny → approve → schedule reassigns + both notified; (3) repeat with **swap** → picker page → choose a shift → requester gets Agree/Decline → Agree → manager → trade executes; (4) test **decline** → broadcast reopens. Only after that is `swap` truly **DONE** end-to-end.
- **Status: ALL STAGES (1, 2, 3a, 3b, 4a, 4b) BUILT.** #10 is code-complete; remaining is the sandbox smoke + merge.

### 2026-06-28 (cont. 6) — Item #10 redesign: Stage 4a (requester Agree/Decline → manager approve → execute / reopen)

- **Aegis (`shift-swap.ts`, `webhooks/internal.ts`, `aegis-actions/types.ts`):**
  - Added `swap_agree` / `swap_decline` action types.
  - `buildSwapProposalEmail(...)` — the REQUESTER's "do you agree to this trade?" email (Agree/Decline magic-link buttons, employee voice, no Homebase CTA).
  - `proposeSwapTrade` now (after recording the proposal) loads the requester's email and SENDS them the Agree/Decline email — closing the candidate-proposes → requester-asked loop.
  - `resolveSwapProposal({company_id, requester_id, decision})`: **agree** → create the two-way `swap_request` (pending_manager) + `sendManagerSwapApprovalRequest` with `target_*` (manager approval → existing `webhooks/decision.ts` trade path → `executeScheduleTrade`) + tell the candidate it's pending manager + clear the proposal; **decline** → REOPEN the broadcast (`status:'open'`, `locked_by:null` — other candidates' unconsumed email buttons work again) + tell the candidate the trade's off + clear the proposal.
  - New endpoint `POST /internal/swap-proposal-decision`.
- **Homebase (`tokens.ts`, `aegis-internal.ts`, `dispatcher.ts`, `route.ts`):** `swap_agree`/`swap_decline` added to `ActionType` + the endpoint union; `describeAction` confirm copy ("Agree to trade your X for Y? (Then it goes to your manager.)" / "Decline this trade and keep your shift?"); dispatcher `swap_agree`/`swap_decline` cases → `handleSwapProposalDecision` → `POST /internal/swap-proposal-decision`. Standard confirm→dispatch pattern (single button) — no new page needed.
- **Tests:** added a `buildSwapProposalEmail` test (mints swap_agree/swap_decline, both buttons, no Homebase CTA). Aegis **163/163** green, tsc clean both repos.
- **Still inert in prod** — the agreement chain is fully built but nothing reaches it until the Stage 4b cut-over turns the broadcast on. Safe to merge. Branches (Alexander to push): Aegis + Homebase `feat/swap-agreement-stage4a`. **Deploy Homebase first.**
- **Next — Stage 4b (THE CUT-OVER — this one changes live behavior):** in `handleInitiateSwap`, capture the requester's willing-days (ask if missing) for the undirected case; in `handleSwapConfirmation` (facilitated branch), replace the old one-at-a-time outreach with the simultaneous broadcast fan-out — build candidates, `partitionSwapCandidates`, store the `SwapBroadcast` (status open), and `buildSwapBroadcastEmail` to EVERY eligible candidate (tailored buttons). After 4b ships (Homebase already has the pages), the whole feature is live end-to-end — needs a careful sandbox smoke before/after merge.

### 2026-06-28 (cont. 5) — Item #10 redesign: Stage 3b (SWAP shift-picker landing page + proposal recording)

- **Aegis (`shift-swap.ts`, `webhooks/internal.ts`):**
  - `buildSwapBroadcastEmail` now embeds a self-contained snapshot in BOTH token payloads (requester's shift in human `date` + raw `shift_date`, names, identity) and the candidate's `tradeable_shifts` in the swap token — so the picker page needs no extra fetch. New `TradeableShiftOption` type + `tradeableShifts` param.
  - `SwapProposal` interface + `storeSwapProposal`/`getSwapProposal` (aegis_memory `swap_proposal:{requesterId}`, TTL-aware).
  - `proposeSwapTrade({company_id, requester_id, receiver_id, selected_shift})` — guard (first-commit-wins) → lock the broadcast → record the proposal (requester's shift + the receiver's chosen shift + requester contact) → return the candidate's confirmation message. (Asking the requester to agree is Stage 4.)
  - New internal endpoint `POST /internal/swap-propose` → `proposeSwapTrade` (validates the selected_shift shape).
- **Homebase (`route.ts`, `dispatcher.ts`, `aegis-internal.ts`):** new interactive `swapPickerPage` (GET for `swap_trade_select`) — renders the candidate's tradeable shifts as radio cards + "Propose this trade"; gracefully shows "nothing to trade — use pickup instead" when the list is empty. POST resolves the chosen index **server-side against the same token payload** (doesn't trust client-sent shift details) → `dispatchSwapProposal` → `POST /internal/swap-propose`. Added the endpoint to the `AegisInternalEndpoint` union.
- **Tests:** extended `swap-broadcast-email.test.ts` to assert the swap token carries `tradeable_shifts` + the enriched requester-shift fields. Aegis **162/162** green, tsc clean both repos.
- **Still inert in prod** (no fan-out yet) → safe to merge, nothing live changes. Branches (Alexander to push): Aegis + Homebase `feat/swap-picker-stage3b`. **Deploy Homebase first.**
- **Next — Stage 4 (the agreement chain + cut-over):** ask the requester to Agree/Decline the proposal (magic-link `swap_agree`/`swap_decline`); on agree → two-way `swap_request` + manager approval (existing `webhooks/decision.ts` trade path) → `executeScheduleTrade`; on decline → **reopen** the broadcast to remaining candidates. THEN the live cut-over: replace the old one-at-a-time facilitated outreach in `handleSwapConfirmation` with the simultaneous broadcast fan-out (`buildSwapBroadcastEmail` to every eligible candidate, partitioned), and add the requester willing-days question in `handleInitiateSwap`. After Stage 4 ships (Homebase-first), the feature is live end-to-end.

### 2026-06-28 (cont. 4) — Item #10 redesign: Stage 3a (PICKUP landing page end-to-end, cross-repo)

- **Stage 3 split:** 3a = the one-way PICKUP path (fits the existing GET-confirm → POST-dispatch magic-link pattern). 3b (next) = the interactive SWAP shift-picker page (custom multi-option UI).
- **Aegis (`src/workflows/shift-swap.ts`, `src/webhooks/internal.ts`):**
  - `swapBroadcastCommitGuard(broadcast)` — pure first-commit-wins gate (open → allow; locked → "someone grabbed it"; missing → expired). Tested.
  - `commitSwapPickup({company_id, requester_id, receiver_id})` — locks the broadcast, creates the one-way pickup `swap_request` (pending_manager), notifies the requester ("{name} offered to pick up your shift, pending manager approval"), and emails the manager via the existing `sendManagerSwapApprovalRequest` (now exported). Manager approve → `webhooks/decision.ts` already does the one-way `executeScheduleSwap` + notifies both.
  - New internal endpoint `POST /internal/swap-pickup-commit` → `commitSwapPickup`, returns `{ok, message}`.
- **Homebase (`tokens.ts`, `app/api/aegis-action/route.ts`, `lib/aegis-actions/dispatcher.ts`, `lib/aegis-internal.ts`):** added `swap_pickup`/`swap_trade_select` to `ActionType`; `describeAction` pickup-confirm copy ("Pick up {requester}'s {shift} on {date}?"); dispatcher `swap_pickup` case → `handleSwapPickup` → `POST /internal/swap-pickup-commit` (surfaces the Aegis message, incl. the "already grabbed" case); added the endpoint to the `AegisInternalEndpoint` union.
- **Lock caveat (logged hardening follow-up):** the lock is a read-then-write on the aegis_memory broadcast record — a tiny race between two simultaneous clicks remains; the manager approval is the true final gate. A DB-level atomic guard (a `swap_broadcasts` table or RPC) would close it but needs a gated migration — deferred.
- **Tests:** 3 new pure guard tests (`shift-swap.test.ts`, now 34 there). Aegis **162/162** green, tsc clean. Homebase tsc clean (full Homebase test runner still a standing follow-up; verify via `npm run build` on push + Stage 4 sandbox smoke).
- **Still inert in prod:** no email mints these tokens yet (fan-out cut-over is Stage 4), so merging 3a changes nothing live. Branches (Alexander to push): Aegis `feat/swap-pickup-stage3a`, Homebase `feat/swap-pickup-stage3a`. **Deploy Homebase first** by convention.
- **Next — Stage 3b:** the SWAP shift-picker landing page (the candidate's tradeable shifts as action cards) + a `/internal/swap-propose` endpoint that records the proposal and asks the requester to agree. Then Stage 4: requester-agree → manager-approve → execute (reopen-on-decline) + the live cut-over (broadcast fan-out replaces the old one-at-a-time outreach; requester willing-days question).

### 2026-06-28 (cont. 3) — Item #10 redesign: Stage 2 (willing-days parse + two-button broadcast email + token types)

- **Token vocabulary (`src/lib/aegis-actions/types.ts`):** added `swap_pickup` + `swap_trade_select` action types (the candidate's two email buttons; both land on Homebase pages built in Stage 3).
- **Willing-days intake (`shift-swap.ts`):** `extractSwapDetails` now also extracts `willing_days` (weekday ints 0–6 the requester can work in return). Pure resolvers: `resolveWillingDates(willingWeekdays, weekDates)` (weekday → concrete dates in the shift's week) and `weekDatesFrom(weekStart)` (the 7 dates of a week). Tested.
- **`buildSwapBroadcastEmail(...)`:** mints the PICKUP token always + the SWAP token only when `swapEligible`, and renders the branded email with the pickup button always and the swap button only for swap-eligible recipients (Alexander's confirmed per-recipient tailoring). Employee-facing voice, no Homebase CTA. Carries the self-contained broadcast snapshot in each token payload (`receiver_id`, `mode`).
- **Broadcast state persistence:** `storeSwapBroadcast` / `getSwapBroadcast` (aegis_memory, keyed `swap_broadcast:{requesterId}`, TTL-aware) for Stages 3–4.
- **Tests:** 4 new pure-helper tests in `shift-swap.test.ts` (now 31 there) + new `swap-broadcast-email.test.ts` (3 tests: both-tokens/both-buttons when eligible, pickup-only token/button otherwise, willing-days copy). Full Aegis suite **159/159** green, tsc clean.
- **IMPORTANT — these builders are NOT yet cut into the live path.** Nothing calls the broadcast fan-out yet, so merging Stage 2 changes NO live behavior (safe to merge anytime). The cutover (replace the old one-at-a-time facilitated outreach with the simultaneous two-button broadcast + the requester willing-days question) ships in Stage 3/4 **with** the Homebase landing pages, deployed **Homebase-first** (the buttons link to pages that don't exist until Stage 3). Branch (Alexander to push): suggested `feat/swap-broadcast-stage2`.
- **Next — Stage 3:** Homebase landing pages — pickup-confirm page + swap action-card shift-picker — plus the dispatcher cases / Aegis internal endpoints they call (lock the broadcast, create the request, notify). Then Stage 4 (requester-agree → manager-approve → execute, reopen-on-decline) + the live cutover.

### 2026-06-28 (cont. 2) — Item #10 redesign: Stage 1 Aegis groundwork (pure helpers + broadcast state)

- **Scope confirmed with Alexander (3 design Qs):** simultaneous broadcast (first-to-commit locks the shift), web landing pages via the email buttons, manager approval ALWAYS on both pickup + swap, and reopen-the-broadcast if the requester declines a proposed swap. Full spec written into the #10 roadmap entry.
- **Stage 1 built (`src/workflows/shift-swap.ts`, pure + tested — no behavior wired yet):**
  - `tradeableShiftsForCandidate(candidateAssignments, requesterWillingDates, requesterQualifiedRoles)` — the candidate's own shifts the requester could take in a trade (on a willing day + a role the requester is qualified for); these are exactly the swap-page action cards.
  - `partitionSwapCandidates(pickupEligible, assignmentsByEmployee, willingDates, requesterRoles)` → `{ pickup, swap }` — everyone vetted gets pickup (button A); the subset with a tradeable shift also gets swap (button B) with their tradeable shifts attached.
  - `SwapBroadcast` interface — the in-flight broadcast state (`status: open|locked`, `locked_by`, `willing_dates`, `contacted_ids`) the lock/fan-out stages will persist.
- **Tests:** 5 new pure-helper tests in `shift-swap.test.ts` (now 27 in that file). Full Aegis suite **152/152** green, tsc clean. Branch (Alexander to push): suggested `feat/swap-broadcast-stage1`.
- **Next — Stage 2:** willing-days intake/parse + the two-button branded broadcast email + new `swap_pickup` / `swap_trade_select` action-token types + simultaneous fan-out. Then Stage 3 (Homebase landing pages) and Stage 4 (requester-agree → manager-approve → execute, with reopen-on-decline).

### 2026-06-28 (cont.) — Item #10 (undirected swap broadcast) made email-first

- **Diagnose-first finding:** the "anyone want my Saturday?" broadcast (`mode: 'facilitated'` in `src/workflows/shift-swap.ts`) was built but **SMS-only** — it would silently reach no one on Watermark's live email channel. Three SMS-only spots: (1) `handleSwapConfirmation` first-candidate contact (required `contact_phone` + `aegisSmsNumber`, called `sendSms`), (2) `handleSwapOutreachResponse` decline→next-candidate queue advance (same), (3) `executeSwapNow` receiver "you're covering it" notify. The **directed** trade path already solved this with the email-first `sendOutreachMessage` helper; facilitated just never adopted it.
- **Fix:** all three now go through `sendOutreachMessage` (email → SMS fallback). New exported pure helper `isReachableForOutreach(emp, hasSmsChannel)` defines the gate — an email is reachable on its own; a phone only counts when an SMS channel is live (post-A2P). The broadcast picks the first *reachable* candidate and the queue-advance walks past contactless records rather than dead-ending. `SwapOutreach` already carried `receiver_email`/empty `aegis_sms_channel`, so no shape change.
- **Untouched (already email-capable):** directed two-way trades, and the manager-approval execution in `webhooks/decision.ts` (it already does SMS-if-channel-else-email for both parties).
- **State:** 4 new pure-helper tests in `shift-swap.test.ts` (now 22 in that file); full Aegis suite **147/147** green; tsc clean. Change is contained to `shift-swap.ts` + its test. Needs Alexander to push the branch + open a PR, then one live sandbox smoke (employee broadcast → a candidate replies YES by email → schedule reassigned + both notified) to flip #10 to DONE.

## HOMEBASE UI OVERHAUL (backlog — end of roadmap; set 2026-06-16)

**Repo:** Homebase · **Status:** `NOT STARTED` · **Phase tag:** `[P3]` (polish / product-readiness for more clients)

A broad, general modernization of the entire Homebase web app to the Quria look and a cleaner UX — the larger follow-on after the targeted design pieces in NEXT BATCH (rules-management UI, veteran badge, publish button) land. This is deliberately parked LAST so the high-leverage, client-facing operational pieces ship first.

Scope (to be refined when picked up — diagnose-first, page-by-page):
- **Brand system pass.** Apply the Quria palette (black / brushed silver / orange `#f97316` with subtle orange glow) and the orchid logo consistently across every page, not just the schedule view. Single source of truth in `homebase/src/app/globals.css`; retire one-off inline colors.
- **Navigation + information architecture.** Review the app shell / nav, page hierarchy, and empty/loading/error states for consistency.
- **Schedule view polish.** Build on the template-unification work (shared `resolveCellAppearance`) so the on-screen grid, downloads, and emailed schedule are visually identical; tighten the grid, badges (veteran + rule indicators), and the Preview & Edit affordances.
- **Forms + tabs.** Time Off, Availability, Rules, and any settings tabs brought to a consistent component style (inputs, buttons, toasts, confirmations).
- **Magic-link landing pages.** Fold the Approve/Deny pages into the same design system (also covered tactically under the NEXT BATCH branding sweep — this is the comprehensive pass).
- **Responsive / mobile.** Managers open these on phones — verify the core flows (schedule view, approvals) hold up at phone width.
- **Accessibility baseline.** Contrast, focus states, and labels on interactive controls.

**Done when:** the app reads as one cohesive Quria-branded product across all pages, with consistent components, states, and responsive behavior — ready to show prospective clients. Break into per-area sub-tasks when started.
